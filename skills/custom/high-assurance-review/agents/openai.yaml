policy:
  allow_implicit_invocation: false

interface:
  display_name: "High-Assurance Review"
  short_description: "Heavy two-reviewer code judgment"
  default_prompt: "Explicitly use $high-assurance-review for a thorough read-only review of this fixed code candidate before I accept or merge it."
