interface:
  display_name: "Simplify Code"
  short_description: "Cut complexity without changing behavior"
  default_prompt: "Use $simplify-code on this explicitly selected target to apply one behavior-preserving simplification, or specify until-clean with a finite campaign budget for one named region."

policy:
  allow_implicit_invocation: false
