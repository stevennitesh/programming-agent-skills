policy:
  allow_implicit_invocation: true

interface:
  display_name: "Change Review"
  short_description: "Evidence-backed review of one code change"
  default_prompt: "Use $change-review to inspect this selected code change read-only, verify concrete findings, and return a concise result."
