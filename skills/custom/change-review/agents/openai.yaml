policy:
  allow_implicit_invocation: true

interface:
  display_name: "Change Review"
  short_description: "Single fixed-snapshot review gate"
  default_prompt: "Use $change-review to pin this candidate, cover and judge Spec and Standards separately with risk-proportional depth, and return one read-only gate decision."
