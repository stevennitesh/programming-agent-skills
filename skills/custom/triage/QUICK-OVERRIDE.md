# Quick Override

Use when the maintainer names the exact target state.

Read current roles and enough source context to produce the required comment or brief. Skip request verification and grilling.

The command selects the named outcome. Build and display the exact roles, labels, full comment or brief, `.out-of-scope/` change, and close state; wait for explicit approval before mutation. Any packet change requires fresh approval. Keep the normal ready brief, disclaimer, `.out-of-scope/`, close, and read-back gates.

For `implemented`, require an explicit label-correction request and existing closeout evidence.

## Completion

Complete only when the exact approved packet is applied, tracker state is read back, both role invariants hold, every required post, brief, or rejection record exists, and the triage packet is returned.
