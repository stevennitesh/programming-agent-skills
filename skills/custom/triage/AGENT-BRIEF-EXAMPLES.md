# Codex-Ready Brief Examples

Use only the example matching the current branch. [AGENT-BRIEF.md](AGENT-BRIEF.md) is authoritative.

## Bug Tracer Bullet

```markdown
> *This was generated by AI during triage.*

## Codex-Ready Brief

**Category:** bug
**State:** ready-for-agent
**Slice type:** tracer bullet
**Summary:** Preserve word boundaries when truncating skill descriptions

**Current behavior:** Descriptions over the existing limit can end mid-word.

**Verification evidence:**
- Result: confirmed
- Evidence: focused metadata-loading reproduction produces `Use when the user wants to confi`

**Desired behavior:** Truncate at the last word boundary that fits and append `...`. Short descriptions remain unchanged.

**Why this slice:** Proves the user-visible formatting behavior end to end without changing parsing or the length limit.

**Relevant interfaces or seams:**
- Skill metadata loading - produces the displayed description

**Relevant context:**
- Domain terms: skill description, invocation
- ADRs: `none`

**Source Trace:**
- Reporter reproduction and triage comments
- Skill metadata loading contract and focused tests
- Prior rejection: `none`

**Dependencies and parallel safety:**
- Parent: `none`
- Blocked by: `none`
- Expected write scope: metadata loading and focused tests
- Parallel safety: independent

**Acceptance criteria:**
- [ ] Short descriptions remain unchanged.
- [ ] Long descriptions truncate at the last fitting word boundary.
- [ ] Truncated descriptions end with `...` and stay within the existing limit.

**Validation notes:** Prove short and long cases through the metadata-loading interface.

**Out of scope:**
- Changing the length limit
- Multiline description support
```

## Enhancement Tracer Bullet

```markdown
> *This was generated by AI during triage.*

## Codex-Ready Brief

**Category:** enhancement
**State:** ready-for-agent
**Slice type:** tracer bullet
**Summary:** Show tracker work that needs maintainer attention

**Current behavior:** Maintainers manually inspect labels and comments to find unclassified, `needs-triage`, and reactivated `needs-info` work.

**Verification evidence:**
- Result: confirmed
- Evidence: the tracker adapter exposes labels, comments, authors, and timestamps but no combined attention view

**Desired behavior:** Return oldest-first Role drift, `needs-triage`, and reactivated `needs-info` buckets with type and one-line summary.

**Why this slice:** Proves the core read-only attention scan.

**Relevant interfaces or seams:**
- Tracker query interface - supplies item state and reporter activity
- Attention presentation - classifies and summarizes results

**Relevant context:**
- Domain terms: triage, role drift
- ADRs: `none`

**Source Trace:**
- Enhancement request and triage decisions
- Tracker and label contracts
- Existing tracker adapter
- Prior rejection: `none`

**Dependencies and parallel safety:**
- Parent: `none`
- Blocked by: `none`
- Expected write scope: attention-query and presentation behavior
- Parallel safety: independent

**Acceptance criteria:**
- [ ] Role drift includes unlabeled, missing-role, and conflicting-role items.
- [ ] `needs-triage` and reactivated `needs-info` buckets are oldest first.
- [ ] Each item shows type and one-line summary.
- [ ] The scan performs no tracker mutations.

**Validation notes:** Exercise all three buckets with tracker fixtures, including `needs-info` with and without a reporter reply.

**Out of scope:**
- Applying labels
- Posting comments
- Triaging a selected item
```

## Support Slice

```markdown
> *This was generated by AI during triage.*

## Codex-Ready Brief

**Category:** enhancement
**State:** ready-for-agent
**Slice type:** support slice
**Summary:** Add a local tracker fixture harness for triage tests

**Current behavior:** Each triage test hand-builds tracker responses, making role drift and reporter activity difficult to prove consistently.

**Verification evidence:**
- Result: confirmed
- Evidence: focused triage tests duplicate issue, label, comment, author, and timestamp fixtures

**Desired behavior:** Provide a reusable local substitute that represents issues, PRs, roles, comments, authors, and timestamps without live network access.

**Why this slice:** De-risks later attention-scan tracer bullets with independently verifiable infrastructure.

**Relevant interfaces or seams:**
- Tracker adapter seam - behavior the harness substitutes
- Triage fixtures - concise scenario construction

**Relevant context:**
- Domain terms: triage, state transition
- ADRs: `none`

**Source Trace:**
- Triage test suite
- Tracker adapter contract
- Engineering test conventions
- Prior rejection: `none`

**Dependencies and parallel safety:**
- Parent: triage behavior packet
- Blocked by: `none`
- Expected write scope: test harness and focused triage tests
- Parallel safety: likely overlap with sibling triage tests; serialize test-fixture edits

**Acceptance criteria:**
- [ ] Tests can create issue and PR fixtures with roles, comments, authors, and timestamps.
- [ ] Fixtures can represent reporter activity after the latest triage note.
- [ ] The harness makes no live network calls.
- [ ] At least one focused triage test uses it.

**Validation notes:** Run the focused triage tests through the local substitute.

**Out of scope:**
- Replacing the production tracker adapter
- Adding new triage behavior
```

## PR Finish Tracer Bullet

```markdown
> *This was generated by AI during triage.*

## Codex-Ready Brief

**Category:** enhancement
**State:** ready-for-agent
**Slice type:** tracer bullet
**Summary:** Finish JSON error output for the PR's triage-list flag

**Current behavior:** The PR emits JSON for success, but errors remain human text and the flag lacks focused coverage.

**Verification evidence:**
- Result: confirmed
- Evidence: PR diff inspection plus focused command run

**Desired behavior:** With `--json`, success and error output are valid JSON and retain existing exit codes. Default output remains unchanged.

**Why this slice:** Finishes one bounded behavior already started by the PR.

**Relevant interfaces or seams:**
- Triage-list command - preserves default behavior
- JSON serializer - handles success and error paths

**Relevant context:**
- Domain terms: attention scan
- ADRs: `none`

**Source Trace:**
- PR body, decision-bearing comments, and diff
- Focused command results
- Existing serializer and command tests
- Prior rejection: `none`

**Dependencies and parallel safety:**
- Parent: current PR
- Blocked by: `none`
- Expected write scope: JSON error path and focused command tests
- Parallel safety: serialize with other edits to the same command

**Acceptance criteria:**
- [ ] `triage list --json` emits valid JSON for success and error cases.
- [ ] Exit codes match the default command.
- [ ] Default output remains unchanged.
- [ ] Focused tests cover one success and one error.

**Validation notes:** Run focused command tests and parse both JSON outputs.

**Out of scope:**
- Adding `--json` to other commands
- Changing the existing success payload
```
