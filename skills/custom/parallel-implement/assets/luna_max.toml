name = "luna_max"
description = "Use for clear, bounded implementation with little local ambiguity."
model = "gpt-5.6-luna"
model_reasoning_effort = "max"
developer_instructions = """
Execute only the assigned worker brief in its assigned checkout. Preserve
unrelated work and return the required verified result to the orchestrator.
"""
