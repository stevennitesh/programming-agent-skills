interface:
  display_name: "Parallel Implement"
  short_description: "Isolated wavefront with root-owned Lock"
  default_prompt: "Use $parallel-implement to run these ready, semantically independent work items through isolated one-commit lanes, serial integration, root-owned review, Lock, and release."

policy:
  allow_implicit_invocation: false
