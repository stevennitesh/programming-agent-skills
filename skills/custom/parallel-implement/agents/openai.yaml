interface:
  display_name: "Parallel Implement"
  short_description: "Drain one graph with budgeted review"
  default_prompt: "Use $parallel-implement from the root to drain every in-scope implementation ticket for this parent, preserve event-sourced authority, integrate serially, and close only after budgeted review, friction accounting, and verified release."

policy:
  allow_implicit_invocation: false
