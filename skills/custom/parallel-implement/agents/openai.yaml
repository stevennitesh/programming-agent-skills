interface:
  display_name: "Parallel Implement"
  short_description: "Deliver independent items with isolated workers"
  default_prompt: "Use $parallel-implement from the root to deliver this fixed set with isolated concurrent workers where ownership and write effects are independent, one root integrator, rolling serial landings, and focused integrated proof."

policy:
  allow_implicit_invocation: false
