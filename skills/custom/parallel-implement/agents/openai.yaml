interface:
  display_name: "Parallel Implement"
  short_description: "Drain one ticket graph to verified release"
  default_prompt: "Use $parallel-implement from the root to trace this parent graph, open isolated ready lanes, land serially, obtain formal review, and close only the verified reviewed HEAD."

policy:
  allow_implicit_invocation: false
