interface:
  display_name: "Parallel Implement"
  short_description: "Isolated wavefront, root-owned review"
  default_prompt: "Use $parallel-implement to run these ready, non-overlapping work items as an isolated wavefront with fresh-context one-commit workers, serial integration, root-owned review, tracker lock, and release sweep."

policy:
  allow_implicit_invocation: false
