interface:
  display_name: "Audit Codebase"
  short_description: "Map and audit codebase architecture"
  default_prompt: "Use $audit-codebase to Map this repository, Audit one subsystem I select, or Analyze one candidate I select. Update the organized HTML report and stop before implementation."

policy:
  allow_implicit_invocation: false
