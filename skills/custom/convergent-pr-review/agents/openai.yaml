policy:
  allow_implicit_invocation: true

interface:
  display_name: "Convergent PR Review"
  short_description: "Terminal high-risk fixed-snapshot review"
  default_prompt: "Use $convergent-pr-review to pin one immutable local PR or high-risk diff, admit only Charter-relevant findings through independent fresh-context review, and return one verified read-only decision ledger without remediation."
