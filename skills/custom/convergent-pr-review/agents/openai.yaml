policy:
  allow_implicit_invocation: true

interface:
  display_name: "Convergent PR Review"
  short_description: "Fresh-context reviewers, verified ledger"
  default_prompt: "Use $convergent-pr-review on this local PR or high-risk diff: pin one snapshot, dispatch 2 fresh-context reviewers, run targeted convergence, and return a root-verified ledger."
