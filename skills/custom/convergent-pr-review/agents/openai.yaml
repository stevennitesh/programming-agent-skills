policy:
  allow_implicit_invocation: true

interface:
  display_name: "Convergent PR Review"
  short_description: "Independent reviewers, verified ledger"
  default_prompt: "Use $convergent-pr-review to review this PR locally from its resolved merge base with 2 independent reviewers for up to 2 rounds."
