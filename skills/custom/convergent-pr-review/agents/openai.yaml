policy:
  allow_implicit_invocation: true

interface:
  display_name: "Convergent PR Review"
  short_description: "Independent reviewers, verified ledger"
  default_prompt: "Use $convergent-pr-review to pin this local PR or high-risk diff, run independent fresh-context review, and return a verified decision ledger."
