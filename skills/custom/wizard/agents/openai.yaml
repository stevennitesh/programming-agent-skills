interface:
  display_name: "Wizard"
  short_description: "Build a guided human-only procedure"
  default_prompt: "Use $wizard to create one repository-native interactive script for this bounded human-only procedure, verify it statically, and stop before running it."
policy:
  allow_implicit_invocation: false
