policy:
  allow_implicit_invocation: false

interface:
  display_name: "Implement"
  short_description: "Deliver one ready item exactly"
  default_prompt: "Use $implement to deliver this one caller-selected ready item exactly."
