policy:
  allow_implicit_invocation: false

interface:
  display_name: "Implement"
  short_description: "Deliver one ready item leanly"
  default_prompt: "Use $implement to understand this one selected ready item, choose the smallest sound design, implement it in the current owner, run proportionate proof, inspect the final diff, and stop. Use heavier workflows only when their stated condition applies."
