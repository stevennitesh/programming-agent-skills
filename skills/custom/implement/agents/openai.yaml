policy:
  allow_implicit_invocation: false

interface:
  display_name: "Implement"
  short_description: "Deliver one ready item leanly"
  default_prompt: "Use $implement to deliver this one caller-selected ready item through the smallest integrated change, focused proof, one Luna implementer when available, and one fresh Change Review."
