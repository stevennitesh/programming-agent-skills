interface:
  display_name: "Triage"
  short_description: "Classify raw tracker intake"
  default_prompt: "Use $triage to classify this incoming tracker item and prepare or apply only the authorized next-state effects."
policy:
  allow_implicit_invocation: false
