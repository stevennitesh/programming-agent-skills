interface:
  display_name: "Cost-aware coding"
  short_description: "Route GPT-6 work to reduce Astra token churn"
  default_prompt: "Use $cost-aware-coding for this coding task. Keep consequential reasoning and final review with GPT-6 Astra, route substantial implementation to GPT-6 Sol, and use GPT-6 Luna only for compact bounded tasks when briefing and verification remain cheaper than a stronger tier. Optimize for reduced Astra lead-token churn without weakening acceptance."
policy:
  allow_implicit_invocation: false
