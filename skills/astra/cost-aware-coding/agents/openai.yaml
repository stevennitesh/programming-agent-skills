interface:
  display_name: "Cost-aware coding"
  short_description: "Plan model allocation, then execute the accepted coordination plan"
  default_prompt: "Use $cost-aware-coding to propose a coordination plan for acceptance, or execute the existing accepted plan for this coding task."
policy:
  allow_implicit_invocation: false
