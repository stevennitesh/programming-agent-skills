interface:
  display_name: "MLE Workflow"
  short_description: "Build production-ready machine learning systems"
  default_prompt: "Use $mle-workflow to turn this model work into a reproducible, deployable ML system."

policy:
  allow_implicit_invocation: false
