---
name: grill-with-docs
description: Sharpen a plan or design by running grilling for the interview loop and domain-modeling for durable domain memory.
---

# Grill With Docs

Run `$grilling` and `$domain-modeling` together.

Use `$grilling` for the interview loop.

Use `$domain-modeling` for durable domain memory.

Follow `$domain-modeling` exactly for what may be written to `CONTEXT.md` or ADRs.
Do not add any additional persistence rules in this wrapper.

Stop when shared understanding is reached and the next artifact, skill, or implementation step is clear.
