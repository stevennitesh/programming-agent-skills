# EP-BORDER-03

Disposition: `needs-more-evidence`

Operation: Audit. Coverage is limited to the assigned runtime and supplied case facts.

The registered quality-lift deficit appeared in six fresh control runs (B03-F1), so this is not a `reject-no-control-deficit` case. From the supplied candidate-minus-control observations `[2, 3, 2, 3, 2, 3]` (B03-F2), the computed minimum is 2, the maximum is 3, and the spread is 1. The fixed rule permits acceptance only when every improvement is greater than 2 and explicitly requires more evidence for boundary-touching results (B03-F3). Because three observations equal 2, the record does not support `accept`.

No critical failure or protocol deviation was observed (B03-F4), so the supplied record does not establish `reject-regression` or a protocol block. That clean result does not override the unmet acceptance threshold. A second realistic fixture family is available under the same fixed execution configuration (B03-F5), providing a named next evidence source for resolving the borderline result.

Evidence limits: the judgment applies only to this supplied task, runtime, facts, and fixed execution configuration. No prevalence, transfer, or broader efficacy claim is supported.
