# Disposition: `needs-more-evidence`

The record does not support acceptance. The five observed candidate-minus-control improvements are `[3, 1, 4, 0, 3]` (B01-F2): minimum `0`, maximum `4`, and spread `4`. Two observations (`1` and `0`) fail the fixed acceptance boundary requiring every improvement to be greater than `2` (B01-F3). Thus the results cross the decision boundary (B01-F4), and their material variability is an uncertainty signal that supports extending rather than stopping.

No critical failure appeared (B01-F4), so the record does not support `reject-regression`. The registered control deficit did appear in all five fresh runs (B01-F1), so it also does not support `reject-no-control-deficit`. Because the fixed rule says mixed results require more evidence (B01-F3) and one additional matched wave is available under the same configuration (B01-F5), the supported next operation is to run that matched wave.

This judgment is limited to the supplied quality-lift case and its fixed execution configuration. The record supplies no wrong-condition evidence, real-world prevalence evidence, or evidence supporting transfer to other tasks, models, hosts, or configurations; none is inferred.
