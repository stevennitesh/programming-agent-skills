`partial` — Audit, advisory only.

Diagnosis: the current pointer is structurally incomplete and behaviorally unreliable. “Consult security guidance when relevant” names neither `SECURITY.md`, the secret-handling trigger, nor the required load-and-apply action. Three secret-handling runs skipped the owner and mandatory steps.

Ordered repair:

1. Replace only the pointer with:
   > When a task handles secrets, load `SECURITY.md` and apply its mandatory secret-handling steps.

   Evidence gate: exact read-back must confirm the pointer names the owner, trigger, and action. No `SECURITY.md` body moves inline.

2. Freshly test the revised pointer before claiming reliability. Freeze task, context, model, host, reasoning, tools, authority, evidence, runtime, and rubric; compare uncontaminated controls whose only difference is the pointer wording.

   Evidence gate: inspect every secret-handling output for both loading `SECURITY.md` and applying all mandatory steps. Include non-secret controls to confirm the branch is not loaded outside its trigger.

3. If any freshly exercised secret-handling run still skips mandatory material, ask the `SECURITY.md` owner to authorize and identify the smallest must-have instruction to add inline, then repeat the same validation.

`SECURITY.md` remains authoritative. Its branch-only body remains outside common runtime; only the explicit routing pointer belongs inline unless the revised pointer is freshly shown unreliable. The caller owns any canonical edit, and the reference owner controls any fallback inline content.

No mutation or behavioral proof was performed. Residual risk is the untested revised wording; the safe boundary is to report `partial`, not claim the repair reliable, until fresh direct-control validation passes.
