`partial` — **Audit**

**Diagnosis:** The current pointer is unreliable: it names neither `SECURITY.md`, the secret-handling trigger, nor the required load-and-apply action. Three secret-handling runs skipped the owner and mandatory steps.

**Smallest repair:** Replace the pointer with:

> When a task handles secrets, load `SECURITY.md` and apply its mandatory secret-handling steps.

**Ordered gates:**

1. Confirm `SECURITY.md` remains authoritative and the caller owns the canonical edit.
2. Make only the pointer replacement above; keep the branch-only procedure out of common runtime.
3. Validate behavior with uncontaminated direct controls: freeze task, context, model, host, tools, evidence, runtime, and rubric; compare old and revised wording with that wording as the only arm difference.
4. Exercise secret-handling cases and verify workers load `SECURITY.md` and apply its mandatory steps. Include non-secret cases to confirm the branch is not loaded unnecessarily.
5. Read back the canonical wording and run deterministic structural and relationship checks separately from behavioral evaluation.

The revised pointer has not yet been behaviorally tested, so no completed proof can be claimed. If fresh secret-handling trials still omit mandatory material, escalate only by moving the smallest necessary must-have instruction inline, with owner approval; retain all remaining branch-only content in `SECURITY.md`. No mutation or foreign-state change was performed.
