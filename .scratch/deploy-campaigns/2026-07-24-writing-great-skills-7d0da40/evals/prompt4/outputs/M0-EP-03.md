`partial` — Audit, read-only.

Diagnosis: the current pointer is underspecified. It neither names `SECURITY.md`, precisely identifies secret-handling tasks, nor directs workers to load and apply its mandatory steps. Three recent secret-handling runs skipped the reference, but no evidence yet shows that revised wording fixes the behavior.

Ordered repair:

1. The caller should replace the pointer with:

   > When a task handles secrets, load `SECURITY.md` and apply its mandatory secret-handling steps.

   Evidence gate: exact read-back confirms the target, trigger, and action are present; `SECURITY.md` remains authoritative; none of its branch-only body is copied inline.

2. Freshly test the revised pointer against the current pointer using uncontaminated direct controls with task, model, tools, authority, evidence, and rubric fixed.

   Evidence gate: on secret-handling runs, workers actually load `SECURITY.md` and apply every mandatory step. Include non-secret controls to confirm the reference is not loaded outside its trigger. Reference mentions or template echoes alone are not behavioral proof.

3. Only if those fresh tests show the revised pointer still skips mandatory material, ask the reference owner to identify the smallest must-have instruction and let the caller move only that instruction inline.

   Evidence gate: documented failure of the revised pointer, owner authorization, one unambiguous owner for the moved instruction, and fresh behavioral validation after the change.

`SECURITY.md` and its remaining procedure stay branch-only. Without fresh failure evidence for the revised pointer, inlining is unauthorized and unnecessarily duplicates foreign guidance. If behavioral validation cannot run, stop at `partial`: retain the explicit pointer candidate, report reliability as unproved, and do not escalate to inlining.
