`partial` — advisory audit only.

Diagnosis: the current pointer omits the branch target, trigger, and required action. That ambiguity aligns with three secret-handling runs skipping `SECURITY.md`. No evidence yet shows that revised wording repairs the behavior.

Smallest candidate repair:

> When a task handles secrets, load `SECURITY.md` and apply its mandatory secret-handling steps.

Ordered actions and gates:

1. Caller replaces only the current pointer with the candidate wording.
   - Gate: exact read-back confirms the target, secret-handling condition, and load-and-apply action.
2. Leave `SECURITY.md` authoritative and its body branch-only.
   - Gate: confirm no security procedure was copied into common runtime.
3. Run fresh, uncontaminated secret-handling controls whose only arm difference is the pointer wording.
   - Gate: workers load `SECURITY.md` and apply every mandatory step; structural wording checks alone are insufficient.
4. If the revised pointer still fails, ask the reference owner to identify and permit only the smallest must-have instruction for inline placement, then repeat the controls.

No files were changed and no behavioral repair is claimed proven. The `SECURITY.md` body, canonical edit, installation, and delivery remain outside this advisory boundary.
