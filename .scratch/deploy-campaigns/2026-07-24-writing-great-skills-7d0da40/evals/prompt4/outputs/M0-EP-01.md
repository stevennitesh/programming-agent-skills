`partial` — Audit, advisory only.

Diagnosis: the current pointer is too implicit. It names neither `SECURITY.md`, the secret-handling trigger, nor the required load-and-apply action (F-EP-02). Three secret-handling runs then skipped the authoritative guidance and mandatory steps (F-EP-03).

Smallest repair candidate:

> When a task handles secrets, load `SECURITY.md` and apply its mandatory secret-handling steps before proceeding.

Ordered actions and gates:

1. The caller replaces only the current pointer with that sentence.
   - Gate: exact read-back confirms the condition, owner document, and action are all explicit.
2. Keep `SECURITY.md` unchanged and branch-only.
   - Gate: no security procedure is copied inline; ownership remains with the reference owner (F-EP-01, F-EP-04).
3. Run fresh uncontaminated direct controls with all conditions frozen except old versus revised wording.
   - Secret-handling gate: revised-arm workers actually load `SECURITY.md` and apply every mandatory step; mentions or template echoes are insufficient.
   - Non-secret gate: workers do not load the branch unnecessarily.
   - Predeclare sample size and acceptance criteria; report only the tested bounds.
4. If any revised-pointer run still skips mandatory material, ask the `SECURITY.md` owner to identify the smallest must-have instruction and move only that instruction inline, then repeat the controls (F-EP-06).

The full security procedure—and every instruction not freshly shown necessary inline—remains branch-only. The reference owner controls its content; the caller controls the canonical pointer edit. No mutation or behavioral proof has been completed here, and reliability remains unproven until the fresh controls pass.
