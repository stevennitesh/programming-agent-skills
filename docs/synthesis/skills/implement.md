# Implement Minimum-Runtime Synthesis

## Active Promoted And Installed Decision — 2026-07-24

Decision: `complete`; exact P1 is canonical is installed is V1 is M0.

Prompt 4 acceptance remains exact: the accepted transcript is
`sha256:9ca3c016847764b32ec99b17ea6cf952bac643947a628c0ab5d33f5de86b5343`,
V1/M0 is
`1f61b9880b881771b579372075dd89e3bccb7ce9c94ae3bd0e30f1f43ae8724f`,
the rejected original H1 is
`3960bc073a49022faaf581c4829653e16d470e3c9a727066d8a755a1086b5e8d`,
and the admitted Prompt 4 workspace is
`80f6ae5dfcb2367545b77a1b5c7207890c947636f283b9e0f38c1620cc26dfeb`.

The corrected frozen M0 checkpoint is intact at
`sha256:c56d01368a49d19ed33fb4d0c4b926029d6264885e36bea5af820f40c31d5f0c`.
The matching R2 research packet is `research-complete` at whole-file SHA-256
`4d08005025411969cbadb583077ff3fabe28a8a6f6fc7887c99bb9dc62d438cf`.
Both retain the intended singleton-delivery contract and the same starting
Git `HEAD`.

The correction closes the historical blocker: Local Markdown closeout now
occurs after acceptable review and before Lock, is staged with selected work,
and is Mutation-read back; GitHub and GitLab closeout remain external
post-commit mutations. Complete current, owner, caller, relationship, test,
evaluation, candidate, pruning, promotion, and research inspection exposed no
other missed minimum authority, compatibility, safety, or relationship
obligation.

The pre-evaluation shape was a `hypothesis-candidate`; Prompt 4 rejected both
quality lifts because exact M0 exhibited no registered deficit:

```text
current = b918d2762505a69d1ba577533a2b9bd040133188b0e0c6dee4cb7e78351b4f7c
M0 runtime = 1f61b9880b881771b579372075dd89e3bccb7ce9c94ae3bd0e30f1f43ae8724f
original H1 candidate = 3960bc073a49022faaf581c4829653e16d470e3c9a727066d8a755a1086b5e8d
rederived H1 = V1 = P1 = M0
```

The promoted P1 is the active canonical and managed installed runtime at
`1f61b9880b881771b579372075dd89e3bccb7ce9c94ae3bd0e30f1f43ae8724f`.
The canonical package was promoted byte-for-byte from exact P1; the managed
installer synchronized only `implement`, canonical/installed parity passed,
and the post-install dry-run was clean.

Historical-current/M0 byte equality is not claimed. M0 removes the prior
canonical-only
staged-worker outcome and numeric two-generation default, adds the
checkpoint-required explicit Simplify unit, and expresses the corrected
tracker-kind order. The two evaluated H1 transformations survive only as
rejected hypotheses and do not enter V1.

### Intended Contract And Standalone M0

M0 implements all 17 frozen units without research or current-only behavior.
It admits exactly one selected bounded Ready-for-agent item; applies repository
setup and domain routing; reconciles packet, dependencies, fixed point,
authority, Git/tracker state, and unrelated work; claims tracker work; freezes
one Charter and finite Repair budget; chooses technique inside settled
commitments; routes only matching inner proof work to TDD or Diagnosis;
implements a narrow path and assigned state branches; simplifies and
reconciles authored work; obtains and, when authorized, repairs one immutable
risk-scaled review lineage; applies tracker-kind-correct closeout and Lock;
commits the exact locked tree; pushes only under separate authority; and
returns one truthful terminal or nonterminal packet.

The neutral M0 Repair rule is executable without the current numeric default:
record a finite caller-, repository-, or risk-bounded budget before production
mutation; when no bound can be selected inside settled authority, return the
decision instead of repairing. The neutral M0 has no staged-worker branch.
Parallel Implement and its worker contract already own delegated parent-graph
lanes; no inspected caller invokes Implement for staged handoff.

### Corrected M0 Clause Map

| M0 Clause | Units | Cheapest Neutral Runtime Meaning | Current Disposition |
| --- | --- | --- | --- |
| C01 | U01 | One selected bounded ready item; exclude foreign work classes. | `M0-equivalent`; staged-worker description text is separate and removed. |
| C02 | U02 | Follow relevant setup, tracker, label, domain, and engineering owners; stop on incompatibility. | `M0-equivalent`, `required compatibility`. |
| C03 | U03 | Reconcile packet, dependencies, fixed point, authority, Git/tracker state, and unrelated work before mutation. | `M0-equivalent`. |
| C04 | U04 | Claim configured tracker work with read-back; fabricate no direct-item tracker state. | `M0-equivalent`, `required compatibility`. |
| C05 | U05 | Freeze Charter, bounded slice, proof lanes, state branches, fence, and finite Repair budget. | `M0-equivalent`; numeric default is `replace`. |
| C06 | U06 | Inspect alternatives inside commitments and return commitment changes. | `M0-equivalent`. |
| C07 | U07 | Invoke Diagnosis or TDD only under its owned trigger and integrate a complete Return. | `M0-equivalent`, relationship-protected. |
| C08 | U08 | Prove a narrow path, then implement assigned acceptance and state branches within scope. | `M0-equivalent`. |
| C09 | U09 | Simplify authored work and reconcile candidate, proof, and disposable state. | Current explicit passage absent; `replace` with required M0 expression. |
| C10 | U10 | Pin a proved candidate and obtain the applicable formal review with required Spec. | `M0-equivalent`, relationship-protected. |
| C11 | U11 | Repair only a complete admitted Charter-preserving batch within the finite budget; rereview successors. | `M0-equivalent`; numeric default is `replace`. |
| C12 | U12 | For Local Markdown, close, release, stage with selected work, and Mutation-read back after review and before Lock. | `M0-equivalent`, `required compatibility`. |
| C13 | U13 | Lock reviewed work plus applicable Local Markdown closeout through current proof, identity, scope, staged state, and risk. | `M0-equivalent`, `required compatibility`. |
| C14 | U14 | Commit exactly the locked tree, atomically including applicable Local Markdown state and excluding unrelated state. | `M0-equivalent`, `required compatibility`. |
| C15 | U15 | After commit, reconcile GitHub/GitLab closeout and release; mutate no local/direct tracker post-commit. | `M0-equivalent`, `required compatibility`; H1-02 may refine failure judgment. |
| C16 | U16 | Push only under separate authority and verify the approved remote identity. | `M0-equivalent`. |
| C17 | U17 | Complete only after every applicable gate; otherwise return safe `partial` or `blocked` state. | `M0-equivalent`; staged-handoff result is removed. |

M0 is implementable from this map and the complete frozen checkpoint alone.
Research terms, current wording, historical B0/C1 names, H1 weaknesses, and
evaluator conclusions are unnecessary to construct it.

### Complete Current-Clause Dispositions

IDs retain the exact 40-passage inventory from the historical Pruning Pass.
Each current passage has one safe disposition; mixed passages are split at
their smallest semantic unit.

| Clause | Semantic Unit | Classification And Decision |
| --- | --- | --- |
| M01 | Singleton trigger; staged-worker trigger | Singleton `M0-equivalent`; staged-worker trigger `remove`. |
| M02 | Explicit-only policy | `M0-equivalent`; preserve. |
| M03 | Provider display identity | `required compatibility`; preserve. |
| M04 | Singleton and bounded-Repair provider summary | `M0-equivalent`; adapt to neutral finite Repair. |
| M05 | Charter, review, Repair, Lock, closeout prompt | `M0-equivalent`; `replace` generic closeout with corrected tracker-kind order. |
| S01 | Selected ready singleton outcome | `M0-equivalent`; preserve meaning. |
| S02 | Owner sequence | `M0-equivalent`; adapt to the complete C01-C17 order. |
| S03 | Staged-worker sequence | `remove`; current-only alternate outcome with no caller dependency. |
| S04 | Owner authority; staged-worker limits | Owner `M0-equivalent`; staged-worker unit `remove`. |
| S05 | Owner mutation authority and external-action exclusions | `M0-equivalent`; preserve meaning. |
| S06 | Setup and tracker-guidance gate | `M0-equivalent`, `required compatibility`; preserve pointer ownership. |
| S07 | Select one ready item | `M0-equivalent`; preserve meaning. |
| S08 | Binding named target | `M0-equivalent`; preserve safety boundary. |
| S09 | Parent remains context; shaping Return | `M0-equivalent`; preserve exclusion and owner. |
| S10 | Repository-policy selection or caller question | `M0-equivalent`; preserve authority. |
| S11 | Read-only selection state | `M0-equivalent`; preserve no-self-readiness guard. |
| S12 | Ready definition | `M0-equivalent`, `required compatibility`; adapt to the full configured contract. |
| S13 | Claim, fixed point, unrelated-state preservation, isolation | `M0-equivalent`, `required compatibility`; preserve meaning. |
| S14 | Immutable Charter; default two generations | Charter and finite budget `M0-equivalent`; exact `two` `replace` with neutral finite authority-bound budget. |
| S15 | Bounded slice, proof story, commitment-change Return | `M0-equivalent`; H1-01 may substitute coherence steering. |
| S16 | Conditional TDD/Diagnosis routing | `M0-equivalent`, relationship-protected; preserve trigger and Return. |
| S17 | RED-unsuitable proof and adjacent-work Return | `M0-equivalent`; preserve claim-matched fallback. |
| S18 | Staged-worker patch, proof, staging, Return | `remove`; no M0 or relationship owner. |
| S19 | Acceptance, selected staging, immutable review tree | `M0-equivalent`; adapt after removing staged-worker assumptions. |
| S20 | Exactly one risk-scaled review route and packet | `M0-equivalent`, relationship-protected. |
| S21 | Complete current-review acceptance gate | `M0-equivalent`; preserve. |
| S22 | Complete-report finding admission; no partial Repair | `M0-equivalent`, owned by Review's Finding Contract. |
| S23 | Batched Repair and successor review within Budget | `M0-equivalent`; exact numeric default `replace`. |
| S24 | Local Markdown closeout before Lock and staging | `M0-equivalent`, `required compatibility`; this is now frozen M0. |
| S25 | Review-to-lock delta, index equality, exact commit | `M0-equivalent`, `required compatibility`. |
| S26 | Connector closeout after commit and read-back | M0 core `M0-equivalent`, `required compatibility`; recovery expression is `H1 hypothesis` H1-02. |
| S27 | Exactly one typed Return | `M0-equivalent`; preserve. |
| S28 | Setup-precondition Return | `M0-equivalent`; preserve. |
| S29 | Selection-gate Return | `M0-equivalent`; preserve. |
| S30 | Assignment-blocker Return | `remove` with staged-worker branch. |
| S31 | Staged-handoff Return | `remove` with staged-worker branch. |
| S32 | Decision-required Return | `M0-equivalent`; preserve. |
| S33 | Blocked Return | `M0-equivalent`; adapt to complete M0 failure fields. |
| S34 | Complete Return | `M0-equivalent`; adapt to tracker-kind proof. |
| S35 | Completion and downstream stop | M0 completion `M0-equivalent`; staged-handoff exclusion `remove`. |

Safe-removal decision: staged-worker text has no current relationship edge,
caller contract, candidate manifest entry, or required compatibility owner.
Parallel Implement workers use their own bounded worker brief and Return to
the root. Removing the branch narrows Implement to its frozen terminal
singleton outcome and cannot reach promotion until Prompt 4 proves the
invocation/exclusion and protected relationship cases. The numeric default
has no independent authority or professional support and is replaced, not
silently dropped. No unresolved removal risk may reach promotion.

### Research Registry And H1 Decisions

| Candidate | Origin | Method Evidence | Current State | Decision And Limit |
| --- | --- | --- | --- | --- |
| Self-contained small batch | `professional-method`, intent-adjacent | `independently-supported` | Current says bounded slice but does not distinguish coherence from file count. | Admit H1-01 as a `quality-lift`; bounded to one independently understandable, acceptance-complete change. |
| Evidence before claims | `professional-method`, intent-adjacent | `independently-supported` rationale; exact phrase unproved | M0 already requires fresh claim-matched proof and evidence-bearing Returns. | `M0-equivalent`; reject a duplicate H1 term. |
| Latest accepted tree | `professional-method` | `independently-supported` Git identity mechanics plus local authority | M0 C10-C14 already bind review, local cohort, Lock, and commit identities. | `M0-equivalent`; reject a duplicate leading-term substitution. |
| Commit-anchored reconciliation | `professional-method`, intent-adjacent | `independently-supported` under the settled no-rollback condition | Current blocks partial read-back but does not require preserve/refetch/classify/safe-resume judgment. | Admit H1-02 as a post-commit connector `quality-lift`; no universal retry or compensation claim. |
| Green simplification | `professional-method` | `independently-supported` | Current explicit passage is absent; corrected M0 U09/C09 already requires simplify under proof. | `M0-equivalent`; no H1. |
| Atomic tracker closeout | `intent-adjacent` term | `contested` outside one Git tree | Absent; corrected M0 separates repository and connector systems. | Reject; use no cross-system atomicity claim. |
| Provider retry by HTTP verb | `professional-method` inference | `unverified` | Absent. | Reject; provider owner and state read-back govern replay safety. |
| Universal test-first | `pack-observed` | `contested` | Current conditional routing is compatible. | Reject; preserve Diagnosis, TDD, and direct-evidence conditions. |
| Per-task review and finishing menu | `pack-observed` | `pack-specific` | Absent. | Reject; wrong singleton mutation and completion boundary. |
| Ponytail ordered-sufficiency ladder | `pack-observed` | `pack-specific` | Absent. | Defer; no distinct M0 weakness justifies the full ladder. |
| Fixed two-generation Repair default | `current-observed` | `unverified` | Present. | Replace with M0 finite authority-bound budget; no H1. |

Broader research found no decisive contradiction to H1-01 or H1-02. Neither
inherits universal professional authority, exact-wording efficacy, provider
transport ownership, or transfer beyond the registered conditions.

### H1 Transformation Units

**H1-01 — Self-contained coherence**

- Origin: `professional-method` and `intent-adjacent`; method evidence
  `independently-supported`.
- Intended value and owner: Implement holds one independently understandable,
  acceptance-complete, testable change; caller remains scope/acceptance owner.
- Expected M0 weakness: “bounded” may be optimized as few files or a
  horizontal fragment, losing necessary coupled surfaces or admitting adjacent
  concerns.
- Contribution mode: `quality-lift`; M0 remains viable.
- Cheapest expression: substitute one bounded-slice construction sentence
  with a self-contained coherence gate; add no file, table, or procedure.
- Positive cases: tempting horizontal fragments, broad adjacent cleanup, and
  acceptance requiring several coupled surfaces.
- Wrong condition: do not split an indivisible multi-surface behavior merely
  to reduce files or lines; do not reopen caller-owned scope.
- Fixed rubric: score acceptance completeness, coherent proof story, inclusion
  of required coupled surfaces, exclusion/Return of adjacent work, and
  preserved authority. Missing accepted behavior or fabricating a new slice is
  critical.
- Proof: five fresh M0 controls first across at least two families
  (horizontal-fragment and adjacent-work); run five H1 samples only if M0 shows
  the registered quality deficit.
- Residual limit: evidence applies only to registered singleton decomposition
  cases and does not establish a universal size metric.

**H1-02 — Commit-anchored connector reconciliation**

- Origin: `professional-method` and `intent-adjacent`; method evidence
  `independently-supported` for inspect-before-replay and resumable progress
  under the settled no-rollback condition.
- Intended value and owner: Implement preserves the verified commit as the
  recovery anchor, refetches connector state, classifies intended effects, and
  returns only the configured safe continuation; tracker owners retain
  transport and field semantics.
- Expected M0 weakness: generic partial-publication recovery may invite blind
  replay, false rollback, loss of commit identity, or an incomplete account of
  ambiguous effects.
- Contribution mode: `quality-lift`; M0 remains viable and truthful.
- Cheapest expression: refine the post-commit connector failure sentence to
  preserve the commit, refetch, classify `applied`, `missing`, `conflicted`, or
  `unknown`, and resume only safe missing steps.
- Positive cases: lost response before or after apply, partial
  label/comment/claim effects, concurrent provider change, and provider error.
- Wrong condition: do not apply the method before a verified commit, to Local
  Markdown or direct work, after confirmed complete closeout, or when the
  configured owner cannot establish a safe replay.
- Fixed rubric: score commit preservation, actual-state refetch, complete
  effect classification, no blind replay/automatic rollback, truthful
  completion status, and one owner-matched continuation. False completion,
  overwriting concurrent state, or reversing the accepted commit is critical.
- Proof: five fresh M0 controls first across lost-response and
  partial/concurrent-state families; run five H1 samples only if M0 shows the
  registered quality deficit.
- Residual limit: no claim that PATCH/PUT or a composite GitHub/GitLab
  operation is universally idempotent; no live-provider or cross-model
  transfer claim.

Neither unit changes outcome, invocation, exclusion, authority, Return,
completion, or relationships. Both are agent-owned runtime expressions and
need no Prototype or Behavior Decision Interlude.

### Decision Ledger

| Unit | M0 Obligation Or H1 Origin | Method Evidence | Current State | Owner | Contribution Mode And Expected M0 Weakness | Cheapest Expression | Wrong Condition | Proof | Decision |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| M0-U01 | Selected ready singleton | Local intent; upstream nucleus is observation | Present plus staged trigger | Caller selects; Implement admits | M0; none | One admission/exclusion gate | Raw, blocked, parent, review-only, conflict-only | Invocation/exclusion suite | Preserve M0; remove staged trigger |
| M0-U02 | Setup/domain compatibility | Local authority | Present | Repo Bootstrap/docs own setup; Implement checks | M0; none | Triggered pointers and safe stop | Missing/incompatible setup | Pointer trace and setup cases | Preserve |
| M0-U03 | Packet/state/authority reconciliation | Local authority | Present | Implement | M0; none | One pre-mutation reconciliation gate | Blocker, ambiguity, unsafe overlap | Packet/Git/frontier cases | Preserve |
| M0-U04 | Claim or direct N/A | Local tracker authority | Present | Tracker transport; Implement applies | M0; none | Claim plus read-back | Competing/partial claim | Before/after state | Preserve |
| M0-U05 | Charter and finite budget | Local intent | Present with numeric default | Caller owns commitments; Implement records | M0; numeric default unsupported | Neutral finite authority-bound budget | Missing commitment/bound | Clause trace and decision case | Replace numeric default |
| M0-U06 | Technique inside commitments | Local intent | Present | Implement; caller owns commitment change | M0; none | Alternatives then bounded choice | Choice changes commitment | Source/authority trace | Preserve |
| M0-U07 | Diagnosis/TDD routing | Corroborated, condition-bound | Present | Inner owners; Implement integrates | M0; none | Trigger-bearing pointers | Wrong owner or incomplete Return | Relationship cases | Preserve |
| M0-U08 | Narrow path then assigned branches | Local intent; bounded-change support | Present | Implement | M0; none | Narrow proof then expansion | Contradiction or scope escape | Acceptance/state suite | Preserve |
| M0-U09 | Simplify and reconcile | Independently supported but required by local contract | Explicit passage absent | Implement | M0; none | One green simplify/reconcile gate | Cleanup changes commitment/unrelated work | Proof/diff/disposable inventory | Add neutral M0 |
| M0-U10 | Immutable formal review | Direct/corroborated plus local authority | Present | Implement routes; reviewer judges | M0; none | One risk-scaled route and packet | Drift/incomplete review | Ordinary/high-risk cases | Preserve |
| M0-U11 | Admitted Repair and rereview | Local Finding Contract | Present with numeric default | Implement admits; caller owns decisions | M0; numeric default unsupported | Complete batch within finite budget | Partial/mixed/over-budget repair | Finding and successor cases | Replace numeric default |
| M0-U12 | Local closeout pre-Lock | Local tracker authority | Present | Local tracker owns fields/order | M0; none | Close, stage, read back | Wrong kind, partial, unstaged | Tracker/index/frontier cases | Preserve required compatibility |
| M0-U13 | Exact Lock | Local engineering authority | Present | Implement | M0; none | Identity/scope/proof/risk gate | Drift or extra state | Review-to-lock identity | Preserve |
| M0-U14 | Exact commit | Git mechanics plus local authority | Present | Implement/caller | M0; none | Commit locked tree once | Unrelated index/hook drift/mismatch | Tree and status checks | Preserve |
| M0-U15 | Connector closeout | Local tracker authority | Present, recovery compact | Tracker owns transport; Implement reconciles | M0; none | Post-commit mutation/read-back | Local/direct or unsafe retry | Provider-state cases | Preserve M0; H1-02 transforms recovery |
| M0-U16 | Conditional push | Local authority | Present | User/caller | M0; none | Separate-authority gate | No authority or wrong remote | Remote identity/boundary | Preserve |
| M0-U17 | Truthful Return/completion | Local intent | Present plus staged Returns | Implement | M0; none | Terminal/nonterminal packet | Unchecked gate or false completion | Packet-to-gate suite | Preserve M0; remove staged Returns |
| H1-01 | Professional method + intent-adjacent coherence | `independently-supported` | Adjacent bounded wording | Implement inside caller scope | `quality-lift`: M0 may optimize for file count/horizontal fragments | One sentence substitution | Indivisible coupled behavior or new scope | Two-family M0-first comparison | Admit |
| H1-02 | Professional method + intent-adjacent recovery | `independently-supported`, bounded | Compact partial-failure block | Implement judgment; tracker transport | `quality-lift`: M0 may replay/rollback or lose ambiguous effects | One recovery refinement | Pre-commit, local/direct, complete, unsafe replay | Two-family M0-first comparison | Admit |

### Protected Behavior, Relationships, And Affected Surfaces

The active protected set is all M0-U01 through M0-U17. No H1 unit entered it.
Independently required compatibility, explicit-only policy and provider
identity, tracker-kind ordering, exact-tree identity, non-intuitive authority
and safety boundaries, and truthful Return and completion remain protected.
Historical presence alone protected neither staged-worker behavior nor the
numeric Repair default.

Relationship delta is `none`. Direct users, To Tickets, Triage, Diagnosis, and
Improve Codebase may supply or recommend one selected item under their existing
authority. Implement invokes TDD or Diagnosis under their triggers and exactly
one of Review or Convergent PR Review. It recommends and stops at To Tickets
for shaping and Repo Bootstrap for setup. Parallel Implement continues to own
parent graphs and delegated lanes. Foreign procedures remain behind their
existing context pointers.

Prompt 5 changed only the exact canonical Implement package, directly affected
canonical contract tests, this active synthesis, the Prompt 5 validation
record, and removal of the Implement experimental package and manifest entry.
It published no relationship-topology change and preserved every unrelated
experimental candidate.

### Proof Matrix And Prior Evidence

| Lane | Exact Required Proof | Prior Evidence Disposition |
| --- | --- | --- |
| Checkpoint and research identity | Marker fingerprint, whole-file research hash, intended-contract match | `exact-reusable` for identity and admission |
| M0 intent fidelity | Every runtime passage maps only to C01-C17/U01-U17 and local authority | Passed by the frozen Prompt 3 runtime map and Prompt 4 read-back |
| M0 viability | Complete 17-fixture frozen suite, including direct/local/connector order and injected failures | `passed`: 17/17 valid fresh M0 samples; one zero-credit source-incomplete sample was repaired without task/rubric change |
| Staged-worker removal | Explicit-only singleton positives/negatives; no staged admission/Return; parent lanes remain Parallel Implement-owned | Relationship/tests `lane-limited`; no behavioral reuse |
| Numeric-budget replacement | Finite bound recorded before repair; missing authority returns decision; no unbounded or partial Repair | Historical mixed-Repair cases `historical-admission-only` |
| H1-01 contribution | M0-first five-sample control, then H1 only on registered coherence deficit; two fixture families | `rejected-no-control-deficit`: all five controls scored 5/5; no H1 dispatch |
| H1-02 contribution | M0-first five-sample control, then H1 only on registered recovery deficit; two fixture families | `rejected-no-control-deficit`: all five controls scored 6/6; no H1 dispatch |
| Protected relationships | Trigger, verb, callee authority, Return, and no delta | Passed fresh structural read-back; no topology change published |
| Invocation/context/machine | Exact inventory, explicit-only policy, metadata, hashes, links, anchors, fences, tables | Passed for exact V1=M0 |
| Removal safety | Current clause disposition, caller search, candidate/manifest absence, negative relationship proof | Passed; staged-worker and numeric-default removal has no unresolved protected dependency |
| Pruning | Complete audit of the exact runtime-clause map, plausible-cut ledger, V1/P1 identity, and load delta | `pruning-not-needed`; P1 is byte-identical to V1; no behavioral equivalence wave was warranted |
| Promotion/install | Exact P1/canonical/installed identity, authorized cohort, parity, clean dry-run, canonical tests, full suite | Passed; only `implement` changed before synchronization, all 25 were unchanged afterward |

Prior exact current bytes and package inventory are reusable only as current
comparison evidence. The 2026-07-23 construction, D0/B0 evaluation, pruning,
promotion, and install records preserve their historical claims; they do not
prove corrected M0, H1 contribution, removal safety, current installed parity,
or this epoch's lifecycle. The older pre-prune fixture remains
`historical-admission-only`. No prior candidate behavioral proof matches
H1-01 or H1-02.

### Pruning Audit And P1

The complete runtime-facing package was audited against the exact Prompt 3
runtime map and corrected C01-C17/U01-U17 clause map. Every
instruction-bearing passage remains `keep`; no passage is `collapse`,
`disclose`, or `delete`. Only plausible cut groups are recorded:

| Cut Group | Affected Units | Expected Unchanged Behavior And Named Load | Disposition |
| --- | --- | --- | --- |
| P-CUT-01 outcome and boundary wording | U01, U05, U10, U12, U14, U17 | Preserve singleton invocation, authority, completion, and provider reach while reducing repeated outcome words | Rejected: each occurrence owns a distinct invocation, runtime, authority, completion, or provider-interface surface; collapsing it produces no package-load reduction without deleting a surface |
| P-CUT-02 reconciliation and identity wording | U04, U10, U12-U15 | Preserve claim, review, tracker-kind order, Lock, commit, and connector behavior while reducing repeated read-back words | Rejected: the clauses guard different irreversible phases and cannot become one generic gate without weakening order or identity |
| P-CUT-03 negative and nonterminal guards | U01-U06, U11, U15, U17 | Preserve all positive gates while reducing guardrail and recovery words | Rejected: the passages protect non-intuitive no-substitution, no-self-readiness, authority, safe-failure, and truthful-Return behavior |
| P-CUT-04 owner and tracker disclosure | U02, U07, U10, U15 | Preserve routing and tracker behavior while shortening the common body | Rejected: foreign procedure is already behind owner pointers; observable triggers, authority, Returns, and tracker-kind order remain common-path behavior |

No cut is material and no package support is unused. P1 therefore reuses the
exact V1 runtime files byte-for-byte:

```text
V1 = P1 = 1f61b9880b881771b579372075dd89e3bccb7ce9c94ae3bd0e30f1f43ae8724f
runtime files delta = 0
runtime bytes delta = 0
SKILL.md lines delta = 0
SKILL.md words delta = 0
```

Prompt 4's 17/17 valid M0 viability result and its invocation, context,
relationship, and machine lanes remain exact reusable proof. Because no
material cut entered P1, the Pruning Pass created no fresh behavioral wave and
makes no new wording-equivalence claim.

### Rejected, Deferred, Active Lifecycle, And Residuals

Rejected: staged-worker execution inside Implement; a fixed two-generation
default; universal TDD; provider retry inferred from HTTP verb; cross-system
“atomic closeout”; automatic compensation or commit rollback; whole
parent/batch delivery; duplicate review gates; push/deploy/PR/merge/discard
menus; copied tracker transport; redundant helper/schema/table surfaces; and
H1 terms duplicating M0 evidence, tree, or simplification obligations.

Deferred: the full Ponytail ladder, a separate Bound operation, a larger or
numerically fixed Repair budget, and any new runtime machinery lacking a named
M0 control deficit. No deferred unit enters H1.

| Artifact Or Unit | R2 State |
| --- | --- |
| Corrected Prompt 1 | `ready-for-research`; M0 fingerprint frozen |
| R2 Research Pass | `research-complete`; exact packet verified |
| R2 Prompt 2 | `ready-for-prompt-3` |
| M0 runtime | Materialized once under `skills/experimental/implement/controls/m0` at `1f61b9880b881771b579372075dd89e3bccb7ce9c94ae3bd0e30f1f43ae8724f` |
| Original H1 runtime | Evaluated candidate identity `3960bc073a49022faaf581c4829653e16d470e3c9a727066d8a755a1086b5e8d`; both transformations rejected before H1 sampling |
| V1 / rederived H1 | Exact M0 at `1f61b9880b881771b579372075dd89e3bccb7ce9c94ae3bd0e30f1f43ae8724f` |
| Prototype / Behavior Decision Interlude | Not admitted |
| Prompt 3 | Complete; original exact runtimes and proof manifest frozen |
| Prompt 4 | `accepted`; M0 viable, both H1 units `rejected-no-control-deficit`, V1=M0 |
| Pruning Pass | `complete`; `pruning-not-needed`; exact P1 is V1=M0 at `1f61b9880b881771b579372075dd89e3bccb7ce9c94ae3bd0e30f1f43ae8724f` |
| Prompt 5 | `complete`; exact P1 promoted byte-for-byte, only `implement` synchronized, parity and clean post-install dry-run verified |
| Canonical runtime | Active exact P1 at `1f61b9880b881771b579372075dd89e3bccb7ce9c94ae3bd0e30f1f43ae8724f` |
| Installed runtime | Managed mirror exact P1 at `1f61b9880b881771b579372075dd89e3bccb7ce9c94ae3bd0e30f1f43ae8724f` |
| Relationships | No topology change; existing owner pointers remain exact |
| Experimental candidate | Removed after canonical proof; all non-Implement candidates preserved |
| Git delivery | Pending in bare mode; Prompt 6 was not authorized |

Residual gaps are live dirty-index, hook, and provider mutation;
provider-composite idempotence; cross-model/host transfer; exact backend build,
seed, token, and latency telemetry; upstream remote freshness; and pending
non-decision-changing Implement facet research.

Promotion reused exact accepted behavior evidence because P1 is V1
byte-for-byte. The authoritative promotion and installation record is
`docs/validation/transcripts/2026-07-24-implement-prompt5.md`; raw outputs and
chronology remain in validation, not this active synthesis.

## Historical Blocked Deploy Prompt 2 Decision — 2026-07-24

Decision: `blocked`.

The frozen M0 checkpoint
`sha256:ff4ea3bc80cc12820d1debe8142d26832a0449d392ec843439118157902affbc`
is invalid. It missed a required tracker-compatibility and irreversible-order
obligation:

- M0 C14 and irreversible-order step 8 require every tracker-backed closeout
  only after a verified commit.
- The Local Markdown tracker owner requires closeout after acceptable review
  and before Lock, then stages the tracker file with the selected-work diff so
  the durable tracker state is part of the exact committed tree.
- The current runtime preserves that required split: repo-local closeout enters
  the lock tree before commit, while connector-backed closeout follows commit.

This is not an H1 opportunity. H1 cannot repair the viability floor. Prompt 1
must refreeze M0 with tracker-kind-specific ordering before exact M0/H1
runtime decisions, contribution pre-registration, or Prompt 3 construction.
The 2026-07-24 research packet explicitly observed the split but could not
alter frozen intent.

### Contract and identity audit

The intended outcome, explicit-only singleton invocation, caller-owned
commitments, bounded mutation authority, safe failure Return, exact-tree
completion, conditional push authority, and setup, diagnosis/TDD, review, and
tracker ownership remain supported. The missed minimum is narrow but
load-bearing: Local Markdown is durable repository state, whereas GitHub and
GitLab are connector state.

```text
current != invalid M0
H1 = not admitted
campaign shape = unclassified
```

Current package identity remains historical/current observation only:

- package tree:
  `b918d2762505a69d1ba577533a2b9bd040133188b0e0c6dee4cb7e78351b4f7c`;
- `SKILL.md`:
  `7a8b391a92609e08e1b8f70526981381ddb9336c218aed8d3c28cc4fdb618b65`;
- `agents/openai.yaml`:
  `d1331038252c7012dceebe0627884e12bb2a9975263b974bf9a991168d6dc3b8`;
- inventory: `SKILL.md`, `agents/openai.yaml`;
- explicit-only policy: preserved; and
- ambient install cohort: empty starting-state evidence only.

### Current-clause disposition ledger

IDs retain the complete 40-passage inventory from the 2026-07-23 Pruning Pass.
`M0-equivalent` below means equivalent to an unaffected frozen obligation; it
does not validate the checkpoint as a whole. Mixed rows separate the smallest
semantic behavior units rather than protecting a whole current clause by
presence.

| Clause | Current semantic behavior | Prompt 2 classification |
| --- | --- | --- |
| M01 | Singleton description; staged-worker trigger | `M0-equivalent` singleton; staged-worker behavior `defer` pending refrozen intent/current-observed admission |
| M02 | Explicit-only machine policy | `M0-equivalent` |
| M03 | Provider display identity | `required compatibility` |
| M04 | Singleton and bounded-Repair short description | `M0-equivalent` |
| M05 | Charter, review, Repair, Lock, generic closeout prompt | `M0-equivalent` except closeout order `replace` after M0 refreeze |
| S01 | Deliver one selected ready item | `M0-equivalent` |
| S02 | Owner sequence | `M0-equivalent` except generic Close `replace` with tracker-kind split |
| S03 | Staged-worker sequence | `defer` as current-observed beyond-minimum behavior |
| S04 | Owner authority; staged-worker limits | owner part `M0-equivalent`; staged-worker part `defer` |
| S05 | Owner mutation authority and external-action exclusions | `M0-equivalent` |
| S06 | Setup and tracker-guidance gate | `M0-equivalent` and `required compatibility` |
| S07 | Select one ready item | `M0-equivalent` |
| S08 | Binding named target | `M0-equivalent` |
| S09 | Parent source remains context; shaping Return | `M0-equivalent` |
| S10 | Repository-policy selection or caller question | `M0-equivalent` |
| S11 | Read-only selection state | `M0-equivalent` |
| S12 | Readiness definition | `M0-equivalent` |
| S13 | Claim, fixed point, unrelated-state preservation, isolation | `M0-equivalent` |
| S14 | Immutable Charter; default two-generation Budget | Charter `M0-equivalent`; numeric default `remove` from the minimum and `defer` as an unverified local H1 choice |
| S15 | Bounded proof story and commitment-change Return | `M0-equivalent` |
| S16 | Conditional TDD/Diagnosis routing | `M0-equivalent` and relationship-protected |
| S17 | RED-unsuitable proof and adjacent-work Return | `M0-equivalent` |
| S18 | Staged-worker patch, proof, staging, Return | `defer` as current-observed beyond-minimum behavior |
| S19 | Acceptance, selected-only staging, immutable review tree | `M0-equivalent` |
| S20 | Exactly one risk-scaled review route and packet | `M0-equivalent` and relationship-protected |
| S21 | Complete current-review acceptance gate | `M0-equivalent` |
| S22 | Complete-report finding admission; no partial Repair | `M0-equivalent` and owned by Review's Finding Contract |
| S23 | Batched Repair and successor review within Budget | `M0-equivalent`; exact numeric bound remains `defer` |
| S24 | Repo-local closeout before Lock, read-back, and tracker-file staging | `required compatibility`; missed M0 obligation that blocks this unit |
| S25 | Review-to-lock delta, index equality, one exact commit | `M0-equivalent` and `required compatibility` |
| S26 | Connector closeout after commit and complete read-back | `M0-equivalent` and `required compatibility` |
| S27 | Exactly one typed Return | `M0-equivalent` |
| S28 | Setup-precondition Return | `M0-equivalent` |
| S29 | Selection-gate Return | `M0-equivalent` |
| S30 | Assignment-blocker Return | `defer` with the staged-worker branch |
| S31 | Staged-handoff Return | `defer` with the staged-worker branch |
| S32 | Decision-required Return | `M0-equivalent` |
| S33 | Blocked Return | `M0-equivalent` |
| S34 | Complete Return | `M0-equivalent`; tracker evidence must follow the refrozen split |
| S35 | Completion gate and downstream stop | `M0-equivalent` except staged-handoff exclusion `defer` with that branch |

Safe removal dispositions are therefore explicit: no unaffected
`M0-equivalent` or `required compatibility` unit is removable; the numeric
two-generation default is excluded from M0 and deferred rather than silently
retained; staged-worker units remain deferred and cannot reach promotion
without a fresh M0/H1 decision; no other current clause has unresolved removal
risk. Because M0 is invalid, these dispositions are inspection results, not an
executable M0/H1 transformation.

### Research-candidate registry

No H1 unit is admitted in this blocked run, so contribution modes and
behavioral arms are intentionally not registered. Prompt 2 may not use H1 to
make M0 viable.

| Candidate | Origin and method evidence | Current disposition | Decision and later proof boundary |
| --- | --- | --- | --- |
| Self-contained small batch | `professional-method`; `independently-supported` | Current bounded-slice wording is adjacent but not exact | `defer` until refrozen M0; if still beyond-minimum, pre-register a `quality-lift` against horizontal-fragment and multi-feature cases |
| Evidence before claims | `intent-adjacent`; independently supported proportionate proof, exact phrase unproved | Current proof/Return units partly express it | `defer`; later quality rubric must score claim-to-fresh-evidence entailment and downgrade unsupported claims |
| Latest accepted tree | `professional-method`; `independently-supported` | Current immutable review, successor review, Lock, and tree equality express it | `M0-equivalent` behavior; exact leading-term substitution `defer` unless it has a distinct quality deficit |
| Reconcile before retry | `professional-method`; `independently-supported` | Current Mutation read-back and partial-failure Return express it | `M0-equivalent`; tracker/provider fixtures remain owner-matched proof |
| Green simplification | `professional-method`; `independently-supported` | Current runtime has no explicit simplify/reprove passage | `defer`; a later quality-lift would need unrelated-cleanup and proof-invalidating-refactor controls |
| Two repair generations | `current-observed`; `unverified` | Current default is exactly two | `remove` from M0 and `defer` as a locally justified experiment only if a caller-owned risk/budget rule admits it |
| Universal test-first | `pack-observed`; `contested` | Current runtime is conditional | `remove`/reject; preserve conditional TDD, Diagnosis, and direct-evidence routing |
| Ponytail ladder | `pack-observed`; `pack-specific` | Absent | `defer`; no full-ladder H1 without a distinct M0 failure and comparative safety/completeness proof |

### Protected set, relationships, and proof

The provisional protected set is limited to unaffected M0 obligations and
independently required compatibility, authority, safety, machine, relationship,
Return, and completion contracts. It specifically includes explicit-only
invocation, binding singleton selection, setup/readiness, claim and unrelated
state preservation, Charter authority, conditional TDD/Diagnosis ownership,
immutable risk-scaled review, complete-report Repair admission, exact-tree
Lock/commit, tracker-kind-specific closeout and Mutation read-back, conditional
push authority, and truthful typed Return. Current staged-worker behavior and
the numeric Repair default are not protected by presence.

Relationships remain observationally unchanged: direct user and ready-item
callers select Implement; Implement invokes TDD or Diagnosing Bugs and exactly
one of Review or Convergent PR Review; it recommends To Tickets for shaping and
Repo Bootstrap for setup. Tracker owners retain transport and ordering.
No relationship edit is authorized in this unit.

| Proof lane | Status and required successor proof |
| --- | --- |
| M0 checkpoint integrity | Fingerprint matches, but semantic compatibility fails; refreeze required |
| Local Markdown ordering | Direct owner trace plus current runtime and focused structural contract; establishes the blocker |
| GitHub/GitLab ordering | Direct owner trace; connector closeout remains post-commit |
| Current package identity | Exact historical hashes and inventory; no candidate/runtime mutation |
| Prior behavior evaluations | `historical-admission-only`; package/task-specific history does not prove a new M0/H1 |
| Prior structural tests | `lane-limited`; exact for existing current anchors and relationships only |
| Prior install parity | `historical-admission-only`; ambient cohort is currently empty |
| H1 contribution | `missing` by design because no H1 may be admitted over invalid M0 |
| Prompt 3 construction | blocked until a refrozen M0 resolves tracker-kind ordering |

Rejected now: universal TDD, unconditional push/deploy, fixed-two as
professional doctrine, full-suite-on-every-item, review after every subtask,
the complete Ponytail ladder, provider procedure copied into Implement, and
any H1 that masks the missing Local Markdown minimum. Deferred: staged-worker
compatibility, exact leading-word substitutions, green-simplification
steering, and a locally owned risk-scaled Repair bound.

Lifecycle state:

| Unit | State |
| --- | --- |
| 2026-07-24 Prompt 1 | invalidated by missed required Local Markdown compatibility |
| 2026-07-24 Research Pass | `research-complete`; retained as decision evidence |
| 2026-07-24 Prompt 2 | `blocked` |
| H1 | not admitted |
| Prompt 3 and later | not started |
| Runtime, candidates, relationships, proof/evals, install, Git delivery | unchanged |

Residual gaps are the refrozen tracker-kind ordering, fresh classification of
staged-worker compatibility and the Repair bound against that checkpoint,
exact M0/H1 bytes, and all successor construction and behavioral proof.

## Historical Decision — 2026-07-23

### Promoted and installed state

Status: promoted and installed for the fresh Deploy Campaign epoch started
2026-07-23. The exact shared B0/C1 candidate was integrated by canonical
no-op read-back after disposition `pruning-not-needed`.

Implement owns explicit-only delivery of exactly one selected ready bounded
work item. It produces acceptance-linked semantic proof, routes one immutable
tree through exactly one risk-scaled review owner, admits only a complete
caller-authorized Repair batch under a finite Budget, creates one exact commit,
and reconciles tracker closeout with read-back.

The current campaign identity is:

```text
current = B0 = C1
campaign shape = runtime-no-change
```

Exact package identity:

- package tree SHA-256:
  `b918d2762505a69d1ba577533a2b9bd040133188b0e0c6dee4cb7e78351b4f7c`;
- inventory: `SKILL.md` and `agents/openai.yaml`;
- `SKILL.md` SHA-256:
  `7a8b391a92609e08e1b8f70526981381ddb9336c218aed8d3c28cc4fdb618b65`;
- `agents/openai.yaml` SHA-256:
  `d1331038252c7012dceebe0627884e12bb2a9975263b974bf9a991168d6dc3b8`;
- C1 delta: empty.
- canonical and installed package identities: exact package tree SHA-256 above;
- relationship delta: none; and
- experimental lifecycle: Implement candidate and manifest entry absent.

This identity required no runtime rewrite. The accepted package is canonical
and installed. Prompt 4 accepted its viability while demoting every tested
wording-efficacy claim because the D0 controls did not exhibit their
registered failures at the required threshold.
The current-epoch construction record is
[`2026-07-23-implement-runtime-no-change-construction-evidence.md`](../../validation/transcripts/2026-07-23-implement-runtime-no-change-construction-evidence.md).
The Prompt 4 record is
[`2026-07-23-implement-runtime-no-change-behavior-eval.md`](../../validation/transcripts/2026-07-23-implement-runtime-no-change-behavior-eval.md).
The Pruning Pass record is
[`2026-07-23-implement-runtime-no-change-pruning.md`](../../validation/transcripts/2026-07-23-implement-runtime-no-change-pruning.md).
Promotion and installation evidence is appended to the construction record.

### Viability Floor And Authority

The minimum runtime must preserve:

1. explicit-only invocation for one selected ready singleton;
2. compatible setup, settled acceptance, dependencies, proof seam, and
   repository-policy selection authority before edits;
3. a binding explicit target, with no substitution, self-created readiness, or
   expansion from a parent spec, plan, queue, batch, or bare path;
4. distinct owner and explicitly assigned staged-worker authority;
5. tracker claim read-back, fixed-point capture, selected-work isolation, and
   preservation of unrelated index and worktree state;
6. one immutable Charter covering outcome, acceptance, supported paths, proof,
   commitment boundary, non-goals, fixed point, review route, and Repair
   Budget;
7. a bounded patch and fresh acceptance-linked semantic proof through the
   highest meaningful safe seam;
8. TDD for settled red-testable behavior and Diagnosis when a required bug
   fact is uncertain, without absorbing either procedure;
9. one immutable review tree and exactly one ordinary or high-risk review
   route with Spec required;
10. complete-report finding admission and finite, caller-owned Repair;
11. repository-local closeout inside the lock tree, review-to-lock delta
    inspection, index-tree equality, one commit, and `HEAD^{tree}` equality;
12. connector closeout only after commit, with complete Mutation read-back;
    and
13. typed Returns for setup, selection, assignment, staged handoff, decision,
    blocked, and complete outcomes.

The caller owns commitments, target selection, acceptance, and any decision
that changes the Charter. Implement owns technique inside the Charter,
selected-work staging, proof routing, review routing, eligible Repair, Lock,
one commit, and closeout. An explicitly assigned staged worker owns only its
bounded patch, focused proof, staging, and Return; it never owns formal review,
commit, closeout, or completion.

Setup, readiness, claim, and fixed point precede edits. Proof precedes the one
accepted immutable review. Repository-local closeout enters the selected tree
before Lock and commit; connector closeout follows commit. Partial provider
mutation is blocked, not complete.

### Source-First Checkpoint

The successor checkpoint was frozen before current runtime was used:

```text
sha256:3459091628cf6df71fc7c79dcca283745af9f06569854a8377ed6914eb469c36
```

| Source | Exact Identity | Decision Role And Limit |
| --- | --- | --- |
| Matt Pocock `implement` | `ed37663cc5fbef691ddfecd080dff42f7e7e350d` | Direct generic nucleus: implement, test, review, commit. Adapted for local singleton, authority, proof, Lock, and closeout contracts; not wholesale authority. |
| Superpowers | `d884ae04edebef577e82ff7c4e143debd0bbec99` | Corroborates proof and review. Its multi-task controller and finishing menu are rejected as the wrong unit and mutation boundary. |
| Ponytail | `16f29800fd2681bdf24f3eb4ccffe38be3baec6b` | Applies comprehension and minimization pressure. Volume-only completion and the exact ordered-sufficiency ladder wording are rejected. |
| Upper-bound language packet | Applicable rows retain their recorded `direct`, `corroborated`, `synthesis`, `inference`, or `thin` labels | Establishes bounded source meaning only. It does not prove runtime steering. |
| Implement facet packet | Pending | Research inventory only; no unresolved facet is decision-changing for this epoch. |

The inspected checkout identities are exact; upstream remote freshness is
unverified. Research establishes meaning and counterpressure, not local intent
or behavioral effect. No Research, Prototype, or Behavior Decision Interlude
is admitted.

### Semantic-Unit Ledger

Each unit has one basis, owner, disposition, runtime destination, and proof
obligation. `B0` means required in the executable minimum. Every row is
protected by its stated contract or safety basis, not merely by current
presence. Current equals B0, Prototype evidence is not applicable, and no C1
unit survives.

| Unit | Basis And Source Mechanic | Owner | Current / Research / Prototype / Protected / C1 Disposition | Destination | Required Proof |
| --- | --- | --- | --- | --- | --- |
| B0-01 explicit singleton admission | Intended explicit-only delivery; Matt supplies one supplied-work nucleus; local tracker policy supplies readiness | Caller selects; Implement admits; tracker defines eligibility | Current: keep; research: adapted direct core plus readiness synthesis; Prototype: N/A; protected: required contract; C1: none | Metadata and entry gate | Explicit policy; selected-ready positive; ambiguous, blocked, and unspecific-parent negatives |
| B0-02 compatible setup and readiness | Repo Bootstrap and repository policy are required compatibility | Repo Bootstrap owns setup; tracker owns readiness; Implement checks | Current: keep; research: local compatibility; Prototype: N/A; protected: authority boundary; C1: none | Entry gate and typed Return | Missing/incompatible setup and shaping-unready cases |
| B0-03 binding target and no substitution | Caller commitment and non-intuitive selection safety | Caller owns target; Implement preserves it | Current: keep; research: local contract; Prototype: N/A; protected: safety boundary; C1: none | Entry gate | Blocked explicit target must Return without choosing another or repairing readiness |
| B0-04 owner and staged-worker roles | Shared worker contract | Implement owner; staged worker only under explicit assignment | Current: keep; research: local compatibility; Prototype: N/A; protected: authority boundary; C1: none | Common owner path plus staged branch | Owner/staged positives and worker negatives for claim, review, commit, closeout, and completion |
| B0-05 claim, fixed point, and unrelated isolation | Tracker concurrency plus engineering Lock discipline | Implement; tracker owns claim transport | Current: keep; research: evidence-before-claims corroboration; Prototype: N/A; protected: safety boundary; C1: none | Pre-mutation gate | Claim read-back, dirty-index preservation, fixed-point and selected-work isolation |
| B0-06 immutable Charter | Local engineering contract and caller commitment boundary | Caller owns commitments; Implement records and holds Charter | Current: keep; research: local compatibility; Prototype: N/A; protected: authority boundary; C1: none | Common path | Missing Charter field and changed-commitment decision cases |
| B0-07 bounded patch and proof story | Matt implementation core adapted by local bounded-slice and semantic-proof contract | Implement | Current: keep; research: tracer/vertical/proof terms remain `synthesis` and behavior `untested`; Prototype: N/A; protected: required contract; C1: none | Common path | Acceptance-linked proof through a meaningful seam; included, excluded, and residual claims |
| B0-08 TDD versus Diagnosis routing | Matt supplies TDD pressure; Superpowers corroborates; Ponytail/Superpowers support root-cause pressure | TDD and Diagnosis own inner procedure; Implement owns trigger and Return | Current: keep pointers; research: source-bounded/corroborated; Prototype: N/A; protected: relationship contract; C1: none | Caller-facing pointers | Settled red-testable task routes to TDD; uncertain bug routes to Diagnosis; ownership Returns |
| B0-09 fresh semantic proof | Engineering proof discipline; Superpowers corroborates evidence before claims | Implement | Current: keep; research: corroborated rationale, no efficacy claim; Prototype: N/A; protected: required contract; C1: none | Common path | Fresh claim-matched proof; strongest safe structural proxy and named residual when execution is unsuitable |
| B0-10 immutable risk-scaled review | Matt direct review nucleus; Superpowers corroboration; local fixed-point contract | Implement selects one route; Review or Convergent PR Review owns judgment | Current: keep; research: direct/corroborated, fixed-point language `synthesis`; Prototype: N/A; protected: safety boundary; C1: none | Common route gate | Ordinary and high-risk cases, required Spec, immutable tree, exactly one owner |
| B0-11 complete finding admission and finite Repair | Finding Contract and caller-owned Charter | Review owns findings; Implement admits complete report; caller owns mixed decisions | Current: keep; research: local compatibility; Prototype: N/A; protected: safety boundary; C1: none | Conditional Repair branch | Mixed-authority report, complete eligible batch, unchanged Charter, exhausted Budget, successor review |
| B0-12 Lock, local closeout, one commit, connector closeout | Matt commit nucleus adapted by tracker and engineering irreversible order | Implement; repository and connector tracker owners define closeout/read-back | Current: keep; research: evidence-before-claims rationale; Prototype: N/A; protected: safety boundary; C1: none | Ordered completion chain | Local closeout in lock tree, post-review delta, index/tree equality, one commit, connector read-back, partial-mutation block |
| B0-13 typed Returns | Local completion contract | Implement | Current: keep; research: no steering claim; Prototype: N/A; protected: completion safety; C1: none | Return surface | Every stop branch names state, evidence, owner, release condition or continuation |
| X-01 parent graph, batch, finish, and delivery operations | Exceeds singleton outcome and authorized mutation boundary | To Tickets/Parallel Implement and separate delivery owners | Current: absent; research: Superpowers-only pressure rejected; Prototype: N/A; protected: exclusion; C1: rejected | Outside runtime | Relationship trace; no runtime arm |
| X-02 extra tables, helper, schema, ledger, or operations file | No observed recovery or context-loading failure | No admitted Implement owner | Current: absent; research: inference/thin only; Prototype: N/A; protected: no; C1: rejected or deferred | Non-runtime only | New intent and B0 failure required before reconsideration |
| X-03 ordered-sufficiency ladder and larger Budget | Ponytail inference does not establish local need; larger Budget lacks evidence | Implementation technique / caller | Current: absent; research: inference; Prototype: N/A; protected: no; C1: deferred | Outside current candidate | Exact B0 failure and new Prompt 1 admission required |

### Executable B0 And Exact C1 Decision

B0 is the current package. Its common path is:

```text
explicit singleton admission
-> setup, readiness, claim, fixed point
-> immutable Charter
-> bounded patch and fresh semantic proof
-> one immutable risk-scaled review
-> complete finding admission and bounded Repair when eligible
-> repository-local closeout
-> Lock and one exact commit
-> connector closeout and read-back
-> typed Return
```

Conditional branches are staged-worker handoff, TDD versus Diagnosis, Repair,
and repository-local versus connector closeout. Foreign procedures remain
behind their existing owners.

C1 delta is empty. No current-retention, pack-composition, source-mechanism, or
intent-counterexample hypothesis survived Prompt 1. Thus C1 is byte-identical
to B0; no contribution credit is available or needed.

### Relationships And Boundaries

| Caller | Verb | Callee | Observable Trigger And Return |
| --- | --- | --- | --- |
| Direct user | Invoke | `$implement` | User explicitly selects singleton delivery; Implement applies admission |
| `$to-tickets` | Recommend and stop | `$implement` | One serial ready ticket is selected; no automatic execution |
| `$diagnosing-bugs` | Recommend and stop | `$implement` | Standalone diagnosis yields ready causal remediation |
| `$improve-codebase` | Recommend and stop | `$implement` | One Concentrate candidate is a ready singleton |
| `$implement` | Invoke | `$tdd` | Behavior and red-capable seam are settled; proof Returns to the same owner |
| `$implement` | Invoke | `$diagnosing-bugs` | A required bug fact is uncertain; causal evidence Returns to the same owner |
| `$implement` | Invoke | `$review` | Ordinary immutable tree |
| `$implement` | Invoke | `$convergent-pr-review` | Local PR or bounded high-risk immutable tree |
| `$implement` | Recommend and stop | `$to-tickets` | Work is unsliced or shaping-unready |
| `$implement` | Recommend and stop | `$repo-bootstrap` | Setup is missing or incompatible |

Implement does not own the parent graph, queue, batching, shaping, review
judgment, TDD or Diagnosis procedure, tracker transport, push, deployment, PR,
merge, discard, parent closure, or release. More visible work does not convert
it into Parallel Implement.

### D0 Decision And Proof Matrix

D0 is required for every current-model steering claim because no qualifying
exact current-model telemetry proves that omitting the source-derived guidance
causes the registered failure. D0 holds invocation, task, context, tools,
authority, evidence, runtime, and rubric fixed and removes only the steering
under test. Independently required authority, machine, and relationship
contracts use deterministic or ownership proof unless efficacy is claimed.

The fixed behavior suite has nine families. Deterministic and owner/
relationship lanes remain separate. C1 contribution is not applicable.

| Proof Lane | Fixed Family Or Claim | D0 / Comparator Decision | Prompt 3/4 Proof And Acceptance |
| --- | --- | --- | --- |
| B0 viability | Compatible setup | D0 required for steering; deterministic setup pointers separate | Setup gap Returns without edits; compatible setup proceeds |
| B0 viability | Blocked binding | D0 required | Explicit blocked target is preserved; no substitution or readiness repair |
| B0 viability | Unspecific parent source | D0 required | Parent spec/plan/queue/batch/bare path stays context; shaping owner receives Return |
| Protected authority | Staged worker | D0 required for role steering; owner trace separate | Worker limits patch/proof/staging/Return and does not claim, review, commit, close, or complete |
| Relationship | Uncertain bug | D0 required for routing effect; trigger/Return trace separate | Diagnosis receives uncertain facts; TDD is not used until facts settle |
| Review routing | Ordinary and high-risk review route | D0 required for exactly-one/risk steering; route ownership separate | Ordinary selects Review; local PR/high risk selects Convergent PR Review; Spec is present |
| Repair authority | Mixed Repair authority | D0 required | Partial or reviewer-authorized Repair is refused; complete eligible caller-owned batch respects Budget |
| Lock safety | Post-review tree delta | D0 required for Lock steering; tree checks deterministic | Only verified repository-local closeout enters lock tree; other semantic delta blocks |
| Closeout safety | Partial connector read-back | D0 required for completion steering; provider contract trace separate | Attempted or partial mutation blocks; complete requires read-back after commit |
| Invocation/context | Explicit-only package | No D0 behavioral arm; deterministic machine contract | `allow_implicit_invocation: false`, inventory, metadata, and pointers agree |
| C1 contribution | Empty delta | Not applicable | No C1 arm and no contribution claim |
| Pruning | Exact Prompt 4-accepted C1 | Complete: no material cut admitted | Complete passage audit; C1 remains byte-identical; no equivalence wave required |
| Deterministic package | Exact bytes and relationships | No D0 | Inventory, component hashes, package tree hash, links, anchors, fences, tables, relationship traces |

Prompt 4 must use the current Behavioral Skill Evaluation contract: fixed
fresh contexts, at least five samples per arm for behavioral claims, explicit
rubrics, per-sample inspection, variance and worst-result recording, and no
candidate leakage. The nine families are bounded evidence, not universal
coverage.

Prompt 4 ran D0 before B0 with five fresh samples per arm. Exact B0 passed all
50 scored case decisions with no critical failure. D0 remained highly
compliant under the complete engineering and owner contracts: no family
exhibited its registered failure in five samples. Therefore every claimed
wording-efficacy disposition is `reject-no-control-failure`. The affected B0
units remain protected and byte-identical because independently settled local
authority, machine, relationship, safety, Return, and completion contracts
require them. Viability is accepted; causal wording efficacy is not claimed.

### Prior-Evidence Dispositions

| Evidence | Disposition | Exact Reuse And Limit |
| --- | --- | --- |
| 2026-07-23 construction transcript | `exact-reusable` only for deterministic identities; otherwise `historical-admission-only` | Exact bytes and task identities may be reused; historical decisions do not complete this epoch |
| Post-candidate evaluation | `lane-limited` | Exact for recorded bytes, tasks, and read-only decision lanes; lacks exact current runtime telemetry and did not perform live provider mutation |
| Frozen pre-prune control | `historical-admission-only` | Hash `ef2a52520462266ad0af171869d516c709ba57e737dcf9658e0a3cbb643af8bc`; historical pruning control, not current B0 |
| Structural tests | `lane-limited` | Exact only for machine contracts and relationships; they do not prove behavior |
| Older Implement simulations | `historical-admission-only` | Useful for mechanism inspection; package/runtime identities and authority differ |
| Audit handoff | `lane-limited` | Proves the caller's route into Implement, not Implement behavior |
| Prior install parity | `historical-admission-only` | Does not establish current installed state in this epoch |
| Prior synthesis and promotion record | `historical-admission-only` | Preserves historical truth but cannot replace the fresh Prompt 2 decision |

The older campaign truth remains: the pre-prune package at
`ef2a52520462266ad0af171869d516c709ba57e737dcf9658e0a3cbb643af8bc`
was pruned and promoted to
`b918d2762505a69d1ba577533a2b9bd040133188b0e0c6dee4cb7e78351b4f7c`,
with 45/45 registered read-only decision cases per arm and historical managed
installation parity. That history does not change this epoch's
`runtime-no-change` identity or satisfy its remaining lifecycle units.

### Rejected And Deferred Alternatives

Rejected for this contract: whole parent or batch scope; target substitution;
self-created readiness; worker-owned review, commit, or closeout;
reviewer-authorized, partial, or unbounded Repair; automatic Parallel
Implement conversion; merge, push, deployment, PR, discard, or finishing
menus; volume-only completion; redundant state, operation, authority, or proof
tables; the exact Ponytail ladder wording; and an Implement-specific helper or
schema.

Deferred pending a new Prompt 1 admission: a separate Bound operation, a
larger Repair Budget, or other runtime machinery supported by an exact B0
failure. No rejected or deferred item enters C1.

### Pruning Pass Decision

The complete two-file audit classified all 40 instruction-bearing passages as
`keep`: 0 `collapse`, 0 `disclose`, and 0 `delete`. Every apparent cut either
weakened an independent B0-01 through B0-13 owner or failed to reduce a named
load. Prompt 4's efficacy demotions do not authorize removing independently
required intent, authority, machine, relationship, safety, Return, or
completion contracts.

Final C1 remains
`b918d2762505a69d1ba577533a2b9bd040133188b0e0c6dee4cb7e78351b4f7c`.
Package files, bytes, `SKILL.md` words and lines, common-path instructions,
duplicated ownership, branch load, and unused package surface all have zero
delta. No current-epoch pre-prune fixture or behavioral wave was created.
There are no reverted cut groups; the rejected apparent opportunities and
residual load are recorded in the pruning transcript.

### Lifecycle And Residual Gaps

| Artifact Or Unit | Current Epoch State |
| --- | --- |
| Prompt 1 | Gate passed; source-first checkpoint frozen |
| Prompt 2 | Complete; decision-complete synthesis reconciled |
| Runtime package | Unchanged at exact current/B0/C1 hash |
| Prompt 3 | Complete; exact shared B0/C1 corpus and D0/B0 packet frozen in the current-epoch construction record |
| Prompt 4 | Complete; exact B0/C1 accepted, all tested efficacy claims demoted |
| Pruning Pass | Complete; `pruning-not-needed`; exact C1 unchanged |
| Prompt 5 | Complete; canonical no-op integration, experimental retirement, and managed installation parity verified |
| Git delivery | Bare mode; not authorized |

Residual gaps:

1. causal wording efficacy over the complete owner context remains unproved;
2. live dirty-index Lock and connector mutations, including partial provider
   failure, have not been run;
3. the nine fixed families do not cover every repository or tracker;
4. exact backend model-build, reasoning, token, and latency telemetry is
   unavailable;
5. upstream remote freshness is unverified; and
6. Implement facet research remains pending but is not decision-changing.

Canonical and installed runtime share the accepted identity. The Implement
experimental tree and manifest entry are absent, every unrelated candidate is
preserved, and relationships remain unchanged. Tracker mutation, staging, and
Git delivery remain outside this campaign.

Authorized unit completed: Deploy Prompt 3
Decision: ready-for-prompt-4
Campaign shape: hypothesis-candidate; M0 != H1
Runtime identities: current `b918d2762505a69d1ba577533a2b9bd040133188b0e0c6dee4cb7e78351b4f7c`; corrected M0 checkpoint `c56d01368a49d19ed33fb4d0c4b926029d6264885e36bea5af820f40c31d5f0c`; M0 `1f61b9880b881771b579372075dd89e3bccb7ce9c94ae3bd0e30f1f43ae8724f`; H1 `3960bc073a49022faaf581c4829653e16d470e3c9a727066d8a755a1086b5e8d`
Artifacts changed: skills/experimental/implement/**; only the implement entry in skills/experimental/manifest.json; docs/synthesis/skills/implement.md; docs/validation/transcripts/2026-07-24-implement-prompt3-construction.md
Evidence used or reused: exact corrected M0 checkpoint and R2 research identities; Prompt 2 decision ledger; current canonical identity as comparison; historical behavior and promotion evidence admission-only; no behavioral evidence reused
Residual gaps: fresh corrected-M0 viability and removal-safety behavior; H1 control deficits and contribution; live Git/provider mutation; provider-composite idempotence; transfer, telemetry, upstream-freshness, and non-decision-changing facet-research limits
Recommended next unit: Deploy Prompt 4
Git HEAD: 94d68e78d8812e9a2ceffd093e729402cac1cff2 -> 94d68e78d8812e9a2ceffd093e729402cac1cff2
Git delivery: pending
Exact stop reason: Prompt 3 materialized and froze exact executable M0 and fully classified H1, pre-registered complete M0 viability and both M0-first quality-lift controls, and the Shared Run Contract forbids starting Prompt 4.
