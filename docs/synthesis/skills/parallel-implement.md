# Parallel Implement — Active Synthesis

Campaign epoch: `2026-07-24-r2`
Skill: `parallel-implement`
Campaign shape: `minimum-candidate`
Git fixed point: `94d68e78d8812e9a2ceffd093e729402cac1cff2`

## Active Decision

Deploy Prompt 4 accepted the corrected minimum runtime, and the Pruning Pass
found no material behavior-preserving cut. Deploy Prompt 5 promoted the exact
P1 package into the canonical active skill.

```text
M0 = H1 = V1 = P1 = canonical = installed
c347c36f782f401d770709bf0170dc9a5ac1a77a97038ea42f6d5b163b4c6a0d

pre-promotion canonical
036dfdb8afc4bb34968c83a9bbd429e14d63819f2041c61b9518336d0e4770dc

failed r1 M0 = H1
bbaf9558628dc5370d6b5f5770785746763eee63ca93f84113e18cb75307ed05
```

The eight-file active package is behavior-complete for the frozen local
contract. The supported managed installer synchronized the exact canonical
tree, installed-root validation passed, and the post-install dry-run reports
all 25 managed skills unchanged. Historical r1/r2 research, protocols,
captures, results, transcripts, and prior synthesis fingerprints remain
evidence, not active runtime instructions.

## Intended Contract And Protected Behavior

`parallel-implement` is an explicit-only root orchestrator for one explicitly
requested top-level delivery of a settled parent and its non-empty, exhaustive
Ready-for-agent graph. It validates setup and the complete caller packet
before mutation; freezes the Charter, graph, identities, proof, Repair Budget,
mutation boundary, and durable campaign state; derives a truthful,
resource-qualified frontier; claims before dispatch; uses isolated bounded
lanes; drains and lands results serially; proves the accumulated current
integration state; obtains root-selected formal review; admits only bounded
Repair; closes verified children before the parent with mutation read-back;
and reconciles every owned state before a terminal or resumable Return.

The protected set is:

- M0-01 through M0-17 and clauses C01 through C17 in their irreversible order;
- K-01 through K-08 at their named compatibility boundaries;
- explicit-only root admission and adjacent exclusions;
- parent and ticket commitment authority plus every foreign-owner boundary;
- canonical durable campaign state, derived-view limits, typed invalid-state
  and resume branches, and full reconciliation;
- resource-qualified frontier judgment, claim read-back, isolated bounded
  lanes, bounded worker and integrator authority, and typed Returns;
- serial landing, preserved partial Git state, current accumulated proof,
  invalidated overlays, and reblocked dependents;
- root-owned ordinary or bounded-high-risk review selected from explicit
  target and risk facts;
- intact blocking reports, complete caller admission of the complete
  all-automatic individually Charter-preserving within-budget Repair set,
  identity-matched successor proof, and fresh owner-matched successor review;
- child-first then parent-last closeout with mutation read-back; and
- recovery-complete nonterminal Return and exact terminal completion.

Installation, publication, push, Git delivery, and unrelated cleanup remain
outside the runtime owner.

## Research And H1 Decisions

Independent research supports reconciliation, resource-qualified frontiers,
drain-before-final-review, bounded review generations, serial integration,
partial-state preservation, and verified closeout under their recorded
conditions. These methods already express required M0 behavior and therefore
did not create beyond-minimum H1 units.

| Candidate | Classification | Active decision |
| --- | --- | --- |
| Reconciliation loop | `independently-supported` with replay and external-state limits | M0-equivalent; retain neutral reconciliation behavior |
| Resource-qualified frontier | `independently-supported` under recorded isolation conditions | M0-equivalent |
| Drain barrier | `independently-supported` with early-feedback counterpressure | M0-equivalent |
| Generation budget | `independently-supported` as bounded review practice | M0-equivalent |
| Semantic operation identity | provider-specific support; universally `unverified` | deferred; no universal runtime addition |
| Work stealing, adaptive width, result queue | `contested` for this stateful application | rejected without a settled deficit and hazard proof |
| Concurrent landing | `contested` by stale-base, order, and accumulated-proof conditions | rejected; contradicts serial integration |
| Per-task formal review as final-review replacement | early feedback independently supported | rejected as a replacement for final drained review |
| Prepared terminal facade or projection authority | `unverified` | deferred; cannot replace root reconciliation |
| Passive efficiency or throughput claims | `unverified` | rejected without real telemetry and pre-registered use |
| Warm-primary reuse or compact receipts | `unverified` | rejected without freshness and sufficiency proof |
| Friction synthesis as campaign outcome | `pack-specific` compatibility | rejected as behavior; compatibility only |

H1 admitted no semantic unit and remained byte-identical to M0. Prompt 4
therefore ran no H1 arm and made no comparative contribution claim. The
current-only runtime push action and arbitrary same-actor-once rule remain
rejected and unprotected.

## Verified Behavior And Pruning

The corrected Prompt 4 protocol ran five fresh blinded M0 samples on the exact
candidate with `gpt-5.6-sol`, high reasoning, fixed fixtures, host, tools, and
root-held scoring. All five samples passed all five criteria: 25/25 criteria,
mean 5/5, range 5–5, and zero critical failures.

The fresh affected lanes established:

- ordinary O1/O2 snapshots select `$review`;
- local-PR bounded-high-risk H1/H2 snapshots select
  `$convergent-pr-review`;
- the original F1/F2/F3 report returns intact;
- Repair remains closed until the caller admits exactly `{F1,F2}` and both
  blockers are automatic-in-scope, individually Charter-preserving, and
  within both frozen budgets; and
- every repaired successor requires identity-matched current proof and a fresh
  review by the same selected owner before completion.

Exact helper and deterministic compatibility evidence covered K-02 through
K-08 at their registered boundaries. In particular, the promoted helpers
retain these exact identities:

```text
scripts/run_ledger.py
caa174522351d903985dbe94632bb54f6beb16e5eb3dcdcd31e64ee2bbae1f2d

scripts/lane_worktree.py
0884edd628114a9101c3e9d544ee03699fbbef93b3c3ff2a35dcb915e0897ce8
```

The Pruning Pass audited every instruction-bearing passage in the eight-file
package. Common gates were already inline; ledger commands, lane lifecycle,
worker packets, and optional integrator mechanics were already behind
trigger-bearing pointers. Collapsing boundary-local authority, Review/Repair
gates, lifecycle safety, compatibility commands, or explicit-only metadata
was either non-material, behavior-risking, or forbidden by exact K-02/K-04
compatibility. The result was `pruning-not-needed`: P1 equals V1 byte-for-byte
at 164091 bytes, 3693 lines, and eight files.

## Relationships

No relationship topology, trigger, authority, or Return changed. The active
relationships remain:

| Relationship | Trigger and Return |
| --- | --- |
| caller / `to-tickets` → `parallel-implement` | The user explicitly requests top-level delivery of one settled parent through its non-empty exhaustive Ready-for-agent graph; parent delivery remains with the root campaign |
| `parallel-implement` → `tdd` | A bounded lane has red-testable new behavior, or a bug with every required fact; proved implementation returns to that lane |
| `parallel-implement` → `diagnosing-bugs` | A bounded lane lacks a required bug fact; diagnosis or an authorized fix returns to that lane |
| `parallel-implement` → `review` | A drained proved ordinary immutable candidate or repaired successor needs fixed-snapshot Spec and Standards judgment; judgment returns to root |
| `parallel-implement` → `convergent-pr-review` | A drained proved local PR or bounded high-risk diff needs terminal release judgment; judgment returns to root |
| `parallel-implement` → `resolving-merge-conflicts` | Serial landing enters preserved conflict or partial Git state; exact verified state returns to root |
| `parallel-implement` → `to-tickets` | Graph, readiness, profile, matrix, order, scope, or frontier is incomplete or contradictory; one exhaustive repair packet returns and mutation stops |
| `parallel-implement` → `repo-bootstrap` | Required setup is missing or incompatible; setup is recommended and mutation stops |

Because these rows did not change, Prompt 5 deliberately leaves the
relationship index untouched.

## Evidence Pointers

- Corrected M0 checkpoint:
  [`2026-07-24-parallel-implement-prompt1-m0-r2.md`](../../validation/transcripts/2026-07-24-parallel-implement-prompt1-m0-r2.md)
- Corrected research packet:
  [`parallel-implement-2026-07-24-r2.md`](../../research/parallel-implement-2026-07-24-r2.md)
- Exact construction protocol:
  [`protocol-manifest.json`](../../validation/evals/parallel-implement-prompt4-r2/protocol-manifest.json)
- Accepted behavior decision:
  [`2026-07-24-parallel-implement-prompt4-r2.md`](../../validation/transcripts/2026-07-24-parallel-implement-prompt4-r2.md)
- Compact results:
  [`results-manifest.json`](../../validation/evals/parallel-implement-prompt4-r2/results-manifest.json)
- Pruning decision:
  [`2026-07-24-parallel-implement-pruning-r2.md`](../../validation/transcripts/2026-07-24-parallel-implement-pruning-r2.md)

The failed r1 candidate, its fixtures, five captures, results, transcript, and
earlier synthesis remain historical evidence. They identified a missing route
fact and a superseded F1-only Repair rubric; they do not establish corrected
candidate viability.

## Deliberate Non-Changes And Residual Limits

Prompt 5 makes no relationship-topology, test-contract, helper, evaluation,
historical-evidence, research, or method change. It preserves every unrelated
experimental candidate and all unrelated Implement work. It does not stage,
commit, push, publish, or begin Git delivery.

Behavioral evidence is limited to the exact package bytes, five fixtures,
model, host, reasoning configuration, and tools. Live tracker/provider partial
mutation, remote publication, irreversible closeout, and provider-specific
idempotency remain unproved. Worktree containment does not establish process,
credential, network, cache, submodule, or scarce-resource isolation. Exact
backend build, random seed, token counts, and latency were unavailable.
