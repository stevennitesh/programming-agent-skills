# Convergent PR Review M0/H1 Candidate Record

Campaign: `2026-07-24-convergent-pr-review`

Behavior-audit state: `accepted`

## Authoritative Owners

- Intended contract, M0 units and clauses, viability suite, local authorities,
  and limitations:
  [Prompt 1](../../transcripts/2026-07-24-convergent-pr-review-prompt1-m0.md).
- Independent source provenance, method support, conditions, counterpressure,
  and limits:
  [research packet](../../../research/convergent-pr-review-2026-07-24.md).
- Active H1 admissions, current dispositions, rejected and deferred
  alternatives, protected behavior, and relationships:
  [active synthesis](../../../synthesis/skills/convergent-pr-review.md).
- Shared semantic state, bounded input identities, exact package inventories
  and hashes, passage maps, construction proof, and absence checks:
  [campaign manifest](campaign-manifest.json).
- Worker-visible fixed tasks, facts, authority, initial state, tools, mutation
  boundary, and output contract:
  [worker fixture](fixtures/worker-visible.json).
- Root-only hypotheses, expected M0 weaknesses, candidate terms, rubrics,
  branch judgments, score traces, and conclusions:
  [root-only fixture](fixtures/root-only-evaluation.json).

No Conditional Interlude was required or opened. The Prompt 2 record owns that
closed disposition. This record does not restate those owners.

## Candidate-Specific Proof Plan

Prompt 4 must verify every frozen identity, then run and judge the complete M0
viability suite before any comparison. For each contribution cluster it must
inspect the first fresh M0 sample before releasing the remaining four, run all
five M0 controls, and release H1 only when the registered defect or meaningful
quality deficit appears while M0 remains viable. H1 runs use the same
worker-visible fixture in reverse sample order and a fresh context for every
dispatch.

`Q01-snapshot` isolates H1-01. `Q02-coverage-truth` jointly exercises H1-02,
H1-03, and H1-04 because the same finite coverage fixture and rubric preserve
separate per-unit scores while observing their coherent interaction.
`Q03-bounded-recovery` isolates H1-05. Current canonical bytes are comparison
evidence, not a behavioral arm.

Machine and ownership proof uses the proof IDs registered in the campaign
manifest and root-only fixture. Every worker dispatch is assembled from one
exact runtime arm plus one selected worker-visible case; no hypothesis,
expected weakness, rubric, score, candidate term, prior output, or conclusion
may enter that payload. Exact service model and reasoning identifiers were not
available to Prompt 3 and must be recorded and frozen before Prompt 4's first
sample.

## Prompt 4 Result

The [results manifest](results.json) owns per-sample judgments and points to
all 51 [raw outputs](raw/). Twenty-five valid M0 viability samples passed.
One initial V09 dispatch omitted the fixed simulated-root authority, received
zero credit, and was rerun once with the unchanged task, facts, runtime, and
rubric.

| Cluster | M0 | H1 | Decision |
| --- | ---: | ---: | --- |
| `Q01-snapshot` | 5 | 0 | `reject-no-control-deficit`; H1-01 removed |
| `Q02-coverage-truth` | 5 | 5 | H1-02/H1-04 had no control deficit; H1-03 improved 2/5, below the frozen 4/5 threshold |
| `Q03-bounded-recovery` | 5 | 5 | H1-05 improved 1/5, below the frozen 4/5 threshold |

No H1 unit survived. V1 equals exact M0 at package-tree SHA-256
`6c419036d5cb8000d47666f2e02d414330c2369933a75654bac786bd8cff7280`.
No runtime arm or frozen fixture changed.

## Pruning Pass Boundary

Pruning may begin only after Prompt 4 fixes V1. It may remove or compress
duplicate, no-op, scattered, or branch-only expression only while preserving
every accepted M0/H1 semantic, implicit invocation, root and caller authority,
complete read-only mutation boundary, mode and capacity compatibility,
Standards/Spec separation, finding and advisory owners, drift, four decisions,
complete Return, and stop boundary.

Pruning may not add behavior, revive F01-F12, create a runtime file or helper,
change a caller packet or relationship, treat lower word count as proof, or
reuse an M0 control as P1 equivalence evidence when the cut changes the tested
instruction.

## Affected Relationships

Construction exercises but does not publish changes to these existing
relationships:

- `review` hands off one selected local PR or bounded high-risk candidate and
  stops before its ordinary Pin;
- `implement` and `parallel-implement` invoke this skill while retaining
  Repair, risk acceptance, Lock, Release, and successor-snapshot authority;
- this skill loads the `review` Finding Contract at admission, the Advisory
  Contract only when enabled, and the Smell Baseline only when local Standards
  are thin;
- an immutable repository-baseline audit receives an `audit-codebase`
  recommendation and stop, never automatic execution; and
- this skill never hands an active high-risk review back to `review`.

## Residual Load And Limits

H1 added six classified inline passages and no runtime file, helper, schema,
or context pointer beyond the common two-file package, but none met the frozen
contribution bar. Portable atomic dirty-tree capture, external-state
completeness, authenticity, statistical independence, universal reviewer
counts, and multi-round truth remain outside the claim.

The fixture protocol is bounded to its registered families. Exact service
model and reasoning identifiers were unavailable and recorded as such before
sampling. Git delivery remains unset.

## Pruning Result

The exact 40-passage M0 map was reused against V1. One coherent cut group
collapsed M0-P04, M0-P24, and M0-P34 and deleted duplicated expression from
M0-P28 and M0-P40. Operative owners remain at their gates; no behavior, helper,
relationship, or H1 unit was added.

P1 is materialized under `runtime/p1` with the exact two-file inventory:
`package-tree-sha256:6c33843eb1ad6074af36e634f7ebd98882ede20a24b9120c130796285047001d`.
`SKILL.md` fell from 11,685 bytes/1,646 words to 11,211 bytes/1,576 words;
metadata is unchanged.

Nine fresh-context P1 equivalence samples covered V04, V14-V17, V20,
V22-V23, and V25. All preserved the registered branch, authority, capacity,
advisory, decision, Return, and stop behavior with no critical or meaningful
regression. Structural read-back preserves M0-01 through M0-20, all protected
compatibility and relationships, and F01-F12 absence. Disposition: `pruned`.
## Prompt 5 Promotion And Installation

Exact P1 was promoted without runtime editing. Canonical and installed
inventories are exactly `SKILL.md` and `agents/openai.yaml`; both package trees
equal P1 at
`6c33843eb1ad6074af36e634f7ebd98882ede20a24b9120c130796285047001d`.

The four stale focused assertions that named superseded headings or literal
sentences were reconciled to P1's ordered semantic gates. Focused proof passed
59 tests; the full integration suite passed 207 tests with 4 skips; skill
validation passed. The managed preview named only `convergent-pr-review`,
supported synchronization completed, exact installed parity passed, and the
post-install preview reported all 25 managed skills unchanged.

No behavioral sample was rerun because exact accepted P1 bytes were unchanged.
No caller, shared Review contract, relationship map, or other skill runtime was
changed. Git delivery remains pending and was not authorized.

## Prompt 4 Entry-Predicate Reconciliation

The bounded reconciliation preserved frozen M0/H1, original raw chronology,
M0 viability `25/25`, and H1-01/H1-02/H1-04 decisions. It independently
preregistered and sampled H1-03 and H1-05 under the current observable-entry
predicate contract.

- H1-03: `reject-no-control-deficit`; M0 exposed the registered material
  qualification/common-mode facts in all five entry-positive controls.
- H1-05: `reject-regression`; the control-deficit gate was not met, and
  wrong-condition H1 authorized field completion after target-byte drift in
  `H105-WC04`.

No H1 unit survives. Reconciled V1 remains exact M0 at
`6c419036d5cb8000d47666f2e02d414330c2369933a75654bac786bd8cff7280`.
Because V1 identity is unchanged, affected pruning and Prompt 5 are not
required.
