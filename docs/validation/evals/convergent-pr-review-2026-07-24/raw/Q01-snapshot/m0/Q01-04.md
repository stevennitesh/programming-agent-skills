# Q01-04 M0 raw output

status: complete

declared manifest:
- Main repository: resolved `HEAD`, fixed-point object, exact index entries and computed index-tree identity, staged/unstaged binary diffs, porcelain status, all in-scope untracked paths, and their bytes.
- Submodule: parent `HEAD` and index gitlink OIDs, checked-out submodule `HEAD`, plus the submodule’s own index-tree identity, staged/unstaged diffs, status, and in-scope untracked bytes.
- Nested repository: parent-path classification, nested `HEAD`, index-tree identity, staged/unstaged diffs, status, and in-scope untracked bytes. Nested Git administration is identity evidence, not target content.
- Ignored-path policy: enumerate ignored paths and matching rules per repository. Exclude ignored bytes by default; explicitly include the declared nested-repository root as nested state even if its parent path is ignored. No other ignored path is silently included.
- Each captured stream and included file receives an in-memory content digest. The index-tree OIDs are calculated from index entries in memory, without `git write-tree`.

non-writing evidence:
- Only existing refs, objects, indexes, worktree files, ignore rules, and filesystem metadata were read.
- Diff, status, `ls-files`, `check-ignore`, object-resolution, and hash-without-write operations are read-only.
- No fetch, checkout, reset, clean, add, `write-tree`, `mktree`, stash, worktree creation, ref update, object write, index refresh/lock, dependency/cache write, or review artifact occurred.
- All command output and ledgers remained in root context; no repository file was created.

operation log:
1. Declared authority: read-only capture; mutation authority none.
2. Resolved the fixed point and main `HEAD` from existing objects/refs.
3. Captured the main repository’s index, diffs, status, untracked bytes, and ignored-path classification.
4. Read the parent gitlinks and captured the submodule’s complete live state.
5. Crossed the explicitly declared nested-repository boundary and captured its complete live state independently.
6. Recorded the include/exclude decision for every discovered ignored path.
7. Re-read all main, submodule, nested-repository, untracked, and ignored-policy surfaces; identities and digests matched.

final:
- Capture result: complete.
- Scope includes both required nested states.
- Ignored-path policy: explicit and closed.
- Drift: clear.
- Blockers: none.
- Mutation authority: none.
- Successor snapshot authority: none.
- Return boundary: caller.
