# Q01-01 M0 raw output

Capture plan

- Declare target kind: Git-addressed local commit `T1`.
- Pin the complete 64-hex SHA-256 object ID `O1`; do not use a symbolic ref.
- Read `O1` and capture the exact selected diff bytes `D1`.
- Record `O1`, the byte identity of `D1`, and the caller-declared selected scope in root context only.
- Perform no fetch, checkout, helper invocation, file write, or live-worktree capture.

Observed surfaces

- Local Git object `O1`: initially readable.
- Selected content `D1`: complete, exact diff bytes.
- Live worktree, index, status, and untracked files: intentionally outside scope.

Drift plan

- Before Return, re-resolve `O1`.
- Re-read the originally selected diff content and compare it byte-for-byte, or by the same recorded cryptographic byte identity, with captured `D1`.
- Do not recapture or substitute another target.

Supported claims: the review used the locally available object identified by `O1`; its selected diff content matched `D1`; and those target surfaces did or did not remain unchanged through drift readback.

Identity limits: `O1` provides local content-addressed identity only. It does not establish external authenticity, trusted provenance, authorship, repository origin, or external attestation. No claim extends beyond the declared selected scope.

Failure Return: if `O1` becomes unreadable, resolves inconsistently, the selected bytes differ, scope proves partial or ambiguous, or identity comparison cannot be completed, return `incomplete` with the verified partial evidence and exact blocker. Do not mutate, fetch, infer, narrow scope, or recapture.

Concise log/final state: `capture: O1 readable; D1 captured; mutations none; drift: pending readback`. After a matching readback: `capture/drift complete; target unchanged; authenticity unattested; Return boundary: caller; Mutation authority: none; Successor snapshot authority: none.`
