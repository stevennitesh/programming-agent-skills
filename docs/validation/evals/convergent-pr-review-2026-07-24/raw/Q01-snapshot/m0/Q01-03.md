# Q01-03 M0 raw output

```text
DECLARED CAPTURE MANIFEST

target kind: explicitly selected live local target
scope:
- staged: a.py
- unstaged: b.py
- untracked: c.cfg

capture once, in memory only:
- H0 = exact HEAD object identity
- I0 = exact index-tree identity
- DS0 = complete staged diff bytes
- DU0 = complete unstaged diff bytes
- S0 = complete status
- U0["c.cfg"] = exact c.cfg bytes and content identity

limits:
- Read only.
- No locks, helper changes, writes, artifacts, checkout, fetch, dependency/cache changes, or recapture.
- Later path/status equality is insufficient; captured untracked bytes are decision-bearing target surfaces.

DRIFT READBACK

repeat:
- H1 = HEAD
- I1 = index tree
- DS1 = staged diff
- DU1 = unstaged diff
- S1 = status
- U1["c.cfg"] = exact current bytes

comparison:
- H1 == H0
- I1 == I0
- DS1 == DS0
- DU1 == DU0
- S1 == S0
- c.cfg remains at the same path with the same status
- U1["c.cfg"] != U0["c.cfg"]

drift result: FAILED
reason: captured in-scope untracked content changed after Pin
action: do not recapture or silently replace U0; preserve verified partial evidence

OPERATION LOG

1. Read HEAD identity.
2. Read index tree without changing locks or helpers.
3. Read complete staged diff for a.py.
4. Read complete unstaged diff for b.py.
5. Read complete status.
6. Read exact bytes of in-scope untracked c.cfg.
7. Retain the manifest in root context only.
8. Before decision, repeat all six target-surface reads.
9. Detect c.cfg byte drift despite unchanged path/status.
10. Stop with incomplete; perform no mutation.

FINAL

review mode: initial
snapshot identity: H0 + I0 + DS0 + DU0 + S0 + U0["c.cfg"]
target: live local staged a.py, unstaged b.py, untracked c.cfg
drift: failed — c.cfg content changed
terminal decision: incomplete
exact blocker: the pinned live-local target is no longer immutable because a captured untracked target surface changed
verified partial evidence: HEAD, index tree, staged diff, unstaged diff, and status remained stable
Return boundary: caller
Mutation authority: none
Successor snapshot authority: none
```
