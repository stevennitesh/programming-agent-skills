# V10 M0 raw output

V10 passes M0. The runtime correctly captures the live target as one composite, including `new.cfg` path and exact bytes, while preserving read-only state. No target-capture gap found.
