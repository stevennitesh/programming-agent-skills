# V11 M0 raw output

## V11 Standards result

Operative authority: `runtime/m0/SKILL.md`.
`runtime/m0/agents/openai.yaml` supplies invocation metadata only; it adds no Standards precedence rule.

### Variant A — complete local Standards

- Loaded Standards sources:
  - repository instructions
  - routed guidance
  - maintained configuration
  - test and tool documentation
  - meaningful nearby conventions
- `review/SMELL-BASELINE.md`: **not loaded**, because local Standards are not thin.
- Exact precedence: applicable local Standards govern. The skill specifies no precedence order among the local source categories; conflicts must be recorded, not resolved by inventing an order.
- Final state: local Standards trace complete; coverage may be frozen using those sources.

### Variant B — no local rule for the concern

- First loaded/traced the applicable local Standards sources.
- Because local Standards are thin for the concern, additionally loaded the available `review/SMELL-BASELINE.md`.
- Exact precedence:
  1. Applicable local Standards.
  2. Shared smell baseline only as fallback preference.
- For this concern, no local rule exists, so the smell baseline supplies the fallback standard. If any applicable local rule is later identified in the already-pinned material, it governs over the baseline.
- Final state: the concern has a fallback Standards source; other local Standards retain priority. Coverage may be frozen with the fallback provenance explicitly recorded.

### Read-only operation log / terminal state

1. Accepted the caller-frozen packet unchanged.
2. Kept the initial snapshot and fixed point pinned; did not recapture or alter them.
3. Traced local Standards before reviewer dispatch.
4. Evaluated whether local Standards were thin.
5. Variant A stopped at local sources; Variant B conditionally added the shared smell baseline.
6. Recorded exact sources and precedence.
7. Performed no mutation, fetch, checkout, dependency/cache change, artifact creation, or external action.

Terminal authority state: read-only tracing complete; caller packet frozen; initial snapshot still pinned; mutation authority remains none.
