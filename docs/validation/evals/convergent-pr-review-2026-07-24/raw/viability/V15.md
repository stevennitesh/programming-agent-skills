# V15 M0 raw output

- Lane A: zero credit—contaminated by parent hypothesis and peer conclusion.
- Lane B: zero credit—missing required `coverage` and `blockers` fields.
- Neither initial invalid return enters the candidate ledger or completed-reviewer count.
- Safely use the single replacement slot only with a new fresh context, the same immutable snapshot, and an unbiased factual brief. Select the lane from the frozen coverage plan that best preserves required coverage; do not reuse either invalid payload.
- If replacement is valid: capacity = exactly one; root performs separated passes for every remaining lens. Maximum clean decision is `pass with residual risk`.
- If replacement is invalid: capacity = zero; root performs separated passes with explicit lens resets for all required lenses. Maximum clean decision remains `pass with residual risk`.
- Any lens still uncovered yields `incomplete`; an independently verified admitted blocker may yield `blocked`.
- Log only the replacement’s valid factual observations in the candidate ledger, dispose every resulting candidate, verify drift, and return the full caller-bound packet with reduced confidence plus:
  `Return boundary: caller`
  `Mutation authority: none`
  `Successor snapshot authority: none`
