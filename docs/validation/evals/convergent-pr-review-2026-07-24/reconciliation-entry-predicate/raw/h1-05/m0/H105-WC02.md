# H105-WC02 M0 raw output

Decline recovery because the single recovery action was already consumed. The
report missing `blockers` receives zero credit; no replacement is authorized.
Affected coverage remains unresolved; decision boundary: `incomplete`.

