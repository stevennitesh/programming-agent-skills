{
  "schema_version": 1,
  "campaign": "2026-07-24-to-spec",
  "execution": {
    "model": "gpt-5.6-sol",
    "host": "Codex fresh-context subagent",
    "reasoning_effort": "xhigh",
    "fresh_context": true,
    "tools": [
      "read exact assigned runtime package",
      "read or receive exactly one assigned worker-visible variant",
      "simulate fixture-scoped disposable draft",
      "simulate one configured tracker create",
      "simulate full tracker read-back"
    ]
  },
  "payload_template": {
    "runtime_arm": "{M0|H1}",
    "runtime_tree_sha256": "{tree_sha256}",
    "sample_id": "{sample_id}",
    "task": "{worker_task}",
    "source_facts": "{worker_source_facts}",
    "observations": "{worker_observations}",
    "authority": "{worker_authority}",
    "initial_state": "{worker_initial_state}",
    "operations": "{worker_operations}",
    "mutation_boundary": "{worker_mutation_boundary}",
    "requested_output": "{worker_requested_output}",
    "isolation": "Do not read the root-only fixture, other arm, candidate, synthesis, research, transcripts, peer outputs, hypotheses, rubrics, scoring, candidate terms, or conclusions.",
    "worker_boundary": "Do not edit shared source, decide adoption, interact with the user, or spawn agents."
  },
  "dispatch_identity_algorithm": "SHA-256 of UTF-8 template_sha256<LF>runtime_tree_sha256<LF>worker_visible_sha256<LF>sample_id<LF>; the worker-visible file and unique sample_id reconstruct the exact assigned parameters",
  "capture_contract": [
    "sample id",
    "runtime arm",
    "typed result or explicit interim branch",
    "operation log",
    "final draft state",
    "durable-state snapshot",
    "parent specification when produced",
    "observed limitations"
  ],
  "arm_delta": "The exact runtime package is the only permitted M0 versus H1 delta.",
  "h1_dispatch_gate": "Dispatch H1 only after five valid M0 controls exhibit the root-only pre-registered meaningful deficit while M0 remains viable."
}
