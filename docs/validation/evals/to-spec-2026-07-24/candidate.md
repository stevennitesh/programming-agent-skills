# To Spec M0/H1 Candidate Record

Campaign: `2026-07-24-to-spec`

Evaluation state: `accepted`

## Authoritative owners

- Intended contract, M0 semantic ledger, clause map, viability suite, local
  authorities, and limitations:
  [`2026-07-24-to-spec-prompt1-m0.md`](../../transcripts/2026-07-24-to-spec-prompt1-m0.md)
  bounded fingerprint
  `b19edb0b03a176b0e4f903c001f1705587d04a4306bbd05be8c3d625d3f7a726`.
- Research provenance and method classifications:
  [`2026-07-24-to-spec.md`](../../../research/2026-07-24-to-spec.md)
  byte identity
  `2fecf286bdcdfa8a40269ee59a57e4d996736f447a88db5af15d54cb71215f37`.
- Active H1 decisions, current dispositions, rejected and deferred
  alternatives, protected behavior, and relationships:
  [`to-spec.md`](../../../synthesis/skills/to-spec.md) bounded fingerprint
  `84eee89c376ee5e003428e06fa1a53315dd93241e4137b505e620d56435aed72`.
- Shared semantic state, exact construction identities, package inventories,
  passage maps, proof IDs, and absence checks:
  [`campaign-manifest.json`](campaign-manifest.json).
- Worker-visible fixed tasks and source facts:
  [`worker-visible.json`](fixtures/worker-visible.json).
- Root-only expected weaknesses, rubrics, candidate terms, and conclusions:
  [`root-only-evaluation.json`](fixtures/root-only-evaluation.json).

No Conditional Interlude was required or opened. The Prompt 2 record owns that
closed disposition.

## Candidate-specific proof plan

Prompt 4 must verify exact identities and run the registered M0 viability suite
before any H1 comparison. For each H1 cluster it must inspect one fresh M0
sample before releasing the remaining M0 wave, require the registered
quality-lift deficit while M0 remains viable, and run H1 only after that gate.
The fixed execution tuple, five-sample minimum, two-family coverage for broad
claims, arm ordering, worker/root visibility boundary, rubric traces, and stop
rules live only in the two fixture owners above.

Machine and ownership proof uses `P-STRUCT-01`, `P-STRUCT-02`, `P-ABS-01`,
`P-REL-01`, `P-INV-01`, `P-SAFE-01`, `P-CONTEXT-01`, and
`P-H1-CLASS-01`. Behavioral proof IDs and their exact M0/H1 arms are assigned
in the campaign manifest. Current canonical bytes remain comparison evidence,
not a third arm.

## Pruning Pass boundary

Pruning may begin only after Prompt 4 fixes V1. It may reduce duplicate,
no-op, scattered, or branch-only expression but must preserve every accepted
M0/H1 semantic, explicit-only invocation, typed failure and success Returns,
irreversible mutation order, complete read-back, no-blind-retry recovery,
foreign-owner boundaries, and recommendation-and-stop relationships. It may
not add behavior, revisit rejected units, create a helper, or treat word count
as proof.

Exact V1 is M0 tree
`548af7fd1dd0c581fd472f5652ee0c294381c082ecfc927604300edaf07ddaaa`.
H1-01 through H1-07 are each `rejected-no-control-deficit`; five fresh M0
controls per cluster exposed none of their registered deficits, so no H1 arm
was eligible. The results manifest and human decision record own the exact
samples and aggregate.

## Affected relationships

Construction exercises, but does not publish changes to, these existing
relationships: Skill Router to `to-spec`; Wayfinder to `to-spec`; Improve
Codebase to `to-spec`; vocabulary-only Load of `codebase-design`; setup
failure recommendation-and-stop to `repo-bootstrap`; and verified-success
recommendation-and-stop to `to-tickets`.

## Pruning result

The complete audit reused the exact M0 runtime-clause map and classified all 17
instruction-bearing passages as `keep`. No plausible material `collapse`,
`disclose`, or `delete` group survived the protection test: each passage owns
M0 behavior or a necessary invocation, authority, order, safe failure, Return,
completion, or relationship guardrail. The two-file package contains no
helper, copied foreign procedure, unused support, or branch-only reference.

P1 is V1 byte-for-byte. The package tree remains
`548af7fd1dd0c581fd472f5652ee0c294381c082ecfc927604300edaf07ddaaa`,
stored once under `runtime/m0` with exact alias records for V1 and P1. Package
load remains 5,664 bytes and 745 `SKILL.md` words; the delta is zero. Prompt 4
proof is exact-reusable and no new behavioral wave was run. The cut ledger and
proof disposition live in [`pruning-results.json`](pruning-results.json).

## Residual load and limits

The evaluated H1 added seven inline quality-lift passages and one H1 completion
qualification, but none survives into V1. Live provider mutation/recovery and
claims beyond the fixed model, host, reasoning, tools, fixtures, and family
bounds remain unavailable. P1 is fixed; promotion, installation, and delivery
remain unset.

## Promotion and installation

Deploy Prompt 5 promoted the exact P1 package into canonical `to-spec` without
semantic adaptation. Complete package read-back, the directly affected
contract checks, and the full integration suite established canonical proof;
the accepted Prompt 4 behavior evidence remained exact-reusable because P1 is
byte-identical to V1.

The target experimental package and its exact manifest entry were absent, so
cleanup was a target-scoped no-op. The pre-install managed preview named only
`to-spec` as changed against the controller's 25-skill baseline. The supported
installer synchronized the managed pack, canonical/installed complete-tree
comparison passed, and the post-install preview reported all 25 skills
unchanged. Exact canonical and installed identities, evidence dispositions,
and proof pointers remain owned by
[`campaign-manifest.json`](campaign-manifest.json).

Live provider mutation/recovery and transfer beyond the fixed Prompt 4 model,
host, reasoning, tools, fixtures, and family bounds remain missing. No
relationship, helper, source, domain, tracker, Git, or delivery behavior was
added.
