# Response

The bounded slice is one compatibility-migration behavior slice across every
coupled compatibility surface, not an arbitrary file-count slice:

1. Freeze the Charter around continued compatibility during migration:
   dual-read, new-write, a backfill guard, and legacy/current proof are in
   scope; cutover is explicitly out of scope. The authorized write boundary is
   the compatibility migration surfaces.
2. Read the supplied state matrix and map each settled legacy/current state to
   its read, write, and backfill behavior. Do not choose precedence, guard
   conditions, or storage details that the matrix does not supply.
3. Implement the coupled behavior as one candidate:
   - preserve reads for legacy state through the required dual-read path;
   - preserve reads for current state through that same compatibility path;
   - send new writes through the required new-write behavior;
   - enforce the matrix-defined backfill guard.
   Changing only the storage write is not an admissible smaller slice because
   it would break legacy reads.
4. Prove the candidate through behavior branches:
   - legacy-state read;
   - current-state read;
   - new write followed by a compatible read;
   - every matrix-defined backfill-guard allow branch;
   - every matrix-defined backfill-guard reject/no-op branch;
   - the coupled legacy/current regression suite.
   No cutover branch or legacy-removal claim is included.
5. On the exact proved candidate, perform the required staged checks and route
   one formal review. Any admitted repair must preserve this Charter and be
   re-proved across the same branches before fresh review.
6. Only after accepted review may tracker-kind closeout, Lock, and the one exact
   commit proceed in the runtime-prescribed order. Push is not authorized.

Implementation cannot start from the supplied packet, however, because no
finite Repair budget is available. The runtime requires that budget to be
frozen in the Charter before production mutation. Return this decision to the
caller/repository/risk authority; the safest resume action is to supply a
finite Repair budget, then resume at Charter freeze without changing the
behavioral slice above.

# Metadata

- Fixture: `Q1-HF-03`
- Status: `blocked`
- Failed gate: pre-mutation Charter freeze
- Authority needed: a finite Repair budget from settled caller, repository, or
  risk authority
- Repository mutations: none
- External mutations: none
- Claim state: no claim was supplied or fabricated
- Retained changes: none
- Checks run: none; no candidate exists
- Skipped checks: implementation proof, staged checks, formal review, Lock,
  commit, tracker closeout, and push
- Residual risk: read precedence, exact guard conditions, and other
  matrix-defined behavior must not be invented before the supplied state
  matrix defines them
- Evidence:
  - acceptance requires dual-read, new-write, backfill guard, and
    legacy/current tests;
  - changing storage write alone would break legacy reads;
  - all compatibility surfaces are authorized;
  - cutover is excluded;
  - the runtime requires a finite Repair budget before mutation, and none is
    supplied.
