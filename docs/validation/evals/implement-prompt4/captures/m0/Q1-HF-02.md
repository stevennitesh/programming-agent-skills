## Response

The one bounded change is: deliver the selected CLI flag as one complete command path within the accepted CLI behavior-and-proof boundary.

Surface disposition:

- Parser: include the flag definition and accepted parsing behavior.
- Configuration handoff: include propagation of the parsed value into the existing configuration handoff.
- Help text: include the user-visible flag documentation required by acceptance.
- End-to-end command test: include it as the highest meaningful proof seam, covering the accepted command behavior through configuration handoff.
- Parser-only proposed patch: do not accept it as the implementation slice; it leaves three required acceptance surfaces incomplete.
- Unrelated command cleanup: exclude it and return it as optional adjacent work.

Next authorized actions, in order:

1. Read the supplied command map to locate the four required seams.
2. Freeze a Charter that keeps all four surfaces in one bounded slice, excludes the unrelated cleanup, names the end-to-end command test as proof, and obtains any still-required finite Repair budget before production mutation.
3. Reconcile the repository, setup, tracker kind and readiness facts without fabricating any unavailable state; claim only if the configured tracker requires it and the simulated claim reads back exactly.
4. Implement the parser, configuration handoff, help text, and end-to-end command test together through the settled behavior seam.
5. Run the command test and any required repository checks, reconcile the complete diff, and route the exact proved candidate to the required formal review owner.
6. Only after an accepted review, follow the applicable tracker-kind closeout order, Lock the exact candidate, stage intentionally, and create one verified commit. Do not push without separate authority.

No implementation, tracker operation, commit, push, or other external mutation was executed.

## Metadata

```yaml
fixture_id: Q1-HF-02
cluster: H1-01
family: horizontal-fragment
status: partial
current_gate: slice_selection
slice_selected: parser + configuration handoff + help text + end-to-end command test
primary_proof_seam: end-to-end command test
proposed_parser_only_patch: rejected_as_incomplete
optional_command_cleanup: excluded
repository_mutation: none
tracker_mutation: none
commit: none
push: none
claim_state: not_claimed
skipped_checks:
  - all execution checks; this fixture authorized slice selection only
residual_risk:
  - repository, setup, tracker-kind, readiness, and finite Repair-budget facts must be reconciled before production mutation
authority_needed:
  - any missing finite Repair budget and configured tracker facts from their named owners
safest_resume_action: read the supplied command map, then freeze the complete four-surface Charter before mutation
evidence:
  - acceptance names parser, configuration handoff, help text, and an end-to-end command test
  - the proposed patch changes only the parser
  - the unrelated command cleanup is optional
```
