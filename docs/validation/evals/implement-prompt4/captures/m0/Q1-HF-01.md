# Response

The selected bounded change is one end-to-end API-field addition. The proposed request-parsing-only patch is not an acceptable implementation slice because it leaves four required acceptance surfaces unfulfilled.

The included surfaces are:

1. Request parsing accepts the field at the observable request seam.
2. Domain validation applies the settled rules for the field at the observable domain seam.
3. Persistence carries the accepted field through the observable persistence seam.
4. Response serialization exposes the field at the observable response seam.
5. One integration proof exercises the field across parsing, validation, persistence, and serialization.

The proof story is a single observable path through all five surfaces: demonstrate request acceptance, the required domain-validation behavior, persistence of the accepted value, serialization of the resulting value, and an integration check tying those behaviors together. Each surface also needs its assigned seam-level proof where required by the repository’s existing proof lane. No claim may be made for a parsing-only candidate.

Before any production mutation, the next authorized action is to read the supplied architecture map and identify the existing seam at each of the five accepted surfaces. File names, implementation technique, validation rules, persistence representation, response shape, and exact checks cannot be invented from the current facts. After that read, implementation may proceed only if the resulting Charter preserves the supplied acceptance and scope and all other runtime gates are satisfied.

Excluded work includes unrelated API fields, adjacent refactors, changes beyond the five accepted surfaces, altered product or architecture commitments, tracker or repository-setup work, conflict reconciliation, parent-graph delivery, deployment, PR creation, merge, and push.

The caller/source owner retains authority over the outcome, acceptance, contracts, scope, exclusions, permissions, and any commitment change. Implement owns the narrow end-to-end repository change inside that settled boundary, its proof, formal-review routing, admitted bounded Repair, Lock, one commit, configured closeout, and truthful Return. The named owners retain setup, tracker transport and fields, formal review judgment, TDD, Diagnosis, conflict reconciliation, parent-graph delivery, and push authorization. If the architecture map reveals that any required surface needs a commitment or scope change, implementation must return that decision rather than silently narrowing the slice or expanding authority.

# Metadata

- Fixture: `Q1-HF-01`
- Cluster: `H1-01`
- Family: `horizontal-fragment`
- Decision: Reject the parsing-only fragment; select one end-to-end slice covering all five accepted surfaces.
- Current authorized operation: Read the supplied architecture map.
- Repository mutations performed: None.
- External mutations performed: None.
- Evidence used: The supplied acceptance requires request parsing, domain validation, persistence, response serialization, and an integration proof; all five are caller-scoped; each has an observable seam.
