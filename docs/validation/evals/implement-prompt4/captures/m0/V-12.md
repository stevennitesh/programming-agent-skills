# Response

`partial` — commit identity verification failed.

- Failed gate: the verified commit tree must equal the accepted lock tree. The index equaled lock tree `L` before commit, but the commit hook changed a generated tracked file and the resulting `HEAD` tree is `H`, where `H != L`.
- Repository state: the authorized commit exists at tree `H`. The accepted candidate remains identified by `L`, but `L` is not the committed tree. The generated-file delta in `H` is retained; the post-commit index and worktree state were not supplied and are not inferred.
- Lock/completion: Lock is not established, the commit is not accepted, and completion is not claimed.
- Scope evidence: unrelated index state was excluded from the pre-commit index because that index equaled `L`; no claim is made about any unsupplied post-hook status.
- Claim/tracker state: not supplied and unchanged by this simulation.
- Skipped gates: tracker closeout, claim release, push, and any completion read-back were not performed.
- Residual risk: tree `H` contains generated tracked bytes that were not part of the exact accepted tree `L`.
- Authority needed: a further commit operation or other recovery that changes repository history requires renewed caller authority because the single authorized commit has already been used.
- Safest resume action: preserve `H` and `L`, inspect the simulated post-hook status and exact `H`-versus-`L` tree delta, then return to the caller for authority to restore and commit the exact accepted tree.

# Metadata

- Fixture: `V-12`
- Family: `commit-identity`
- Requested outcome: return the commit-tree mismatch and retained state
- Terminal status: `partial`
- Mutations performed: supplied simulated commit only
- External mutations: none
- Repository mutations: none
