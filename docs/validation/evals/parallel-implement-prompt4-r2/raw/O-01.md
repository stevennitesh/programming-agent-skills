# O-01

## Review owner

Select `$review`. The supplied target facts say O1 is an ordinary local branch diff, not a PR, touches no high-risk surface, and is classified ordinary by the frozen risk record; the runtime rule assigns ordinary candidates to `$review` and reserves `$convergent-pr-review` for local PRs or bounded high-risk diffs.

## Disposition before caller admission

Return the complete formal-review report intact as a nonterminal, decision-required blocked disposition, containing F1, F2, and F3. The supplied report facts classify F1 and F2 as blocking and F3 as advisory/nonblocking, so the complete blocking set is exactly `{F1, F2}` while F3 remains visible but grants no Repair scope.

No Repair plan, assignment, mutation, or successor snapshot may open at this point. The supplied facts say caller admission is absent; the runtime rule says review grants no mutation and forbids all Repair and successor work until the complete blocking report has first been preserved and returned intact for caller decision.

F1 alone cannot authorize a partial Repair even though the supplied report classifies it automatic-in-scope, gives concrete defect evidence and required proof, and says it is Charter-preserving if resolved. The supplied blocking set also contains F2, and F2 is decision-required before admission; the runtime rejects mixed, partial, or decision-required blocker batches.

The remaining one Repair generation and one successor-review invocation do not by themselves authorize work. The supplied facts establish available budget, while the runtime requires caller admission and every eligibility gate in addition to budget.

## First moment Repair may open

Repair may first open only after the intact report has been returned and the caller newly admits the complete blocking set exactly as `{F1, F2}`. The supplied post-decision facts say that exact admission occurs and both F1 and F2 are then automatic-in-scope.

The admitted blocker IDs are exactly `F1` and `F2`; `F3` is excluded because the supplied report classifies it advisory and nonblocking. The gates are:

- completeness: admitted IDs `{F1, F2}` equal every blocking ID, from the supplied complete report and exact caller admission;
- automatic scope: both F1 and F2 are automatic-in-scope after admission, from the supplied post-decision classifications;
- Charter preservation: F1's admitted remedy stays within the frozen Charter and changes no commitment, and F2's caller-authorized disposition stays within the frozen Charter and changes no commitment, from the supplied per-finding evidence;
- proof specificity: F1 already carries concrete defect evidence and required proof, while F2 may be corrected only according to the supplied caller-authorized disposition and its required correction proof;
- budget: one Repair generation and one successor-review invocation remain, from the supplied frozen budget facts.

Only when all of those facts coexist may one bounded Repair generation open for the complete batch `{F1, F2}` under the original Charter.

## Successor requirements

The repaired successor must be a new immutable snapshot O2, and every delta from O1 to O2 must map to admitted finding F1 or F2. This follows from the supplied successor identity O2 and the runtime's requirement that Repair change only admitted findings and that every successor delta map to an admitted finding ID.

O2 must receive identity-matched proof, and that proof must pass before review. The supplied successor facts say identity-matched proof can run and pass on O2; the runtime requires identity-matched proof for every admitted repaired successor and requires current-HEAD proof before formal review.

O2 must then receive a fresh formal-review invocation; the prior O1 report cannot approve it. The supplied facts say one successor-review invocation remains and a fresh formal review can judge O2, while the runtime requires fresh formal review for every repaired successor.

The successor review owner remains `$review`, because the supplied O2 facts preserve the same ordinary, non-PR, non-high-risk target type under the frozen ordinary risk record.

## State

The state is nonterminal and resumable, not complete. The supplied facts authorize opening one bounded Repair generation after admission and make O2 proof and fresh review feasible, but supply neither an executed Repair, an O2 proof result, nor an O2 review decision. Resume by opening Repair generation one for exactly `{F1, F2}`, producing O2, running identity-matched proof, and invoking the one remaining fresh `$review`; Lock remains closed until that successor review accepts O2.
