# O-02

## Review owner

Select `$review`. The target is an ordinary local branch diff, not a PR, touches no high-risk surface, and remains ordinary under the frozen risk record; those facts select the ordinary review owner rather than `$convergent-pr-review`.
**Supplied fact:** “It is an ordinary local branch diff, not a PR, touches no high-risk surface, and the frozen risk record classifies it ordinary.”

O1 is eligible to be pinned for review because implementation is drained, the integration worktree is clean, and identity-matched proof passes.
**Supplied fact:** “Implementation is drained, the integration worktree is clean, and identity-matched proof passes.”

## Complete disposition before caller admission

Return the complete report intact for caller decision:

- `F1`: blocking; currently `automatic-in-scope`; concrete defect evidence and required proof are present; Charter-preserving if resolved.
- `F2`: blocking; currently `decision-required`; caller disposition is required before correction; Charter-preserving if resolved.
- `F3`: advisory and nonblocking; preserve it in the report, but it is not part of the blocking set and grants no Repair authority.

**Supplied fact:** “Before admission, the complete formal-review report is: F1 blocking, automatic-in-scope, with concrete defect evidence and required proof, Charter-preserving if resolved; F2 blocking, decision-required, requiring caller disposition before correction, Charter-preserving if resolved; F3 advisory and nonblocking. Caller admission is absent.”

The complete blocking set is `{F1,F2}`. No Repair plan, assignment, mutation, or successor snapshot may open at this point: caller admission is absent, and the set is mixed because F2 is still decision-required. F1’s eligibility alone does not authorize a partial Repair. The exact decision gap is caller disposition for F2 plus admission of the complete blocking set.
**Supplied facts:** F1 is automatic-in-scope; F2 is decision-required and needs caller disposition; caller admission is absent; F1 and F2 are the only blocking findings; F3 is nonblocking.

The pre-admission state is therefore nonterminal `blocked` and resumable at O1, retaining the clean identity-proved snapshot and the intact three-finding report.
**Supplied facts:** O1 is clean and identity-matched proof passes; caller admission is absent; F2 requires caller disposition.

## First moment Repair may open

Repair may first open only after the intact report has returned for decision and the caller’s new admission of exactly `{F1,F2}` is recorded, with the caller-authorized F2 disposition and the evidence below reconciled. It may not open at any earlier moment.
**Supplied fact:** “After the intact report returns for decision, the caller newly admits the complete blocking set exactly as {F1,F2}; both F1 and F2 are then automatic-in-scope.”

The exact admitted blocker IDs and gate results are:

| Gate | F1 | F2 | Batch result |
| --- | --- | --- | --- |
| Admitted ID is in the complete blocking set | admitted as `F1` | admitted as `F2` | admitted IDs equal every blocking ID: `{F1,F2}` |
| Automatic-in-scope after admission | yes | yes, after caller-authorized disposition | pass |
| Individually Charter-preserving | yes; remedy stays within the frozen Charter and changes no commitment | yes; authorized disposition stays within the frozen Charter and changes no commitment | pass |
| Concrete correction/proof authority | existing concrete defect evidence and required proof | caller-authorized disposition supplies the formerly missing decision; correction remains bounded by its required proof | pass |
| Repair-generation budget | one generation remains | same single batch | pass |
| Successor-review budget | one invocation remains | same successor review | pass |

**Supplied facts:** the caller admits exactly `{F1,F2}`; both become automatic-in-scope; F1’s admitted remedy remains within the frozen Charter and changes no commitment; F2’s caller-authorized disposition remains within the frozen Charter and changes no commitment; one Repair generation and one successor-review invocation remain.

F3 remains advisory and nonblocking and does not enter the Repair write scope.
**Supplied fact:** “F3 advisory and nonblocking.”

## Successor O2 requirements

The one Repair generation must be one bounded batch containing exactly `F1` and `F2`, with every resulting delta mapped to one of those admitted IDs and with the required proof for both remedies.
**Supplied facts:** the complete admitted blocking set is exactly `{F1,F2}`; both are automatic-in-scope and Charter-preserving; one Repair generation remains.

O2 must receive identity-matched proof on the repaired successor, and that proof may be recorded as passing because the fixture explicitly permits it.
**Supplied fact:** “Identity-matched proof can run and pass on O2.”

O2 must then receive a fresh formal review; select `$review` again because O2 is still the same ordinary, non-PR, non-high-risk target under the frozen ordinary risk record. The fresh review must judge O2 itself rather than reusing O1’s decision.
**Supplied facts:** “Successor: O2, still the same ordinary non-PR, non-high-risk target under the frozen ordinary risk record”; “a fresh formal review can judge O2.”

## Resulting state

This fixture does not supply a fresh review decision for O2, so the campaign cannot enter Lock or declare `complete`. After the admitted Repair and passing successor proof, the state is nonterminal `partial` and resumable at the fresh `$review` invocation/decision for O2, with the single remaining successor-review invocation reserved for that review.
**Supplied facts:** one successor-review invocation remains; identity-matched proof can pass on O2; a fresh formal review can judge O2; no result of that fresh review is supplied.

All tracker, Git, worktree, remote, installation, publication, and destructive operations remain simulated only.
**Supplied fact:** “Simulation only; no real mutation, publication, installation, or destructive action.”
