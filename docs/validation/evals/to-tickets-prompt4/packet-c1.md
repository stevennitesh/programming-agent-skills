# To Tickets Prompt 4 C1 Fixed Packet

Use the assigned runtime instructions to answer every case independently. This
is a no-mutation simulation: do not edit a tracker, invoke another skill, or
start implementation. Tracker setup is compatible, publication authority
belongs to `Owner A`, and all source below is settled.

## Cases

1. `tracer-positive`: A new payment-provider integration has one load-bearing
   uncertainty: whether signed webhook events traverse the real HTTP adapter,
   verification library, and event boundary correctly. Early feedback on that
   integration risk is the named purpose. Propose the thinnest useful work
   unit and its proof.
2. `tracer-negative`: A normal independently valuable vertical slice lets a
   user save notification preferences through the UI and API. There is no
   named learning or load-bearing-risk purpose. Propose the work unit and its
   proof.
3. `support-positive`: Delivery ticket D1 adds exports. A reusable test-harness
   adapter is a durable enabling change with its own behavior-preserving proof.
   Supplied estimates show adding it inside D1 costs three sessions and blocks
   another approved delivery, while a separate adapter ticket costs one
   session and unlocks both. Decide whether and how to slice it.
4. `support-cleanup`: The repository has generic cleanup and speculative
   prefactoring unrelated to a named delivery ticket. Decide whether it gets a
   separate ticket.
5. `support-inline`: Delivery D2 needs a six-line durable helper with focused
   proof; supplied estimates show a separate ticket adds more coordination
   cost than implementing it inside D2. Decide whether and how to slice it.
6. `exposure-positive`: A backfill changes ten million records. It can advance
   through tenant batches. Before widening each batch, operators can inspect
   health, old/new compatibility, and a tested rollback. Propose work units,
   gates, acceptance, and proof.
7. `exposure-negative`: A low-risk atomic validation-message change has no
   expanding exposure boundary and no compatibility migration. Propose the
   work unit and gates.

## Output

Write one Markdown file. Give each case its own heading. Include concrete work
unit form, acceptance, proof, edges or gates, and rationale where applicable;
do not merely assert compliance. End with `Actions actually performed:` and
list any mutation, skill invocation, implementation, or dispatch actually
performed.
