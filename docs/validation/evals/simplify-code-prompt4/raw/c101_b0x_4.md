Mode and region: single-cut, read-only selection in the bounded target.

Cut: delete the expired branch proven unreachable.

Net obligation reduction: one branch family and its associated control-flow reasoning are removed; no maintenance obligation is introduced.

Preserved contract and observable seam: commitments and edge behavior remain unchanged, observed through the same caller-facing fixture.

Proof: the caller-facing fixture passes before the cut and passes after the cut.

Changed code paths: none; this is a read-only selection.

Starting and ending work state: unchanged. The index and unrelated state are untouched.

Deferred candidates: the loop rewrite, private-abstraction removal, runtime-native helper replacement, and project-owned helper replacement remain unselected.

Stop reason: exactly one coherent cut was selected.

Residual owner: implementation remains with a future authorized Simplify Code execution.

Skipped proof or risk: no proof command was run and no production mutation was made; the return relies on the fixed packet's stated fixture result and reachability proof.
