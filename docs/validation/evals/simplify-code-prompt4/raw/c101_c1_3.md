Mode: one bounded cut

Region: the named bounded target

Cut: Delete the expired branch proven unreachable, including only fallout created by that deletion.

Net obligation reduction: One branch family and its unreachable behavior path are removed. No replacement obligation is introduced.

Preserved contract and observable seam: Existing commitments and edge behavior remain unchanged. The same caller-facing fixture is the observable seam.

Proof:
- Before: the caller-facing fixture passes.
- After: the same caller-facing fixture passes.

Changed paths: Only the bounded target path containing the expired branch, plus deletion-created fallout within that target.

Work state:
- Start: existing work and index recorded without mutation.
- End: the cut remains unstaged; unrelated work is unchanged; staged entries and cached binary diff are byte-identical to the starting snapshots.

Deferred candidates: loop rewrite, private-abstraction removal, runtime-native helper replacement, and project-owned helper reuse remain untouched.

Stop reason: The requested single coherent cut is complete.

Residual owner: The user owns any later cut selection.

Skipped proof or risk: None.
