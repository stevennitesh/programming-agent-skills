## Main condition

**Lock:** Lock does not succeed. The cached bytes no longer have the exact
identity recorded at the starting state, even though the staged paths and
status letters still read `M assets/logo.bin` and `M src/config.txt`.

**Before evidence:** Before the cut, record the starting ref, full work status,
relevant diff, and an identity-bearing snapshot of the index. For the staged
files, that snapshot must include each index entry's mode, object ID, and stage
number (or an equivalent comparison of the cached bytes), not merely its path
and status letter.

**Comparison:** Refresh the work state after proof and compare the current
index entries with the pre-cut index snapshot. The changed object IDs for both
staged paths expose the drift that a status-text comparison cannot expose.

**Mutation and state:** The simplification may change only the admitted
working-tree cut and fallout created by that cut, and it must remain unstaged.
It may not rewrite, restore, stage, or otherwise reconcile either concurrent
index change. Unrelated working-tree and external state remain untouched.

**Return:** Return **No safe simplification** with the admitted region, the
otherwise-safe candidate, the preserved behavior and proof seam, the before
and after proof, the changed paths from the cut, the starting index identities,
the ending index identities, the concurrent index drift as the exact stop
reason, and the concurrent actor's staged work as an owning residual. Report
that the index was not changed in response to the drift and that the result
remains unstaged.

## Companion wrong-condition

**Lock:** Lock does not succeed. A snapshot first taken after the concurrent
mutation cannot establish the starting index, and status text alone does not
identify cached content.

**Before evidence:** There is no usable before snapshot of the index entries or
cached bytes. The available `M assets/logo.bin` and `M src/config.txt` lines
describe both states identically.

**Comparison:** Comparing the post-mutation status text with refreshed status
text shows no path or letter change and therefore cannot detect or exclude the
cached-byte drift. No exact before-to-after index comparison is available.

**Mutation and state:** No further production, index, staging, unrelated, or
external state may be changed to manufacture the missing evidence. In
particular, neither staged file may be restored or restaged.

**Return:** Return **No safe simplification** with the bounded region, candidate
accounting, current work state, the missing pre-mutation index identity as the
exact proof gap, the inability to establish index preservation as the stop
reason, any already-created unstaged cut paths, and the residual owner. State
which proof was run or skipped and leave the index and unrelated state as
currently found.
