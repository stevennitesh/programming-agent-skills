Mode: single-cut, read-only.

Region: the bounded target described by C1-01 Neutral B0 Control.

Cut: delete the expired branch proven unreachable.

Selection: Delete is the first rung in Simplify Code's required inspection
order. The packet establishes that this candidate is safe, behavior-preserving,
inside the region, and supported by credible caller-facing proof, so the first
admissible rung wins. Stop after this one coherent cut.

Net obligation reduction: one unreachable branch and its associated decision
path are removed. No replacement abstraction, helper, dependency, branch, or
coordination obligation is introduced.

Preserved contract and observable seam: all existing commitments and edge
behavior remain unchanged; the same caller-facing fixture is the observable
seam.

Proof before: the caller-facing fixture passes for the starting target.

Proof after: the same caller-facing fixture passes after deletion of the
expired branch.

Changed paths: none in this read-only execution.

Starting and ending work state: unchanged. The index, staged entries, cached
binary diff, unrelated work, installed state, and external state are
unchanged.

Deferred candidates: reuse the existing project-owned semantic owner; replace
the local helper with the runtime-native semantic match; collapse the private
abstraction; shrink the loop's branching and data movement. Each remains
admissible but is outside this one-cut result because an earlier rung was
selected.

Stop reason: Budget exhausted — the bounded one-cut allowance is consumed.

Residual owner: the user owns any decision to authorize another bounded cut or
an until-clean campaign.

Skipped proof or risk: no proof gap is identified by the packet. No repository
command was run because execution is read-only and the packet supplies the
before-and-after fixture result.
