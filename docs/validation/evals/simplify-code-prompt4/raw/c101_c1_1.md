Mode: one bounded cut, read-only.

Region: the supplied bounded target.

Selected cut: delete the expired branch proven unreachable. The active package
inspects Delete first and states that the first admissible rung wins. The
packet makes this deletion safe, behavior-preserving, in-region, and credibly
proved, so the presentation order does not control selection.

Net obligation reduction: one expired branch and its branch family removed;
no maintenance obligation introduced.

Preserved contract and observable seam: commitments and edge behavior remain
unchanged; the same caller-facing fixture is the proof seam.

Proof: the caller-facing fixture passes before the cut and after the cut, as
specified by the packet.

Changed paths: none; this run is read-only.

Work state: starting and ending work state are identical. The index, staged
state, and unrelated work remain unchanged.

Deferred candidates: the loop rewrite, private-abstraction removal,
runtime-native replacement, and project-owned reuse remain admissible but are
not selected because Simplify Code stops after the first admissible rung and
permits exactly one coherent cut here.

Stop reason: one coherent cut selected; stop after selection.

Residual owner: the target owner may admit any deferred candidate in a future
run.

Skipped proof or risk: no repository mutation or live proof command was run in
this read-only exercise; the proof result and safety claims come from the
fixed packet.
