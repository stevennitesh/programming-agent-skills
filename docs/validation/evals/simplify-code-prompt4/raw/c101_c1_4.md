Mode: single bounded Simplify Code cut selection, read-only.

Region: the bounded target described in C1-01.

Cut: delete the expired branch proven unreachable. The package inspects Delete
before Reuse, Standardize, Collapse, and Shrink. Because the deletion satisfies
the stated behavior, commitment, edge-case, proof, and obligation gates, select
it and stop before considering later rungs.

Net obligation reduction: one expired branch and its branch-family reasoning
are removed; no new concept, dependency, configuration, or coordination
obligation is introduced.

Preserved contract and observable seam: caller-visible behavior, commitments,
and edge behavior remain unchanged. The caller-facing fixture is the observable
seam.

Proof: before, the caller-facing fixture passes. After the selected deletion,
the same fixture passes. Both observations are supplied by the packet; no proof
command was run in this read-only selection.

Changed paths: none. The cut was selected but not applied.

Work state: starting and ending repository work state, staged entries, cached
binary diff, index, and unrelated state are identical because this run was
read-only.

Deferred moves: reuse of the project-owned semantic owner, runtime-native
standardization, collapse of the private abstraction, and shrinking the loop
remain unexamined after the first admissible rung wins.

Terminal classification: Boundary stop. The read-only boundary prevents
mutation and independent proof execution.

Residual owner: a mutation-authorized Simplify Code run owns applying the
selected deletion and executing before/after proof.

Skipped proof and risk: repository proof was not independently executed.
Current evidence is limited to the packet's fixture and reachability
statements.
