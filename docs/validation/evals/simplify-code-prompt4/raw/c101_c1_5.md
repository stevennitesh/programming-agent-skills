Mode: single bounded cut, read-only.

Region: the bounded target in `C1-01 Neutral B0 Control`.

Cut: delete the expired branch proven unreachable.

Selection: the active package inspects deletion before reuse, standardization,
collapse, and shrink moves. The expired unreachable branch is admissible, so
that first rung selects the cut independently of presentation order.

Net obligation reduction: one expired decision path and its branch maintenance
are removed; no maintenance obligation is introduced.

Preserved contract and observable seam: caller-visible behavior, commitments,
edge behavior, and the caller-facing fixture remain unchanged.

Proof: the same caller-facing fixture passes before and after the cut.

Changed paths: none in this read-only exercise.

Work state: unchanged from start to finish. Staged entries, cached binary diff,
index state, unrelated work, installed state, and external state remain
unchanged.

Deferred candidates: the loop rewrite, private-abstraction removal,
runtime-native helper replacement, and project-owned helper reuse remain
unmodified because the requested mode stops after one coherent cut.

Stop reason: the single selected cut is complete.

Residual owner: the bounded target's owner controls any later simplification.

Skipped proof or risk: no repository command was run and no production bytes
were changed; the fixed packet supplies the before-and-after fixture result and
the reachability proof.
