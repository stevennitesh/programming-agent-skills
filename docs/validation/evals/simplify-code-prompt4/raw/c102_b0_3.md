# C1-02 — B0 control sample 3

## Positive

**Cut:** Yes. Delete the proposed branch, bounded to the private parser and
fallout created by that deletion.

**Proof boundary:** Preserve the versioned protocol contract at the parser's
operational caller: the specified input must produce the same caller-visible
output. Observe the smallest caller-facing characterization before mutation,
then rerun that same characterization after the cut; it must detect the branch
deletion if the contract changes. Follow with the nearest relevant tests and
proportionate repository checks. The characterization licenses only this
contract and cut, not other parser behavior.

**State:** Read-only decision. No mutation, proof run, staging change, or
work-state inspection was performed.

**Return:** Return the licensed bounded cut to Simplify Code for
`Cut -> Prove -> Lock`, with the versioned contract and caller-facing
characterization held fixed.

## Negative

**No safe simplification:** Do not delete the branch.

**Proof boundary:** The proposed assertion observes private branch layout
rather than caller-visible output, so it cannot establish preserved behavior.
Creating a public hook would introduce a new commitment and lies outside this
simplification.

**State:** Read-only decision. No mutation, proof run, staging change, or
work-state inspection was performed.

**Return:** Return the missing caller-facing proof seam to Codebase Design. The
branch remains until an authoritative observable contract and a trusted
red-capable caller-facing characterization exist.
