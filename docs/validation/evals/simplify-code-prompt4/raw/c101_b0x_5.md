Mode and region: read-only Simplify Code selection for the bounded target in
`C1-01 Neutral B0 Control`.

Selected cut: remove the private abstraction that earns no boundary.

Net obligation reduction: one private abstraction and its indirection are
removed without introducing a replacement concept, dependency, contract, or
coordination path.

Preserved contract and observable seam: caller-visible behavior and edge
behavior remain unchanged; the same caller-facing fixture is the proof seam.

Proof: the packet states that the caller-facing fixture passes before and after
this cut. No additional proof was run in this read-only selection.

Changed paths: none.

Work state: the starting and ending repository state are unchanged; no edit,
staging, installation, tracker, or external-state action was performed.

Deferred candidates: the loop rewrite, both helper replacements, and deletion
of the expired unreachable branch remain unselected because this operation is
limited to exactly one coherent cut.

Stop reason: one admissible cut has been selected and the requested read-only
operation is complete.

Residual owner: implementation and repository-state verification remain with
the owner of any later mutation.

Skipped proof or risk: repository work state, concrete callers, and executable
test output were outside the supplied read-only packet, so they must be
refreshed before any production mutation.
