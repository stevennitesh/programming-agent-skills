# C1-02 B0 Control Sample 5

## Positive

**Decision:** Cut.

The authoritative versioned protocol contract fixes the caller-visible input
and output, so deleting the proposed branch needs no new commitment. The
smallest caller-facing characterization is the proof seam: observe it passing
against the exact starting state, make the bounded branch deletion and only
its fallout, then rerun that same characterization and the nearest relevant
tests. The cut is locked only if the characterization detects the deletion
when expected, passes after the behavior-preserving implementation change, and
the obligation ledger shows a strict net reduction.

**State:** Read-only decision. No production file, test, index, unrelated work,
installed state, or external state was changed. Starting ref, status, staged
shape, and ending work state were not supplied, so execution must record and
preserve them.

**Return:** Bounded branch deletion licensed at the caller-facing protocol
seam. Preserved contract: the versioned protocol's caller-visible input and
output. Before proof: the smallest characterization passes and detects the
proposed deletion. After proof, changed paths, exact net reduction, and ending
work state remain for the executing Simplify Code run.

## Negative

**Decision:** No safe simplification.

The proposed assertion describes the private helper's branch layout but cannot
detect a change in caller-visible output. It therefore supplies no
caller-facing proof boundary for the branch deletion. Creating a public hook
would add a contract and requires an interface or proof-seam decision outside
this bounded simplification.

**State:** Read-only decision. No production file, test, public hook, index,
unrelated work, installed state, or external state was changed. Starting ref,
status, staged shape, and ending work state were not supplied.

**Return:** No cut. Bounded region: the private parser branch and the proposed
assertion. Rejected candidate: branch deletion, because the available
assertion cannot distinguish changed caller output. Exact proof gap: no
trusted caller-facing observation of the preserved behavior. Residual owner:
Codebase Design if a new interface or proof seam is required; otherwise the
repository owner must supply an existing caller-facing seam before a new
Simplify Code run.
