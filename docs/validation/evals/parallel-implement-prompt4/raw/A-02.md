# A-02 — Family A simulation capture

## Simulation boundary

This is a no-side-effect simulation. No tracker, Git, worktree, remote,
installation, publication, cleanup, or other destructive action was performed.
The only permitted write is this capture.

## Worker-visible fact index

The citations below refer only to the supplied runtime package and the allowed
fixture fields.

- **AUTH** — fixture `authority`: simulate only; perform no real tracker, Git,
  worktree, remote, installation, publication, or destructive mutations.
- **MUT** — fixture `mutation_boundary`: read the exact runtime and fixture and
  write only this disposable capture.
- **OUT** — fixture `requested_output`: report ordered gates, allowed simulated
  mutations, blockers/routes, progression evidence, overlays, and terminal or
  resumable state, citing supplied facts for every decision.
- **A0** — family A `source_trace`: frozen parent `P-41` and child facts A–D are
  authoritative.
- **A1** — family A request: explicit top-level delivery of `P-41` through its
  complete, non-empty, exhaustive Ready-for-agent graph.
- **A2** — family A setup: tracker, label, domain, engineering, Git, and proof
  surfaces exist and are compatible.
- **A3** — graph packet: A, B, C, and D are Ready-for-agent and have complete
  profiles and matrices; D depends on C. Their declared write/proof pairs are
  A=`core/invariant`+`exclusive-db`,
  B=`core/invariant`+`exclusive-db`, C=`ui/c`+`fixture-c`, and
  D=`docs/d`+`fixture-d`.
- **A4** — observations: A and B share a semantic invariant, overlapping
  production writes, and `exclusive-db`; C is independently bounded from both;
  D is bounded but cannot land before C.
- **A5** — observations: A's claim reads back as owned by another actor; B's
  claim reads back exactly.
- **A6** — observation: B's preflight resolves a Python project origin outside
  its recorded worktree.
- **A7** — observations: C and D have exact claims and ready isolated-lane
  packets; content-valid worker packets for C and D arrive out of dependency
  order.
- **A8** — observation: C returns a bounded, clean, proved commit from the
  frozen base.
- **A9** — observation: D returns a bounded, proved commit whose base predates
  the accepted C landing.
- **A10** — observation: attempting to land C enters a merge conflict and
  preserves partial Git operation state.
- **R1** — `skills/experimental/parallel-implement/SKILL.md`, Admission and
  Trace: only the root may admit an explicitly requested parent-backed,
  exhaustive, non-empty Ready-for-agent graph; missing setup routes to
  `$repo-bootstrap`, and defective graph/readiness/profile/matrix information
  routes as one repair packet to `$to-tickets`.
- **R2** — `SKILL.md`, Trace and
  `references/RUN-LEDGER.md`: `events.jsonl` is the sole authority; validate and
  freeze scope before mutation, and reconcile durable state after interaction
  or resume. Missing state is not completed state.
- **R3** — `SKILL.md`, Select: derive the dependency-ready set, qualify every
  pair across semantic ownership, production writes, proof seams/resources,
  ordering, and serial tripwires, and dispatch concurrently only when all
  dimensions are independently bounded.
- **R4** — `SKILL.md`, Open and
  `references/CODEX-WORKTREE-LAUNCH.md`: claim then read the claim back; dispatch
  only from exact `ok: true, state: ready` lane evidence proving containment,
  base, clean status, actor, checkout/index/object writes, startup proof,
  lane-local Python provenance, temp roots, and cleanup.
- **R5** — `references/CODEX-WORKTREE-LAUNCH.md`, Python provenance: every
  concrete project origin or namespace location must resolve under the lane;
  a preflight failure preserves the lane and may be retried only after its
  observed cause changes.
- **R6** — `SKILL.md`, Open/Drain and
  `references/WORKER-BRIEF.md`: a worker owns only its dispatched item; a return
  is accepted only when scope, acceptance, proof, commit, final state, skipped
  checks, risk, and next need are fully accounted for.
- **R7** — `SKILL.md`, Drain and
  `references/INTEGRATOR-BRIEF.md`: land accepted commits one at a time; inspect
  actual diff, stale-base overlap, conflicts, and proof; on conflict stop,
  preserve partial state, report operation/status/unmerged paths/worker commit/
  current HEAD/landing mode/authority, and route `$resolving-merge-conflicts`.
- **R8** — `SKILL.md`, Select/Lock and `references/RUN-LEDGER.md`:
  `landed-awaiting-lock` is campaign-scoped and dependency-enabling only while
  its accepted landing remains in current integration history with valid
  proof; it never closes a tracker item, and rollback/invalidation/failed proof
  removes it.
- **R9** — `SKILL.md`, Review/Lock: Review requires a drained graph, idle
  implementation actors, clean immutable current integration HEAD, and final
  proof. Lock additionally requires accepted review of that same HEAD, then
  child-first verified tracker mutations, parent closeout, released claims,
  and safe lanes.
- **R10** — `SKILL.md`, Lock and `references/RUN-LEDGER.md`, Checkpoint:
  nonterminal returns preserve accepted/unrelated state, halt unsafe progress,
  account for actors/claims/lanes, invalidate unsafe overlays, and leave
  incomplete items open. A runtime-contract-3 checkpoint requires idle actors,
  a non-empty repository-backed current HEAD, clean/preserved integration,
  safe lanes, continuation/frontier/blockers, tracker/remote observations, and
  complete retained-or-released claim accounting with read-back.

The supplied fixture `common` value is null and adds no facts.

## Ordered gate decisions

1. **Admission — pass.** `P-41` is an explicit top-level parent delivery
   request with a complete, non-empty, exhaustive Ready-for-agent graph. This
   is neither delegated invocation, a single-item delivery, nor graph shaping,
   so do not route to `$implement` or reject as delegated. **Evidence:** A1,
   R1.

2. **Setup — pass.** Do not route to `$repo-bootstrap`: every required setup
   surface is stated compatible. **Evidence:** A2, R1.

3. **Trace packet — pass and freeze before any simulated campaign mutation.**
   All four children have readiness, complete execution profiles, and complete
   matrices; record `P-41`, A–D, the C→D dependency, acceptance/proof labels,
   write scopes, and proof resources in one canonical simulated scope event.
   No owner judgment needs reconstruction and no `$to-tickets` repair packet is
   justified by the supplied packet. **Evidence:** A0, A3, R1, R2.

4. **Initial dependency frontier — A, B, and C; D blocked by C.** D's
   Ready-for-agent label and ready-lane evidence do not erase its execution
   dependency. **Evidence:** A3, A4, R3.

5. **Concurrency qualification — never dispatch A with B.** Their semantic
   ownership, production writes, and scarce proof resource all overlap. C may
   run beside at most one of them because C is independently bounded. A safe
   first simulated selection is A+C; after A's failed claim, rederive B+C.
   D remains outside the dispatch frontier. **Evidence:** A3, A4, R3.

6. **A claim gate — fail before lane creation or dispatch.** The read-back
   names another actor, so this root has no claim authority. Preserve the
   external ownership observation, make no competing claim mutation, and leave
   A open. **Exact blocker:** `A claim owner != this campaign actor`.
   **Route:** reconcile with the tracker/owner and retry only after the claim is
   released or reassigned and an exact read-back proves this actor's ownership.
   **Evidence:** A5, R4.

7. **B claim gate — pass; B lane gate — fail.** The claim read-back may be
   recorded, but the external Python origin means the packet is not
   dispatchable `ok: true, state: ready` evidence. Preserve the created lane;
   do not dispatch B and do not treat skipped provenance as a pass.
   **Exact blocker:** `B project origin escapes B's recorded worktree`.
   **Route:** correct the lane/import-root/environment cause, then rerun
   preflight in the same preserved lane only if base, actor, authority, and
   assignment remain current; otherwise open a fresh lane. Progress requires
   every project origin beneath B's worktree plus the complete ready packet
   listed in R4. **Evidence:** A5, A6, R4, R5.

8. **C claim/lane gate — pass; C dispatch — allowed.** C's exact claim and
   isolated ready packet satisfy the stated read-back gates, and C is
   dependency-ready and independently bounded. The simulated dispatch packet
   must still bind the frozen base, actor, worktree, acceptance `A accepted` is
   not C's acceptance; C must instead carry `C accepted`, proof `proof-C`,
   scope `ui/c`, and its complete matrix. **Evidence:** A3, A4, A7, R4, R6.

9. **D claim/lane artifacts — account for them, but withhold dispatch.** Exact
   claim and lane readiness prove isolation only; they do not prove dependency
   readiness. Therefore the stipulated content-valid D return cannot be
   accepted into the canonical campaign as a completed dispatched item before
   C has a valid landing overlay. Preserve its commit as an unlanded packet and
   treat the premature return/history as `needs-feedback`, not completion.
   Release D's claim with read-back and make its lane safe unless the campaign
   can retain both under a fully evidenced recovery packet. **Exact blocker:**
   `D depends_on C, but C has no valid landed-awaiting-lock overlay`.
   **Route:** first land and prove C; then reselect D and issue a current
   dispatch/continuation from reconciled authority. **Evidence:** A3, A4, A7,
   R2, R3, R4, R6, R8.

10. **C return gate — accept the typed packet, not a landing.** C's bounded,
    clean, proved commit from the frozen base satisfies the supplied acceptance
    facts for a simulated acceptance, provided the returned packet accounts
    for all R6 fields. Acceptance alone creates no dependency overlay and does
    not close C. **Evidence:** A3, A8, R6, R8.

11. **D return/stale-base gate — preserve, do not land.** Even after a future C
    landing opens D, D's proof from a predecessor base cannot by itself prove
    the C+D state. Before any landing, inspect D's actual diff and ancestry
    against the then-current integration HEAD, check dependency-sensitive
    overlap and conflicts, and rerun focused/recombined proof. If its old lane,
    actor, or authority is no longer current, use a fresh lane from the recorded
    C integration HEAD. **Exact blockers now:** missing C overlay, stale base,
    and no current-head combined proof. **Evidence:** A3, A9, R3, R6, R7, R8.

12. **C serial landing gate — stop on the preserved merge conflict.** Do not
    record a `land` event, do not claim a new integration HEAD, do not attempt D,
    and do not abort or overwrite the partial operation. **Route:** invoke
    `$resolving-merge-conflicts` from the preserved operation after capturing
    the exact operation, status, unmerged paths, C worker commit, current HEAD,
    landing mode, and landing authority. Progress requires a clean successor
    HEAD containing the accepted C change, an inspected landed diff, focused
    and recombined proof, and a canonical successful landing record.
    **Evidence:** A10, R7.

13. **Graph-drained/Review/Lock — closed.** A is unclaimed, B is not
    dispatchable, C is conflicted rather than landed, and D is dependency- and
    stale-base-blocked. Therefore the graph is not drained, no immutable review
    candidate exists, no tracker closeout is allowed, and `complete` is false.
    **Evidence:** A5, A6, A9, A10, R8, R9.

## Allowed simulated mutations and forbidden effects

The only allowed campaign mutations are hypothetical ledger/tracker/lane
decisions; AUTH and MUT prohibit executing any of them.

| Order | Allowed simulated mutation | Required read-back/evidence | Result |
| --- | --- | --- | --- |
| 1 | Validate then record one canonical scope packet for P-41 and A–D | A0–A4; canonical event-stream identity per R2 | Scope frozen |
| 2 | Attempt A claim | Tracker owner read-back | Rejected; no campaign claim (A5, R4) |
| 3 | Record exact B claim | Claim token/owner/time read-back | Claim valid but no dispatch (A5–A6, R4–R5) |
| 4 | Record C exact claim and ready lane; simulate C dispatch | Full `ok:true,state:ready` packet and bounded brief | C active (A3, A7, R4, R6) |
| 5 | Account for D's observed claim/lane without authorizing work | Exact claim/lane packet plus C dependency state | Preserve/release; no dispatch authority (A3, A7, R3–R4) |
| 6 | Record C worker handoff and acceptance | Complete typed return, `C accepted`, `proof-C`, clean commit | Accepted-unlanded (A3, A8, R6) |
| 7 | Record D handoff only as preserved/needs-feedback evidence | Commit/base identity and no fabricated dispatch | Unlanded; blocked (A7, A9, R6–R8) |
| 8 | Simulate one C landing attempt | Landing authority and integration pre-state | Conflict preserved; no `land` event (A10, R7) |
| 9 | Prepare a blocked checkpoint, but record it only after all R10 evidence exists | Exact HEAD, actors, claims, tracker/remote, safe lanes, retained state | Not yet recordable from supplied facts (R10) |

No simulated result may be presented as a real tracker edit, claim, worktree,
commit, conflict operation, ledger append, cleanup, push, or publication
because AUTH and MUT deny those effects.

## Overlay transitions

| Transition | Overlay state | Dependency effect | Citation |
| --- | --- | --- | --- |
| Scope/claim/lane ready | none | D remains blocked by C | A3, A7, R3, R8 |
| C packet accepted | none | Acceptance is not landing; D remains blocked | A8, R6, R8 |
| C landing enters conflict | none | No successful C landing identity exists; D remains blocked | A10, R7, R8 |
| Future conflict resolution + successful C landing + valid current-HEAD proof | `C = landed-awaiting-lock` | D may enter the dependency-ready frontier | A3, R7, R8 |
| C rollback, invalidation, ancestry loss, or failed proof | C overlay removed | D reblocked; any dependent proof invalidated | R8 |
| Future safe D landing with recombined proof | `D = landed-awaiting-lock` | D is implemented but still tracker-open | R7, R8 |
| Accepted review and verified Lock | overlays replaced by verified child closeouts | Children close only child-first; parent follows | R9 |

## Current terminal/resumable disposition

**State: `blocked`, nonterminal, resumable; not yet a valid recorded
checkpoint.** Retain C's accepted commit and preserved conflict operation,
retain D's unlanded commit as stale/needs-feedback evidence, keep A and B open,
and admit no dependency overlay. The immediate recovery route is the preserved
C conflict through `$resolving-merge-conflicts`; A separately needs claim
reconciliation, B needs lane-local provenance, and D needs a successful C
overlay plus current-head stale-base proof. **Evidence:** A5–A10, R5–R10.

Before a canonical checkpoint or resume is valid, the root still needs facts
not supplied here: a repository-backed non-empty current HEAD; idle actor
read-back; exact retained/released claim packets (including release read-backs);
safe dispositions for every lane; clean or explicitly preserved integration
state; the next frontier and blocker list; tracker and remote observations; and
the continuation owner. On resume, record `resume`, then reconcile Git,
worktrees, actors, claims/tracker, remote, retained commits, and the partial
conflict before reopening any dispatch, landing, review, or lock authority.
**Evidence:** R2, R10.
