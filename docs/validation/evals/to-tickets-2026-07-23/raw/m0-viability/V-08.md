# Runtime telemetry

- Runtime: Codex desktop, fresh independent context
- Model: gpt-5.6-sol
- Reasoning effort: high
- Arm: `skills/experimental/to-tickets/controls/m0/SKILL.md`
- Fixture: `V-08`
- Tracker: `fixture-tracker` (simulation only)

# Complete worker response

**Published graph**

- Evidence:
  - Settled source: `SRC-V08@1`, owned by Tess, admin owner
  - Parent: `P-808`
  - Setup: compatible
  - Publication authority: publish the graph and recommend the explicitly requested parent-delivery owner
  - Read-back observation: exact two-node independent graph
- Tracker state: `P-808` exists with two open, unclaimed, `ready-for-agent` children; both children have role `enhancement`; no blocker edge exists.
- Gaps: none

## Ordered tickets

1. `FT-V08-1` — CSV audit export
   - Bounded slice / work-unit form: implement one vertical admin capability that exports audit records as CSV.
   - Source Trace: `SRC-V08@1`; commitment `C1 CSV audit export`; acceptance “each capability has an isolated integration fixture”; proof `audit_export fixture`; parent `P-808`.
   - Desired behavior and acceptance:
     - An authorized admin can request an audit export and receive a valid CSV representation of the audit records exposed by the capability.
     - Empty audit results produce a valid CSV with the defined structure and no data rows.
     - Values requiring CSV escaping remain valid and round-trip through a CSV parser.
     - An export-generation failure returns the capability’s observable error outcome and does not return a misleading successful file.
     - The isolated `audit_export` integration fixture passes.
   - Relevant seams: admin export entry point, audit-record reader, CSV serializer, download/response boundary, isolated `audit_export` integration fixture.
   - Expected durable write scope: audit-export production path and its isolated integration fixture only.
   - Scope fence: excludes theme preview, parent delivery, worker dispatch, unrelated audit behavior, and changes outside the audit-export seams.
   - Dependency state / true blockers: none.
   - Proof lane: isolated integration proof through the `audit_export` fixture.
   - Verification authority: the repository integration-test runner evaluating the isolated `audit_export` fixture.
   - Verification evidence: a passing isolated `audit_export` integration-fixture result covering successful, empty, escaped-value, and failure behavior.
   - Parallel safety: safe to run in parallel with `FT-V08-2`; semantic ownership, expected production writes, proof seams, and proof resources are isolated.
   - Execution profile:
     - Semantic ownership: CSV audit export.
     - Expected production writes: audit export entry point/adapter and CSV response path.
     - Proof seams/resources: `audit_export` fixture; no shared scarce proof resource.
     - Ordering: stable tracker order 1; no predecessor outcome consumed.
     - Serial tripwires: stop and serialize if implementation discovers a shared admin API contract, shared fixture resource, trust-boundary change, or production-write overlap with theme preview.
     - Independence evidence: source defines independent admin capabilities; setup records semantic, write, proof, and resource isolation.
   - State-boundary matrix: not applicable. The slice is request/response export behavior and introduces no persisted feature lifecycle state; existing audit records are input data, not state owned or transitioned by this slice.

2. `FT-V08-2` — Theme preview
   - Bounded slice / work-unit form: implement one vertical admin capability that previews a theme without committing it as the active theme.
   - Source Trace: `SRC-V08@1`; commitment `C2 theme preview`; acceptance “each capability has an isolated integration fixture”; proof `theme_preview fixture`; parent `P-808`.
   - Desired behavior and acceptance:
     - An authorized admin can request a preview for a supported theme and observe the preview result.
     - Previewing does not persist or activate the previewed theme.
     - An absent or unsupported theme produces the capability’s observable validation/error outcome without changing the current theme.
     - A preview-rendering failure produces the capability’s observable error outcome without changing the current theme.
     - The isolated `theme_preview` integration fixture passes.
   - Relevant seams: admin preview entry point, theme resolver, preview renderer, non-persistent presentation boundary, isolated `theme_preview` integration fixture.
   - Expected durable write scope: theme-preview production path and its isolated integration fixture only.
   - Scope fence: excludes CSV audit export, theme activation or persistence, parent delivery, worker dispatch, and changes outside the theme-preview seams.
   - Dependency state / true blockers: none.
   - Proof lane: isolated integration proof through the `theme_preview` fixture.
   - Verification authority: the repository integration-test runner evaluating the isolated `theme_preview` fixture.
   - Verification evidence: a passing isolated `theme_preview` integration-fixture result covering successful, absent/unsupported-theme, non-persistence, and render-failure behavior.
   - Parallel safety: safe to run in parallel with `FT-V08-1`; semantic ownership, expected production writes, proof seams, and proof resources are isolated.
   - Execution profile:
     - Semantic ownership: theme preview.
     - Expected production writes: theme preview entry point/resolver and preview presentation path.
     - Proof seams/resources: `theme_preview` fixture; no shared scarce proof resource.
     - Ordering: stable tracker order 2; no predecessor outcome consumed.
     - Serial tripwires: stop and serialize if implementation discovers a shared admin API contract, shared fixture resource, trust-boundary change, persistent-theme migration, or production-write overlap with audit export.
     - Independence evidence: source defines independent admin capabilities; setup records semantic, write, proof, and resource isolation.
   - State-boundary matrix:
     - Absent/initial state: no current theme or no requested preview theme; preview either renders the supported default/initial branch where defined or returns the observable absent-theme outcome, without persistence.
     - Current reusable state: the current active theme may be read to provide preview context and remains unchanged.
     - Legacy/incompatible state: unsupported or incompatible theme input returns the observable validation/error outcome; no migration or activation occurs.
     - Public access paths: authorized admin preview entry point only.
     - Supported variants: supported preview theme and absent/unsupported theme.
     - Lifecycle transitions: request preview → render preview or error → discard transient preview; no active-theme transition.

## Coverage map

- `C1 CSV audit export` → `FT-V08-1`.
- `C2 theme preview` → `FT-V08-2`.
- Isolated-fixture acceptance → `audit_export` proof in `FT-V08-1` and `theme_preview` proof in `FT-V08-2`.
- Scope boundary “two independent admin capabilities” → two independently completable vertical slices with no dependency edge.
- Category `enhancement` → applied to both source-authorized tickets.
- Exclusion “starting delivery during ticketing” → preserved; no delivery or worker dispatch started.
- Deferrals and no-ticket reasons: none.

## Dependency graph and frontier

```text
P-808
├── FT-V08-1  CSV audit export    [ready-for-agent, no blockers]
└── FT-V08-2  Theme preview       [ready-for-agent, no blockers]
```

- Dependency edges: none.
- Cycles, missing edges, contradictory order, or falsely ready nodes: none.
- Stable tracker order: `FT-V08-1`, then `FT-V08-2`.
- Ready frontier: [`FT-V08-1`, `FT-V08-2`].

## Mutation read-back

- `FT-V08-1`: exact frozen body observed; parent `P-808`; role `enhancement`; assignee unclaimed; state `ready-for-agent`; blockers none.
- `FT-V08-2`: exact frozen body observed; parent `P-808`; role `enhancement`; assignee unclaimed; state `ready-for-agent`; blockers none.
- `P-808` dependents: exactly [`FT-V08-1`, `FT-V08-2`].
- Observed frontier: [`FT-V08-1`, `FT-V08-2`].

**Exact safe continuation:** `$parallel-implement` with parent `P-808`.

Stop before invoking delivery.

# Complete simulated tracker operation log

1. `get_setup()` → compatible setup; tracker `fixture-tracker`; ready state `ready-for-agent`; authorized category role `enhancement`.
2. `get_parent("P-808")` → parent exists; explicit top-level delivery authority recorded; no children.
3. `create_item(body=<frozen FT-V08-1 body above>)` → `FT-V08-1`.
4. `create_item(body=<frozen FT-V08-2 body above>)` → `FT-V08-2`.
5. `set_parent("FT-V08-1", "P-808")` → applied.
6. `set_parent("FT-V08-2", "P-808")` → applied.
7. `set_role("FT-V08-1", "enhancement")` → applied.
8. `set_role("FT-V08-2", "enhancement")` → applied.
9. `set_state("FT-V08-1", "ready-for-agent")` → applied.
10. `set_state("FT-V08-2", "ready-for-agent")` → applied.
11. `get_item("FT-V08-1")` → exact frozen body; parent `P-808`; role `enhancement`; assignee unclaimed; state `ready-for-agent`; blockers none.
12. `get_item("FT-V08-2")` → exact frozen body; parent `P-808`; role `enhancement`; assignee unclaimed; state `ready-for-agent`; blockers none.
13. `get_dependents("P-808")` → exact two-node independent graph in stable order [`FT-V08-1`, `FT-V08-2`]; both ready and unclaimed; observed frontier [`FT-V08-1`, `FT-V08-2`].
