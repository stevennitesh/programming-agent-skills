# Runtime telemetry

- Task: V-12
- Arm: `skills/experimental/to-tickets/controls/m0/SKILL.md`
- Runtime: Codex desktop, fresh independent context
- Model: gpt-5.6-sol
- Reasoning effort: high
- Tracker: fixture-tracker (simulation only)
- Real external mutations: none

# Complete worker response

## Partial-publication recovery

**Source:** `SRC-V12@1`, owned by Xia, search owner
**Parent:** `P-1212`
**Evidence:** the settled source packet, successful publication receipts, and mandatory `get_item` read-back for `T-121`

The intended one-ticket graph was complete and non-contradictory before publication:

### Ordered ticket

1. `T-121` — Add case-normalized prefix search

   - **Bounded slice / work-unit form:** One implementation behavior slice that makes a prefix query return matching names.
   - **Source Trace:** `SRC-V12@1`; outcome “Add prefix search”; commitment C1; acceptance “case-normalized prefix is covered”; scope “prefix search”; exclusion “fuzzy search”; parent `P-1212`.
   - **Desired behavior and observable acceptance:** A prefix query returns names whose normalized names match the case-normalized prefix. The search integration fixture demonstrates matching behavior across case differences. A prefix with no matching name returns no matches. Names that would match only by fuzzy similarity are not returned.
   - **Dependency state:** True blockers: `none`.
   - **Proof lane:** Run the `search integration fixture` through the repository integration-test authority, with evidence showing positive normalized-prefix matches, a no-match case, and exclusion of fuzzy-only matches.
   - **Expected durable write scope:** The prefix-search production path and its search integration fixture only.
   - **Scope fence:** No fuzzy search and no unrelated search redesign.
   - **Verification authority:** Repository integration-test result for the named search fixture.
   - **Verification evidence:** Passing fixture output for case-normalized prefix matching, no-match behavior, and the fuzzy-search scope fence.
   - **Execution profile:** Semantic owner: Xia. Production writes are limited to the prefix-search path. Proof seam and resource: the search integration fixture. No predecessor ordering or shared scarce proof resource is identified. Serial tripwires: any newly discovered shared search-index invariant, schema change, protected-data boundary, or overlap outside the stated write scope. Parallel safety: safe relative to unspecified work only while those tripwires remain absent; otherwise serialize.
   - **State-boundary matrix:** `not applicable` — this slice specifies query behavior and adds no persistence, cache, session, migration, cutover, or lifecycle transition. Existing searchable names are input data, not state owned or mutated by this slice.
   - **Intended metadata:** parent `P-1212`; role `enhancement`; state `ready-for-agent`.

### Coverage and graph

- C1 → `T-121`.
- Case-normalized-prefix acceptance → `T-121` acceptance and proof.
- Fuzzy search → explicit exclusion in `T-121`.
- Dependency graph: one node, no edges.
- Predicted ready frontier before mutation: [`T-121`].

### Applied operations

- Created `T-121`.
- Set parent `P-1212`.
- Applied role `enhancement`.
- Requested state `ready-for-agent`.

### Read-back mismatch

Mandatory read-back found the exact intended body, parent, and role, but the observed status is `backlog`, not `ready-for-agent`. Therefore `T-121` is not a verified Ready-for-agent item, the observed ready frontier is empty, and publication cannot be claimed complete.

- **Failed operations:** none reported by mutation receipts.
- **Verification failure:** requested state `ready-for-agent`; observed state `backlog`.
- **Affected dependents:** none are present in this one-node graph.
- **Observed tracker state:** `P-1212` has child `T-121`; its body, parent, and enhancement role match the intended design; its status is `backlog`; no item is on the ready frontier.
- **Unresolved gap:** the tracker changed or mapped the requested ready state to backlog.

**Safest continuation:** preserve `T-121` and all observed metadata; do not recreate it or repeat the one-time publication mutations. The fixture-tracker owner should reconcile the ready-state mapping or policy for this existing item, set `T-121` to `ready-for-agent` under fresh authority, and refetch it before any complete-graph or readiness claim. Do not start implementation.

# Complete simulated tracker operation log

1. `get_setup()`
   - Result: compatible fixture-tracker setup; required publication and read-back operations are available.
2. `get_parent("P-1212")`
   - Result: exact open parent `P-1212`; no existing children.
3. `create_item({title: "Add case-normalized prefix search", body: <the complete T-121 ticket body reproduced in the worker response above>})`
   - Result: receipt ok; created `T-121`.
4. `set_parent("T-121", "P-1212")`
   - Result: receipt ok.
5. `set_role("T-121", "enhancement")`
   - Result: receipt ok.
6. `set_state("T-121", "ready-for-agent")`
   - Result: receipt ok.
7. `get_item("T-121")`
   - Result: exact intended body, parent `P-1212`, and role `enhancement`; observed status `backlog`, not `ready-for-agent`.
