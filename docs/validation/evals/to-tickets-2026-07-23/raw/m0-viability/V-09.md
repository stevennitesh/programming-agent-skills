# Runtime telemetry

- Runtime: Codex desktop, fresh independent context
- Model: `gpt-5.6-sol`
- Reasoning effort: high
- Arm: `skills/experimental/to-tickets/controls/m0/SKILL.md`
- Fixture: `V-09`
- Tracker: `fixture-tracker` (simulation only)
- Repository: `E:\GitHub\code\programming-agent-skills`
- External mutations: none

# Complete worker response

Published graph

Source and parent

- Source: `SRC-V09@1`
- Source owner: Uma, settings owner
- Parent: `P-909`
- Published items, in stable tracker order: `FT-V09-1`, `FT-V09-2`, `FT-V09-3`

Coverage

| Source commitment or boundary | Disposition |
| --- | --- |
| C1 compact mode | `FT-V09-1` |
| C2 timezone display | `FT-V09-2` |
| C3 digest opt-in | `FT-V09-3` |
| Each toggle has isolated proof | Required by the proof lane and acceptance of its corresponding item |
| Scope: settings toggles | Enforced by every item’s scope fence |
| Exclusion: parent-delivery campaign | No delivery campaign was started or recommended |

Ordered tickets

1. `FT-V09-1` — Add the compact mode setting toggle

   - Bounded slice/work-unit form: independently completable vertical behavior slice for the compact mode setting toggle.
   - Source Trace: `SRC-V09@1`, commitment C1; owned by Uma; parent `P-909`.
   - Desired behavior: the settings surface exposes compact mode as a two-state control, and a successful user change is durably reflected by the compact mode setting without changing the timezone-display or digest-opt-in settings.
   - Acceptance:
     - The compact mode control can be changed between its supported on and off states.
     - After a successful change, the control and the compact-mode behavior read the resulting durable state consistently.
     - The isolated compact-mode settings fixture passes in both directions.
     - A failed or rejected durable write does not report a successful state change and does not alter either of the other two settings.
   - Seams: compact mode settings control, compact mode setting read/write boundary, and compact-mode behavior consumer.
   - Expected durable write scope: only the durable value representing compact mode; exact storage mechanism remains with the implementation owner.
   - Scope fence: no timezone-display work, digest-opt-in work, unrelated settings redesign, parent delivery, or implementation-technique mandate.
   - Dependency state: blockers `none`; stable order 1.
   - Proof lane: the isolated compact mode settings fixture, exercising both state transitions and the write-failure path.
   - Verification authority: the isolated settings fixture is the acceptance authority for this source slice.
   - Verification evidence: recorded passing results for the isolated fixture covering on, off, and rejected-write behavior.
   - Parallel safety: safe to execute independently because the source declares the toggles independent, its durable value and proof fixture are slice-specific, and it consumes no predecessor outcome. Any newly discovered shared durable key, shared exclusive fixture, or cross-toggle behavior is a serial tripwire and must stop concurrent work before a write.
   - Execution profile: semantic owner Uma; production writes limited to the compact-mode control, its setting boundary, and its behavior consumer; proof seam is the compact-mode fixture; no known scarce proof resource; no blocker ordering; serial tripwires are a shared durable key, exclusive shared fixture, or behavior coupling discovered during implementation.
   - State-boundary matrix:
     - Absent/initial: preserve the existing product default; the source does not authorize changing that default.
     - Current reusable: retain and render any valid current on/off value.
     - Legacy/incompatible: no migration or legacy reinterpretation is authorized; discovery of incompatible state stops the slice for a source decision.
     - Public access path: the existing settings surface and the compact-mode behavior that consumes the setting.
     - Supported variants: on and off.
     - Lifecycle transitions: off to on and on to off; a failed write remains at the prior durable state.

2. `FT-V09-2` — Add the timezone display setting toggle

   - Bounded slice/work-unit form: independently completable vertical behavior slice for the timezone display setting toggle.
   - Source Trace: `SRC-V09@1`, commitment C2; owned by Uma; parent `P-909`.
   - Desired behavior: the settings surface exposes timezone display as a two-state control, and a successful user change is durably reflected by whether the existing timezone display is enabled, without changing compact mode or digest opt-in.
   - Acceptance:
     - The timezone display control can be changed between its supported on and off states.
     - After a successful change, the control and the timezone-display consumer read the resulting durable state consistently.
     - The isolated timezone-display settings fixture passes in both directions.
     - A failed or rejected durable write does not report a successful state change and does not alter either of the other two settings.
   - Seams: timezone-display settings control, timezone-display setting read/write boundary, and existing timezone-display consumer. Timezone choice and formatting are outside this slice.
   - Expected durable write scope: only the durable value representing timezone-display enablement; exact storage mechanism remains with the implementation owner.
   - Scope fence: no compact-mode work, digest-opt-in work, timezone selection or formatting decisions, unrelated settings redesign, parent delivery, or implementation-technique mandate.
   - Dependency state: blockers `none`; stable order 2.
   - Proof lane: the isolated timezone-display settings fixture, exercising both state transitions and the write-failure path.
   - Verification authority: the isolated settings fixture is the acceptance authority for this source slice.
   - Verification evidence: recorded passing results for the isolated fixture covering on, off, and rejected-write behavior.
   - Parallel safety: safe to execute independently because the source declares the toggles independent, its durable value and proof fixture are slice-specific, and it consumes no predecessor outcome. Any newly discovered shared durable key, shared exclusive fixture, or cross-toggle behavior is a serial tripwire and must stop concurrent work before a write.
   - Execution profile: semantic owner Uma; production writes limited to the timezone-display control, its setting boundary, and its existing display consumer; proof seam is the timezone-display fixture; no known scarce proof resource; no blocker ordering; serial tripwires are a shared durable key, exclusive shared fixture, or behavior coupling discovered during implementation.
   - State-boundary matrix:
     - Absent/initial: preserve the existing product default; the source does not authorize changing that default.
     - Current reusable: retain and render any valid current on/off value.
     - Legacy/incompatible: no migration or legacy reinterpretation is authorized; discovery of incompatible state stops the slice for a source decision.
     - Public access path: the existing settings surface and the existing timezone-display consumer.
     - Supported variants: on and off.
     - Lifecycle transitions: off to on and on to off; a failed write remains at the prior durable state.

3. `FT-V09-3` — Add the digest opt-in setting toggle

   - Bounded slice/work-unit form: independently completable vertical behavior slice for the digest opt-in setting toggle.
   - Source Trace: `SRC-V09@1`, commitment C3; owned by Uma; parent `P-909`.
   - Desired behavior: the settings surface exposes digest opt-in as a two-state control, and a successful user change durably records the opt-in state without changing compact mode or timezone display.
   - Acceptance:
     - The digest opt-in control can be changed between its supported opted-in and not-opted-in states.
     - After a successful change, the control and the digest preference reader read the resulting durable state consistently.
     - The isolated digest-opt-in settings fixture passes in both directions.
     - A failed or rejected durable write does not report a successful state change and does not alter either of the other two settings.
   - Seams: digest-opt-in settings control, digest preference read/write boundary, and digest preference reader. Digest generation or delivery is outside this slice.
   - Expected durable write scope: only the durable value representing digest opt-in; exact storage mechanism remains with the implementation owner.
   - Scope fence: no compact-mode work, timezone-display work, digest generation or delivery work, unrelated settings redesign, parent delivery, or implementation-technique mandate.
   - Dependency state: blockers `none`; stable order 3.
   - Proof lane: the isolated digest-opt-in settings fixture, exercising both state transitions and the write-failure path.
   - Verification authority: the isolated settings fixture is the acceptance authority for this source slice.
   - Verification evidence: recorded passing results for the isolated fixture covering opt-in, opt-out, and rejected-write behavior.
   - Parallel safety: safe to execute independently because the source declares the toggles independent, its durable value and proof fixture are slice-specific, and it consumes no predecessor outcome. Any newly discovered shared durable key, shared exclusive fixture, or cross-toggle behavior is a serial tripwire and must stop concurrent work before a write.
   - Execution profile: semantic owner Uma; production writes limited to the digest-opt-in control, its preference boundary, and its preference reader; proof seam is the digest-opt-in fixture; no known scarce proof resource; no blocker ordering; serial tripwires are a shared durable key, exclusive shared fixture, or behavior coupling discovered during implementation.
   - State-boundary matrix:
     - Absent/initial: preserve the existing product default; the source does not authorize changing that default.
     - Current reusable: retain and render any valid current opted-in/not-opted-in value.
     - Legacy/incompatible: no migration or legacy reinterpretation is authorized; discovery of incompatible state stops the slice for a source decision.
     - Public access path: the existing settings surface and the digest preference reader.
     - Supported variants: opted in and not opted in.
     - Lifecycle transitions: not opted in to opted in and opted in to not opted in; a failed write remains at the prior durable state.

Dependency graph and frontier

- Blocker edges: none.
- Predicted and observed ready frontier, in tracker order: `FT-V09-1`, `FT-V09-2`, `FT-V09-3`.
- All three items are open, unclaimed, `ready-for-agent`, and independently ready. Tracker order is not a blocker.

Mutation read-back

- `FT-V09-1`, `FT-V09-2`, and `FT-V09-3` were each read back with the exact frozen body, parent `P-909`, role `enhancement`, no assignee, state `ready-for-agent`, and no blocker edges.
- The resulting graph and frontier match the frozen publication design.
- No failed, unknown, ambiguous, or mismatched operation was observed.

Safe next owner: `none`.

No top-level delivery of `P-909` was requested, and this publication has no serial predicate requiring a single-item delivery handoff. No implementation or worker dispatch was started.

# Complete simulated tracker operation log

1. `get_setup`

   Request:

   ```json
   {}
   ```

   Result:

   ```json
   {
     "tracker": "fixture-tracker",
     "compatible": true,
     "ready_state": "ready-for-agent",
     "category_roles": ["enhancement"]
   }
   ```

2. `get_parent`

   Request:

   ```json
   {
     "id": "P-909"
   }
   ```

   Result:

   ```json
   {
     "id": "P-909",
     "exists": true,
     "source": "SRC-V09@1",
     "children": []
   }
   ```

3. `create_item`

   Request:

   ```json
   {
     "title": "Add the compact mode setting toggle",
     "body": {
       "bounded_slice": "Independently completable vertical behavior slice for the compact mode setting toggle.",
       "work_unit_form": "vertical behavior slice",
       "source_trace": "SRC-V09@1, commitment C1; owner Uma; parent P-909",
       "desired_behavior": "Expose compact mode as a two-state settings control and durably reflect a successful user change without changing timezone-display or digest-opt-in settings.",
       "acceptance": [
         "The control can be changed between supported on and off states.",
         "After a successful change, the control and compact-mode behavior read the resulting durable state consistently.",
         "The isolated compact-mode settings fixture passes in both directions.",
         "A failed or rejected durable write does not report success and does not alter the other two settings."
       ],
       "seams": [
         "compact mode settings control",
         "compact mode setting read/write boundary",
         "compact-mode behavior consumer"
       ],
       "expected_durable_write_scope": "Only the durable value representing compact mode; exact storage mechanism remains with the implementation owner.",
       "scope_fence": "No timezone-display work, digest-opt-in work, unrelated settings redesign, parent delivery, or implementation-technique mandate.",
       "dependency_state": {
         "blockers": [],
         "stable_order": 1
       },
       "proof_lane": "Isolated compact mode settings fixture exercising both transitions and the write-failure path.",
       "verification_authority": "The isolated settings fixture is the acceptance authority for this source slice.",
       "verification_evidence": "Recorded passing results for on, off, and rejected-write behavior.",
       "parallel_safety": "Independent under the source-declared separation, slice-specific durable value, and isolated fixture; a discovered shared durable key, exclusive fixture, or behavior coupling is a stop-before-write serial tripwire.",
       "execution_profile": {
         "semantic_owner": "Uma",
         "expected_production_writes": "Compact-mode control, setting boundary, and behavior consumer only.",
         "proof_seams": ["isolated compact-mode settings fixture"],
         "scarce_proof_resources": [],
         "ordering": "No blocker ordering.",
         "serial_tripwires": [
           "shared durable key",
           "exclusive shared fixture",
           "cross-toggle behavior coupling"
         ]
       },
       "state_boundary_matrix": {
         "absent_or_initial": "Preserve the existing product default; changing it is not authorized.",
         "current_reusable": "Retain and render any valid current on/off value.",
         "legacy_or_incompatible": "No migration or reinterpretation is authorized; incompatible state requires a source decision.",
         "public_access_paths": ["existing settings surface", "compact-mode behavior consumer"],
         "supported_variants": ["on", "off"],
         "lifecycle_transitions": ["off to on", "on to off", "failed write remains at prior durable state"]
       }
     }
   }
   ```

   Result:

   ```json
   {
     "id": "FT-V09-1",
     "created": true
   }
   ```

4. `create_item`

   Request:

   ```json
   {
     "title": "Add the timezone display setting toggle",
     "body": {
       "bounded_slice": "Independently completable vertical behavior slice for the timezone display setting toggle.",
       "work_unit_form": "vertical behavior slice",
       "source_trace": "SRC-V09@1, commitment C2; owner Uma; parent P-909",
       "desired_behavior": "Expose timezone display as a two-state settings control and durably reflect whether the existing timezone display is enabled without changing compact mode or digest opt-in.",
       "acceptance": [
         "The control can be changed between supported on and off states.",
         "After a successful change, the control and timezone-display consumer read the resulting durable state consistently.",
         "The isolated timezone-display settings fixture passes in both directions.",
         "A failed or rejected durable write does not report success and does not alter the other two settings."
       ],
       "seams": [
         "timezone-display settings control",
         "timezone-display setting read/write boundary",
         "existing timezone-display consumer"
       ],
       "expected_durable_write_scope": "Only the durable value representing timezone-display enablement; exact storage mechanism remains with the implementation owner.",
       "scope_fence": "No compact-mode work, digest-opt-in work, timezone selection or formatting decisions, unrelated settings redesign, parent delivery, or implementation-technique mandate.",
       "dependency_state": {
         "blockers": [],
         "stable_order": 2
       },
       "proof_lane": "Isolated timezone-display settings fixture exercising both transitions and the write-failure path.",
       "verification_authority": "The isolated settings fixture is the acceptance authority for this source slice.",
       "verification_evidence": "Recorded passing results for on, off, and rejected-write behavior.",
       "parallel_safety": "Independent under the source-declared separation, slice-specific durable value, and isolated fixture; a discovered shared durable key, exclusive fixture, or behavior coupling is a stop-before-write serial tripwire.",
       "execution_profile": {
         "semantic_owner": "Uma",
         "expected_production_writes": "Timezone-display control, setting boundary, and existing display consumer only.",
         "proof_seams": ["isolated timezone-display settings fixture"],
         "scarce_proof_resources": [],
         "ordering": "No blocker ordering.",
         "serial_tripwires": [
           "shared durable key",
           "exclusive shared fixture",
           "cross-toggle behavior coupling"
         ]
       },
       "state_boundary_matrix": {
         "absent_or_initial": "Preserve the existing product default; changing it is not authorized.",
         "current_reusable": "Retain and render any valid current on/off value.",
         "legacy_or_incompatible": "No migration or reinterpretation is authorized; incompatible state requires a source decision.",
         "public_access_paths": ["existing settings surface", "existing timezone-display consumer"],
         "supported_variants": ["on", "off"],
         "lifecycle_transitions": ["off to on", "on to off", "failed write remains at prior durable state"]
       }
     }
   }
   ```

   Result:

   ```json
   {
     "id": "FT-V09-2",
     "created": true
   }
   ```

5. `create_item`

   Request:

   ```json
   {
     "title": "Add the digest opt-in setting toggle",
     "body": {
       "bounded_slice": "Independently completable vertical behavior slice for the digest opt-in setting toggle.",
       "work_unit_form": "vertical behavior slice",
       "source_trace": "SRC-V09@1, commitment C3; owner Uma; parent P-909",
       "desired_behavior": "Expose digest opt-in as a two-state settings control and durably record a successful user change without changing compact mode or timezone display.",
       "acceptance": [
         "The control can be changed between supported opted-in and not-opted-in states.",
         "After a successful change, the control and digest preference reader read the resulting durable state consistently.",
         "The isolated digest-opt-in settings fixture passes in both directions.",
         "A failed or rejected durable write does not report success and does not alter the other two settings."
       ],
       "seams": [
         "digest-opt-in settings control",
         "digest preference read/write boundary",
         "digest preference reader"
       ],
       "expected_durable_write_scope": "Only the durable value representing digest opt-in; exact storage mechanism remains with the implementation owner.",
       "scope_fence": "No compact-mode work, timezone-display work, digest generation or delivery work, unrelated settings redesign, parent delivery, or implementation-technique mandate.",
       "dependency_state": {
         "blockers": [],
         "stable_order": 3
       },
       "proof_lane": "Isolated digest-opt-in settings fixture exercising both transitions and the write-failure path.",
       "verification_authority": "The isolated settings fixture is the acceptance authority for this source slice.",
       "verification_evidence": "Recorded passing results for opt-in, opt-out, and rejected-write behavior.",
       "parallel_safety": "Independent under the source-declared separation, slice-specific durable value, and isolated fixture; a discovered shared durable key, exclusive fixture, or behavior coupling is a stop-before-write serial tripwire.",
       "execution_profile": {
         "semantic_owner": "Uma",
         "expected_production_writes": "Digest-opt-in control, preference boundary, and preference reader only.",
         "proof_seams": ["isolated digest-opt-in settings fixture"],
         "scarce_proof_resources": [],
         "ordering": "No blocker ordering.",
         "serial_tripwires": [
           "shared durable key",
           "exclusive shared fixture",
           "cross-toggle behavior coupling"
         ]
       },
       "state_boundary_matrix": {
         "absent_or_initial": "Preserve the existing product default; changing it is not authorized.",
         "current_reusable": "Retain and render any valid current opted-in/not-opted-in value.",
         "legacy_or_incompatible": "No migration or reinterpretation is authorized; incompatible state requires a source decision.",
         "public_access_paths": ["existing settings surface", "digest preference reader"],
         "supported_variants": ["opted in", "not opted in"],
         "lifecycle_transitions": ["not opted in to opted in", "opted in to not opted in", "failed write remains at prior durable state"]
       }
     }
   }
   ```

   Result:

   ```json
   {
     "id": "FT-V09-3",
     "created": true
   }
   ```

6. `set_parent`

   Request:

   ```json
   {
     "item": "FT-V09-1",
     "parent": "P-909"
   }
   ```

   Result:

   ```json
   {
     "applied": true
   }
   ```

7. `set_parent`

   Request:

   ```json
   {
     "item": "FT-V09-2",
     "parent": "P-909"
   }
   ```

   Result:

   ```json
   {
     "applied": true
   }
   ```

8. `set_parent`

   Request:

   ```json
   {
     "item": "FT-V09-3",
     "parent": "P-909"
   }
   ```

   Result:

   ```json
   {
     "applied": true
   }
   ```

9. `set_role`

   Request:

   ```json
   {
     "item": "FT-V09-1",
     "role": "enhancement"
   }
   ```

   Result:

   ```json
   {
     "applied": true
   }
   ```

10. `set_role`

    Request:

    ```json
    {
      "item": "FT-V09-2",
      "role": "enhancement"
    }
    ```

    Result:

    ```json
    {
      "applied": true
    }
    ```

11. `set_role`

    Request:

    ```json
    {
      "item": "FT-V09-3",
      "role": "enhancement"
    }
    ```

    Result:

    ```json
    {
      "applied": true
    }
    ```

12. `set_state`

    Request:

    ```json
    {
      "item": "FT-V09-1",
      "state": "ready-for-agent"
    }
    ```

    Result:

    ```json
    {
      "applied": true
    }
    ```

13. `set_state`

    Request:

    ```json
    {
      "item": "FT-V09-2",
      "state": "ready-for-agent"
    }
    ```

    Result:

    ```json
    {
      "applied": true
    }
    ```

14. `set_state`

    Request:

    ```json
    {
      "item": "FT-V09-3",
      "state": "ready-for-agent"
    }
    ```

    Result:

    ```json
    {
      "applied": true
    }
    ```

15. `get_item`

    Request:

    ```json
    {
      "id": "FT-V09-1"
    }
    ```

    Result:

    ```json
    {
      "id": "FT-V09-1",
      "title": "Add the compact mode setting toggle",
      "body": {
        "bounded_slice": "Independently completable vertical behavior slice for the compact mode setting toggle.",
        "work_unit_form": "vertical behavior slice",
        "source_trace": "SRC-V09@1, commitment C1; owner Uma; parent P-909",
        "desired_behavior": "Expose compact mode as a two-state settings control and durably reflect a successful user change without changing timezone-display or digest-opt-in settings.",
        "acceptance": [
          "The control can be changed between supported on and off states.",
          "After a successful change, the control and compact-mode behavior read the resulting durable state consistently.",
          "The isolated compact-mode settings fixture passes in both directions.",
          "A failed or rejected durable write does not report success and does not alter the other two settings."
        ],
        "seams": [
          "compact mode settings control",
          "compact mode setting read/write boundary",
          "compact-mode behavior consumer"
        ],
        "expected_durable_write_scope": "Only the durable value representing compact mode; exact storage mechanism remains with the implementation owner.",
        "scope_fence": "No timezone-display work, digest-opt-in work, unrelated settings redesign, parent delivery, or implementation-technique mandate.",
        "dependency_state": {
          "blockers": [],
          "stable_order": 1
        },
        "proof_lane": "Isolated compact mode settings fixture exercising both transitions and the write-failure path.",
        "verification_authority": "The isolated settings fixture is the acceptance authority for this source slice.",
        "verification_evidence": "Recorded passing results for on, off, and rejected-write behavior.",
        "parallel_safety": "Independent under the source-declared separation, slice-specific durable value, and isolated fixture; a discovered shared durable key, exclusive fixture, or behavior coupling is a stop-before-write serial tripwire.",
        "execution_profile": {
          "semantic_owner": "Uma",
          "expected_production_writes": "Compact-mode control, setting boundary, and behavior consumer only.",
          "proof_seams": ["isolated compact-mode settings fixture"],
          "scarce_proof_resources": [],
          "ordering": "No blocker ordering.",
          "serial_tripwires": [
            "shared durable key",
            "exclusive shared fixture",
            "cross-toggle behavior coupling"
          ]
        },
        "state_boundary_matrix": {
          "absent_or_initial": "Preserve the existing product default; changing it is not authorized.",
          "current_reusable": "Retain and render any valid current on/off value.",
          "legacy_or_incompatible": "No migration or reinterpretation is authorized; incompatible state requires a source decision.",
          "public_access_paths": ["existing settings surface", "compact-mode behavior consumer"],
          "supported_variants": ["on", "off"],
          "lifecycle_transitions": ["off to on", "on to off", "failed write remains at prior durable state"]
        }
      },
      "parent": "P-909",
      "role": "enhancement",
      "state": "ready-for-agent",
      "assignee": null,
      "blockers": []
    }
    ```

16. `get_item`

    Request:

    ```json
    {
      "id": "FT-V09-2"
    }
    ```

    Result:

    ```json
    {
      "id": "FT-V09-2",
      "title": "Add the timezone display setting toggle",
      "body": {
        "bounded_slice": "Independently completable vertical behavior slice for the timezone display setting toggle.",
        "work_unit_form": "vertical behavior slice",
        "source_trace": "SRC-V09@1, commitment C2; owner Uma; parent P-909",
        "desired_behavior": "Expose timezone display as a two-state settings control and durably reflect whether the existing timezone display is enabled without changing compact mode or digest opt-in.",
        "acceptance": [
          "The control can be changed between supported on and off states.",
          "After a successful change, the control and timezone-display consumer read the resulting durable state consistently.",
          "The isolated timezone-display settings fixture passes in both directions.",
          "A failed or rejected durable write does not report success and does not alter the other two settings."
        ],
        "seams": [
          "timezone-display settings control",
          "timezone-display setting read/write boundary",
          "existing timezone-display consumer"
        ],
        "expected_durable_write_scope": "Only the durable value representing timezone-display enablement; exact storage mechanism remains with the implementation owner.",
        "scope_fence": "No compact-mode work, digest-opt-in work, timezone selection or formatting decisions, unrelated settings redesign, parent delivery, or implementation-technique mandate.",
        "dependency_state": {
          "blockers": [],
          "stable_order": 2
        },
        "proof_lane": "Isolated timezone-display settings fixture exercising both transitions and the write-failure path.",
        "verification_authority": "The isolated settings fixture is the acceptance authority for this source slice.",
        "verification_evidence": "Recorded passing results for on, off, and rejected-write behavior.",
        "parallel_safety": "Independent under the source-declared separation, slice-specific durable value, and isolated fixture; a discovered shared durable key, exclusive fixture, or behavior coupling is a stop-before-write serial tripwire.",
        "execution_profile": {
          "semantic_owner": "Uma",
          "expected_production_writes": "Timezone-display control, setting boundary, and existing display consumer only.",
          "proof_seams": ["isolated timezone-display settings fixture"],
          "scarce_proof_resources": [],
          "ordering": "No blocker ordering.",
          "serial_tripwires": [
            "shared durable key",
            "exclusive shared fixture",
            "cross-toggle behavior coupling"
          ]
        },
        "state_boundary_matrix": {
          "absent_or_initial": "Preserve the existing product default; changing it is not authorized.",
          "current_reusable": "Retain and render any valid current on/off value.",
          "legacy_or_incompatible": "No migration or reinterpretation is authorized; incompatible state requires a source decision.",
          "public_access_paths": ["existing settings surface", "existing timezone-display consumer"],
          "supported_variants": ["on", "off"],
          "lifecycle_transitions": ["off to on", "on to off", "failed write remains at prior durable state"]
        }
      },
      "parent": "P-909",
      "role": "enhancement",
      "state": "ready-for-agent",
      "assignee": null,
      "blockers": []
    }
    ```

17. `get_item`

    Request:

    ```json
    {
      "id": "FT-V09-3"
    }
    ```

    Result:

    ```json
    {
      "id": "FT-V09-3",
      "title": "Add the digest opt-in setting toggle",
      "body": {
        "bounded_slice": "Independently completable vertical behavior slice for the digest opt-in setting toggle.",
        "work_unit_form": "vertical behavior slice",
        "source_trace": "SRC-V09@1, commitment C3; owner Uma; parent P-909",
        "desired_behavior": "Expose digest opt-in as a two-state settings control and durably record a successful user change without changing compact mode or timezone display.",
        "acceptance": [
          "The control can be changed between supported opted-in and not-opted-in states.",
          "After a successful change, the control and digest preference reader read the resulting durable state consistently.",
          "The isolated digest-opt-in settings fixture passes in both directions.",
          "A failed or rejected durable write does not report success and does not alter the other two settings."
        ],
        "seams": [
          "digest-opt-in settings control",
          "digest preference read/write boundary",
          "digest preference reader"
        ],
        "expected_durable_write_scope": "Only the durable value representing digest opt-in; exact storage mechanism remains with the implementation owner.",
        "scope_fence": "No compact-mode work, timezone-display work, digest generation or delivery work, unrelated settings redesign, parent delivery, or implementation-technique mandate.",
        "dependency_state": {
          "blockers": [],
          "stable_order": 3
        },
        "proof_lane": "Isolated digest-opt-in settings fixture exercising both transitions and the write-failure path.",
        "verification_authority": "The isolated settings fixture is the acceptance authority for this source slice.",
        "verification_evidence": "Recorded passing results for opt-in, opt-out, and rejected-write behavior.",
        "parallel_safety": "Independent under the source-declared separation, slice-specific durable value, and isolated fixture; a discovered shared durable key, exclusive fixture, or behavior coupling is a stop-before-write serial tripwire.",
        "execution_profile": {
          "semantic_owner": "Uma",
          "expected_production_writes": "Digest-opt-in control, preference boundary, and preference reader only.",
          "proof_seams": ["isolated digest-opt-in settings fixture"],
          "scarce_proof_resources": [],
          "ordering": "No blocker ordering.",
          "serial_tripwires": [
            "shared durable key",
            "exclusive shared fixture",
            "cross-toggle behavior coupling"
          ]
        },
        "state_boundary_matrix": {
          "absent_or_initial": "Preserve the existing product default; changing it is not authorized.",
          "current_reusable": "Retain and render any valid current opted-in/not-opted-in value.",
          "legacy_or_incompatible": "No migration or reinterpretation is authorized; incompatible state requires a source decision.",
          "public_access_paths": ["existing settings surface", "digest preference reader"],
          "supported_variants": ["opted in", "not opted in"],
          "lifecycle_transitions": ["not opted in to opted in", "opted in to not opted in", "failed write remains at prior durable state"]
        }
      },
      "parent": "P-909",
      "role": "enhancement",
      "state": "ready-for-agent",
      "assignee": null,
      "blockers": []
    }
    ```
