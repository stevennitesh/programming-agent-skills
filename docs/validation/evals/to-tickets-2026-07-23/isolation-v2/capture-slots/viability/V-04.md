# Setup precondition

- Source: `SRC-V04@1`, owned by Sora (export owner); parent `P-404`.
- Requested outcome preserved: generate an export artifact, then expose a download that consumes that artifact; download must be unavailable before generation.
- Setup evidence: configured tracker `fixture-tracker` reports `incompatible: missing required blocker representation`. The required `set_blocker` operation is absent, and no ordered-task fallback is configured.
- Blocking gap: commitment C2 consumes the artifact produced by C1, so an explicit true blocker edge is required. The configured tracker cannot represent or later verify that dependency.
- Publication authority: none under the incompatible setup.
- Source state: unchanged. No slicing, ticket creation, relationship, role, or state mutation was attempted.
- Tracker state: unchanged; `P-404` still exists and the mutation log remains empty.
- Safe continuation: repair the tracker setup by adding a supported blocker relationship operation (or a configured compatible relationship representation), then rerun To Tickets on the unchanged `SRC-V04@1`.
- Recommended next owner: `$repo-bootstrap`.
