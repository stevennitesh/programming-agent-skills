# Published graph

## Evidence and tracker state

- Source: `SRC-V02@1`, owned by Inez, CLI owner.
- Publication authority: Inez authorized standalone Ready items only and forbade creating a parent.
- Setup: compatible.
- Initial tracker state: no related parent or child items.
- Source outcome: add JSON output to the existing `inspect` command.
- Scope: `inspect` output formatting.
- Exclusions preserved: no parent issue, YAML output, or command redesign.
- Authorized role: `enhancement`.

## Frozen commitment ledger

| Source commitment or boundary | Disposition |
|---|---|
| C1: serialize the existing inspect result as stable JSON | T-201 acceptance |
| JSON contains exactly `name:string`, `status:string`, and `details:object|null`; all keys are present in that order | T-201 acceptance and subprocess proof |
| Unknown formats exit nonzero without partial output | T-201 acceptance and subprocess proof |
| C2: document the flag and invalid-format error | T-202 acceptance |
| Existing `inspect` result and command remain the behavior seam | T-201 scope fence |
| No parent issue | Both items are standalone; no parent mutation |
| YAML output and command redesign | Explicitly excluded from both tickets |

The two slices are separate because production behavior and user-facing documentation have distinct durable write scopes and observable review seams. T-202 can be written from the settled contract and does not consume a T-201 outcome, so no blocker edge is justified.

## Ordered standalone tickets

### T-201 — Add stable JSON formatting to `inspect`

- State/role: `ready-for-agent`; `enhancement`.
- Work-unit form: independently completable vertical behavior slice.
- Source Trace: `SRC-V02@1`; outcome; C1; both source acceptance statements; scope and exclusions.
- Desired behavior: `inspect --format json` serializes the existing inspect result as one stable JSON object containing exactly `name`, `status`, and `details`, in that order. `name` and `status` are strings; `details` is an object or null; every key is present for every supported inspect result.
- Edge/error behavior: both object-valued and null `details` retain the same three-key shape and order. Any unknown format exits nonzero and emits no partial output.
- Acceptance:
  1. The `inspect_json` CLI subprocess fixture observes exactly the required three keys in the required order for object-valued details.
  2. The fixture observes the same complete shape when details is null.
  3. The fixture observes a nonzero exit and empty partial-output channel for an unknown format.
  4. Existing non-JSON inspect behavior remains unchanged.
- Relevant seams: the existing inspect-result-to-output formatting boundary, format validation, CLI exit handling, and the `inspect_json` subprocess fixture.
- Expected durable write scope: inspect format selection/serialization and error handling, plus the bounded `inspect_json` fixture assertions needed to prove the behavior.
- Scope fence: do not redesign the command, add YAML, change the inspect result model, create a parent, or change unrelated output modes.
- Dependencies: none.
- Stable tracker order: 1.
- Proof lane: CLI subprocess fixture `inspect_json`.
- Verification authority: the settled acceptance owned by Inez.
- Verification evidence required: passing subprocess observations for exact key set/order/types, both supported `details` variants, nonzero unknown-format exit, no partial output, and unchanged existing output.
- Parallel safety: production writes are bounded to the formatter/validation seam, but its named subprocess fixture is a shared proof resource with T-202; proof-lane edits and execution must be serialized if both tickets touch that fixture.
- Execution profile:
  - Semantic ownership: CLI inspect output formatting and format-error behavior.
  - Production writes: formatter/serializer selection and invalid-format handling only.
  - Proof seam/scarce resource: `inspect_json`, shared with T-202.
  - Ordering: first in stable tracker order; no true blocker.
  - Serial tripwire: any edit to the shared subprocess fixture or discovery that documentation is generated from the same production surface.
  - Independence: behavior implementation is otherwise independent of T-202's documentation writes.
- State-boundary matrix: not applicable. This slice is stateless output transformation and process-exit behavior; it creates, reuses, migrates, or exposes no persisted state and has no lifecycle transition. Supported runtime branches are object-valued details, null details, and unknown format, all covered above.

### T-202 — Document the JSON flag and invalid-format error

- State/role: `ready-for-agent`; `enhancement`.
- Work-unit form: independently completable documentation slice with observable CLI proof.
- Source Trace: `SRC-V02@1`; C2; the exact settled JSON contract; invalid-format acceptance; scope and exclusions.
- Desired behavior: user-facing inspect documentation describes `--format json`, the exact always-present `name`, `status`, and `details` fields in their stable order and types, and the nonzero/no-partial-output behavior for unknown formats.
- Edge/error behavior: documentation explicitly states that `details` can be an object or null and that unknown formats are rejected rather than producing partial output.
- Acceptance:
  1. The inspect flag documentation exposes `json` as the supported value.
  2. It documents all three always-present keys, their types, their order, and both supported `details` variants.
  3. It documents that an unknown format exits nonzero without partial output.
  4. It does not promise YAML or a redesigned command.
  5. The documented examples and error description agree with subprocess observations from `inspect_json`.
- Relevant seams: inspect command help/reference content and the `inspect_json` subprocess fixture used to validate documented examples and error semantics.
- Expected durable write scope: inspect flag/help/reference documentation and, only if needed, bounded documentation assertions in `inspect_json`; no production command changes.
- Scope fence: no production implementation, YAML documentation, command redesign, parent creation, or unrelated CLI documentation.
- Dependencies: none. The source contract is settled, so this ticket does not require T-201's implementation outcome to author its content.
- Stable tracker order: 2.
- Proof lane: documentation inspection reconciled with CLI subprocess fixture `inspect_json`.
- Verification authority: the settled contract and acceptance owned by Inez.
- Verification evidence required: reviewed help/reference text covering the flag, exact contract, nullable branch, and invalid-format behavior, plus matching `inspect_json` subprocess observations.
- Parallel safety: documentation-only writes are independent of T-201's production writes. The named subprocess fixture is a shared scarce proof resource, so any edits to it or final proof runs must be serialized with T-201.
- Execution profile:
  - Semantic ownership: user-facing contract for inspect output formatting.
  - Production writes: none.
  - Proof seam/scarce resource: `inspect_json`, shared with T-201.
  - Ordering: second in stable tracker order; no true blocker.
  - Serial tripwire: touching the shared fixture, generated documentation, or the same help-definition source as production format registration.
  - Independence: documentation content is independently derivable from settled source.
- State-boundary matrix: not applicable. Documentation of a stateless formatting flag and process error has no persisted-state branches or lifecycle transitions; object-valued details, null details, and unknown format are the only distinct supported/error branches.

## Dependency graph and frontier

- Graph: `T-201` (no blockers); `T-202` (no blockers).
- Cycles, missing edges, contradictory order, and false readiness: none.
- Ready frontier in tracker order: `T-201`, `T-202`.
- Parallel dispatch note: both items are semantically and write-scope independent, but the shared `inspect_json` proof resource creates a serial tripwire for fixture mutation and final verification.

## Simulated mutation read-back

- `create_item` created standalone T-201.
- `create_item` created standalone T-202.
- No parent or blocker mutation was made.
- `set_role` applied `enhancement` to T-201 and T-202.
- `set_state` applied `ready-for-agent` to T-201 and T-202.
- `get_item` returned the exact standalone bodies above, role `enhancement`, state `ready-for-agent`, no assignee, no parent, and no blockers for both items.
- Observed tracker state: two verified standalone Ready items, ordered T-201 then T-202; frontier is T-201 and T-202.
- Applied operations: both creates, both authorized roles, and both Ready states.
- Failed or unknown operations: none.
- Gaps: none.

## Exact safe continuation

Recommendation: `none`. The request authorized publication and stop, not top-level delivery; no implementation owner is invoked.
