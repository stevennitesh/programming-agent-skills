# Published graph

## Evidence and tracker state

- Source: `SRC-V14@1`, owned by Zed, analytics owner.
- Parent: `P-1414`.
- Publication authority: publish only C1; preserve the dispositions of C2 and C3.
- Setup: compatible.
- Initial tracker state: `P-1414` existed with no children.
- Simulated operations, in order: `get_setup`, `get_parent(P-1414)`, `create_item`, `set_parent`, `set_role(enhancement)`, `set_state(ready-for-agent)`, `get_item`.
- Mutation read-back: exact. One item exists under `P-1414`; its body, parent, `enhancement` role, unassigned status, and `ready-for-agent` state match the frozen design below.
- Observed tracker state: `P-1414` has exactly the one published child described below. No other item or relationship was created or changed.
- Gaps: none blocking publication.

## Ordered ticket

### 1. `SRC-V14-C1` — Expose the daily aggregate usage total

- **Work-unit form:** bounded vertical behavior slice.
- **Source Trace:** `SRC-V14@1`; outcome “Expose basic usage totals”; commitment C1 “daily total authorized now”; acceptance “daily total matches aggregate fixture”; scope “daily aggregate only”; proof seam “aggregate fixture”; parent `P-1414`.
- **Desired behavior:** expose one daily usage total derived from the aggregate source.
- **Acceptance:**
  - For every case represented by the aggregate fixture, the exposed daily total exactly matches that fixture's aggregate value.
  - A fixture case with no contributing usage produces the aggregate fixture's zero total rather than a fabricated or user-derived value.
  - An unavailable or invalid aggregate input does not return a misleading successful total; the existing aggregate-access error boundary remains observable.
  - The exposed result contains only the daily aggregate. It contains no cohort breakdown and no user-level data or export path.
- **Relevant seams:** the existing daily-total exposure boundary, aggregate-read boundary, and aggregate fixture. Exact implementation paths and technique remain with the delivery owner.
- **Expected durable write scope:** only the production surface required to expose the daily aggregate total and its aggregate-fixture verification. No cohort, user-export, privacy-policy, migration, or unrelated analytics writes.
- **Scope fence:** C1 only. Do not implement C2's cohort breakdown or C3's user-level export, create a replacement parent, change aggregation semantics beyond what the fixture proves, or start implementation from this publication task.
- **Dependency state:** no true blockers.
- **Proof lane:** verify the public daily-total behavior against the aggregate fixture, including its zero-total and aggregate-error cases and the absence of cohort or user-level output.
- **Verification authority:** `SRC-V14@1` acceptance and the owner-supplied aggregate fixture.
- **Verification evidence:** passing aggregate-fixture comparison for the daily result, passing zero-total and aggregate-error checks, and an interface/result inspection showing daily aggregate fields only.
- **Parallel safety:** serial by default. There is no blocker edge, but the ticket owns one analytics exposure seam and one aggregate proof resource; concurrent work touching either is a serial tripwire unless the delivery owner first proves disjoint write and proof scopes.
- **Role:** `enhancement`, explicitly authorized by the source category.
- **State:** `ready-for-agent`.
- **Assignee:** none.
- **State-boundary matrix:**

  | Boundary | Supported branch or interaction |
  |---|---|
  | Absent or initial state | No contributing usage is represented by the aggregate fixture's zero total; do not infer user-level state. |
  | Current reusable state | Read the existing daily aggregate and expose the fixture-matching total. |
  | Legacy or incompatible state | None identified or authorized; no migration or compatibility work is in scope. |
  | Public access paths | Daily aggregate exposure only. Cohort breakdown and user-level export are not access paths for this slice. |
  | Supported variants | The daily aggregate cases represented by the authoritative fixture, including zero. |
  | Lifecycle transitions | As the aggregate value changes for a day, the exposed total follows that aggregate; no schema migration, cutover, or irreversible state transition is authorized. |

## Commitment coverage

| Source commitment or boundary | Disposition |
|---|---|
| C1 — daily total | Covered by `SRC-V14-C1` and published now. |
| C2 — cohort breakdown | Explicitly deferred to Q4. No ticket or implementation scope was created in this graph. |
| C3 — user-level export | Explicitly out of product scope for privacy. No ticket, export path, or user-level data scope was created. |
| Outcome — expose basic usage totals | Covered only to the authorized daily-aggregate extent. |
| Scope — daily aggregate only | Enforced by ticket acceptance and scope fence. |
| Exclusion — user-level export | Preserved as an explicit exclusion. |

## Dependency graph and frontier

`P-1414` → `SRC-V14-C1`

There are no blocker edges. The truthful ready frontier, in tracker order, is:

1. `SRC-V14-C1`

## Safe continuation

Recommend exactly one next owner: `$implement` for `SRC-V14-C1`. Do not start it from this result.
