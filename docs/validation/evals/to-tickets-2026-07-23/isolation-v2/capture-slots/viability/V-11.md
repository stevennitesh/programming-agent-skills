# Partial-publication recovery

## Result

Publication of `SRC-V11@1` is incomplete. The first ordered item was created as
`T-111`; creation of the second item failed with `quota`. Publication stopped
at that point, as authorized. No parent, blocker, role, or state mutation was
attempted after the failure. This is not a published graph, and no complete
graph or ready frontier is claimed.

## Evidence

- Source: `SRC-V11@1`, owned by Wen, import owner.
- Parent: `P-1111`.
- Initial observed tracker state: `P-1111` existed with no children.
- Publication authority: attempt exact complete publication once; on failure,
  observe and stop.
- Operation result: first `create_item` returned `T-111`.
- Operation result: second `create_item` failed with `quota`.
- Read-back: `get_item T-111` returned the exact created body reproduced below.
- Relationship read-back: `get_dependents` returned none.
- No live provider was contacted; these are the supplied fixture observations.

## Frozen complete design

### Commitment ledger

| Source commitment or boundary | Disposition |
|---|---|
| C1: parse and validate rows | Slice 1, created as `T-111` |
| C2: apply only validated rows | Slice 2, creation failed |
| Apply consumes parser output | True blocker edge: Slice 2 consumes Slice 1 output |
| Import pipeline scope | Enforced by both scope fences |
| Blind retry exclusion | Enforced by Slice 2 and this recovery |
| Import integration fixture proof | Verification lane for both slices |
| Enhancement category | Intended authorized role for both slices |

No source-visible commitment is otherwise deferred or excluded.

### Stable order and intended graph

1. `T-111` — Parse and validate import rows.
2. Uncreated Slice 2 — Apply validated import rows.

Intended dependency: `T-111 -> Slice 2`. Slice 2 consumes the validated parser
output, so this is a true blocking edge. The predicted frontier after a
successful complete publication was `T-111` only.

### Exact created body read back for `T-111`

**Title:** Parse and validate import rows

**Work-unit form:** Independently completable vertical behavior slice.

**Source Trace:** `SRC-V11@1`; owner: Wen, import owner; parent: `P-1111`;
commitment: C1; outcome: parse an import file before validated rows are
applied; proof seam: import integration fixture.

**Desired behavior and acceptance:** Given an import file, parse its rows and
validate every parsed row. Emit parser output that distinguishes validated rows
from invalid rows and is consumable by the apply slice. Valid rows appear in
the validated output; invalid or malformed rows do not. An empty file produces
an empty validated result rather than rows to apply. Parse or validation
failure is observable to the import pipeline and does not produce a row marked
validated. Acceptance is proven when the import integration fixture observes
these valid, invalid/malformed, empty, and failure branches and exposes the
validated output consumed by the apply slice.

**Relevant seams:** Import-file input, row parser and validator, and the
validated-output boundary consumed by Slice 2.

**Expected durable write scope:** None. This slice produces parser/validation
output and does not apply imported rows.

**Scope fence:** In scope: parsing, row validation, and the validated-output
boundary inside the import pipeline. Out of scope: applying rows, changing
unrelated import behavior, and blind retry.

**Dependency state:** True blockers: none. Stable tracker order: 1.

**Proof lane:** Import integration fixture.

**Verification authority:** The acceptance in `SRC-V11@1`, owned by Wen,
exercised at the import integration seam.

**Verification evidence required:** A passing import integration fixture
showing consumable parser output for valid rows and observable handling of
invalid/malformed rows, empty input, and parser/validation failure.

**Parallel-safety judgment:** Not parallel with Slice 2. Slice 2 consumes this
slice's required output and must remain blocked until this slice passes its
proof. No durable-write overlap exists in this slice.

**Execution profile:** Semantic ownership: import parsing and validation.
Production writes: parser/validator and validated-output seam only; no durable
data writes. Scarce proof resource: the shared import integration fixture.
Ordering: first. Serial tripwire: any change to the validated-output contract
or shared fixture requires serialization with Slice 2. Independence: may run
independently of unrelated work when those seams are not shared.

**State-boundary matrix:** Not applicable. This slice is stateless with respect
to durable imported data: it transforms an import file into validation output
and performs no durable write. Input variants and errors are covered directly
by acceptance.

**Intended role and state:** `enhancement`; `ready-for-agent`, but only through
the separate authorized role and state mutations after the complete graph
exists.

### Uncreated Slice 2 body

**Title:** Apply validated import rows

**Work-unit form:** Independently completable vertical behavior slice blocked
on the parser outcome.

**Source Trace:** `SRC-V11@1`; owner: Wen, import owner; parent: `P-1111`;
commitment: C2; acceptance: apply consumes parser output; proof seam: import
integration fixture.

**Desired behavior and acceptance:** Consume the validated output from Slice 1
and apply only rows marked validated. Invalid or malformed rows are never
applied. Empty validated output performs no row application. A missing,
failed, or incompatible parser result is rejected rather than treated as
validated input. An apply failure is observable and does not trigger blind
retry. Acceptance is proven when the import integration fixture shows that
validated rows are applied, invalid rows are excluded, empty output makes no
row writes, and invalid parser output and apply failure follow the stated error
behavior.

**Relevant seams:** Validated parser-output boundary and durable import-row
application boundary.

**Expected durable write scope:** Imported-row records produced only from
validated parser output.

**Scope fence:** In scope: consuming Slice 1 output and applying its validated
rows within the import pipeline. Out of scope: parser/validator implementation,
unrelated persistence, and blind retry.

**Dependency state:** Blocked by `T-111`, because this slice consumes its
validated-output contract. Stable tracker order: 2.

**Proof lane:** Import integration fixture.

**Verification authority:** The acceptance in `SRC-V11@1`, owned by Wen,
exercised at the import integration seam.

**Verification evidence required:** A passing import integration fixture
showing durable application of validated rows only, zero application for empty
validated output, rejection of invalid parser output, and observable apply
failure without blind retry.

**Parallel-safety judgment:** Serial after `T-111`. Dependency on its output
and use of the shared import integration fixture preclude parallel execution
until the predecessor is complete and its contract is proven.

**Execution profile:** Semantic ownership: validated-row application.
Production writes: apply implementation and imported-row durable records.
Scarce proof resource: the shared import integration fixture. Ordering: second.
Serial tripwires: validated-output contract, durable import writes, or shared
fixture changes. Independence is not evidenced before `T-111` completes.

**State-boundary matrix:**

| Boundary | Supported branch |
|---|---|
| Absent/initial | No validated rows have been applied; empty validated output leaves this unchanged. |
| Current reusable | A compatible validated result from `T-111` may be consumed once by the apply path. |
| Legacy/incompatible | Raw, missing, failed, or incompatible parser output is not accepted as validated input. |
| Public access path | Import pipeline only. |
| Supported variants | All-valid, mixed valid/invalid, all-invalid, and empty validated output. |
| Lifecycle transition | Validated parser output -> apply attempt -> imported-row durable state or observable apply failure; no blind-retry transition. |

**Intended role and state:** `enhancement`; `ready-for-agent`, only after the
blocker relationship is installed and the complete graph passes read-back.

## Applied and failed operations

| Order | Operation | Result |
|---|---|---|
| 1 | Create Slice 1 from the frozen body | Applied: `T-111` |
| 2 | Create Slice 2 from the frozen body | Failed: `quota` |

Not applied: parent relationships to `P-1111`, the blocker edge, enhancement
roles, and ready-for-agent states. Their absence is intentional after the
creation failure, not evidence of completion.

## Observed tracker state and frontier

- `P-1111` remains without a verified child relationship.
- `T-111` exists with the exact body above.
- No dependent of `T-111` exists.
- Slice 2 does not exist from this attempt because its creation returned a
  definite failure.
- `T-111` has no verified parent, role, or ready state.
- Observed ready frontier: empty. `T-111` must not be treated as
  ready-for-agent merely because its body is ready-capable.

## Gaps and safest exact continuation

The graph is missing Slice 2 and every intended relationship, role, and state
mutation. Do not retry blindly, do not dispatch implementation, and do not
claim `T-111` as ready.

The exact safe continuation is:

1. Preserve `T-111` unchanged.
2. Have the fixture-tracker owner resolve or increase the creation quota.
3. In a resumed, explicitly authorized publication attempt, first refetch
   `T-111` and confirm that its body still matches this read-back.
4. Create only the frozen Slice 2 body once. The earlier result was a definite
   failure, not an ambiguous receipt.
5. If creation succeeds, apply both parent relationships to `P-1111`, set
   Slice 2 blocked by `T-111`, apply the authorized `enhancement` roles, and
   then apply `ready-for-agent` state only to items that pass the ready gate.
6. Refetch both items and the affected dependent; verify exact bodies, parent,
   blocker, roles, assignees, states, and the resulting frontier. The expected
   complete frontier is `T-111`.
7. If quota remains unavailable, any body differs, or any later mutation or
   read-back fails, stop again and return the newly observed partial state.

No implementation-owner recommendation is made because publication is
incomplete.
