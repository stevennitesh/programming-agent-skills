# Published graph

## Evidence and tracker state

- Source: `SRC-V13@1`
- Source owner: Yara, reports owner
- Parent: `P-1313`
- Publication authority: “Publish the justified Ready graph.”
- Setup: compatible
- Prior tracker state: `P-1313` existed with no children.
- Source proof seam: report generation filesystem fixture.
- Observed tracker state after publication: `P-1313` has exactly the one child below. No support-only item was created.
- Gaps: none.

## Commitment ledger and coverage

| Source element | Disposition |
|---|---|
| C1: generate the report into the configured output directory | Acceptance of `created-item-1` |
| Create the configured directory when absent | Acceptance and state branch of `created-item-1` |
| Write the monthly report | Acceptance and durable-write scope of `created-item-1` |
| Proposed “prepare folders” support slice | No ticket: it has no observable proof or named consuming behavior, the behavior slice creates its own directory, and the source explicitly excludes a coordination-only setup ticket |
| Scope: report generation | Scope fence of `created-item-1` |

Coverage is exhaustive: every source-visible commitment and boundary has a disposition.

## Ordered ticket

### 1. `created-item-1` — Generate the monthly report in the configured output directory

- **State:** `ready-for-agent`
- **Role:** `enhancement`
- **Work-unit form:** independently completable vertical behavior slice
- **Parent:** `P-1313`
- **Source Trace:** `SRC-V13@1`; owner Yara, reports owner; C1; source acceptance “generation creates the directory when absent and writes the report”; proof seam “report generation filesystem fixture”
- **Desired behavior:** generation produces the monthly report file in the configured output directory.
- **Observable acceptance:**
  1. Given an absent configured output directory, generation creates that directory and writes the monthly report file inside it.
  2. Given an existing usable configured output directory, generation reuses it and writes the monthly report file inside it.
  3. Given a configured output path that cannot be created or written, generation reports failure and does not report successful generation.
- **Dependency state:** no true blockers.
- **Relevant seams:** monthly report generation entry point, configured output-directory boundary, and filesystem write boundary.
- **Expected durable write scope:** the configured output directory when absent and the monthly report file within it; no other durable writes.
- **Scope fence:** include report generation, directory creation needed by that generation, and filesystem failure handling. Exclude a separate folder-preparation or coordination-only unit, unrelated report changes, delivery, and worker dispatch.
- **Proof lane:** exercise report generation through the filesystem fixture with isolated output paths.
- **Verification authority:** the observable acceptance in `SRC-V13@1`, owned by Yara.
- **Verification evidence:** fixture evidence showing the absent-directory branch creates the directory and report, the reusable-directory branch writes the report in place, and unwritable/create-failure branches return failure without a success result.
- **Parallel-safety judgment:** no graph peer exists to run in parallel. Execution must be serialized with any work or verification using the same configured output directory or monthly report path because those durable writes and fixture paths overlap; isolated paths are independent.

## Dependency graph and frontier

```text
P-1313
└── created-item-1 (ready-for-agent; blockers: none)
```

There are no blocker edges, cycles, missing edges, or contradictory ordering. The ready frontier is, in tracker order:

1. `created-item-1`

## Execution profile

| Ticket | Semantic ownership | Production writes | Proof seam / scarce resource | Ordering | Serial tripwires | Independence |
|---|---|---|---|---|---|---|
| `created-item-1` | Monthly report generation | Configured output directory if absent; monthly report file | Report generation filesystem fixture; configured fixture path | First and only item; no predecessor outcome consumed | Same output directory, same report path, or same non-isolated fixture path | Independent only when output and fixture paths are isolated |

## State-boundary matrix

| Boundary branch | Supported behavior / proof |
|---|---|
| Absent or initial state | Create the configured output directory, then write the monthly report; verify both exist in the fixture. |
| Current reusable state | Reuse the existing usable directory and write the monthly report inside it. |
| Legacy or incompatible state | If the configured output path cannot serve as a writable directory, fail generation and do not claim success. No migration is authorized. |
| Public access paths | The configured output-directory boundary used by report generation; no separate preparation path is introduced. |
| Supported variants | Absent directory and existing usable directory. |
| Lifecycle transitions | Absent directory → created directory → report written; reusable directory → report written; unusable/unwritable path → reported failure. |
| High-risk interactions | Concurrent use of the same output directory/report path and filesystem create/write failures; serialize shared-path execution and cover failure branches in the fixture. |

## Mutation read-back

Applied simulated operations, in order:

1. `create_item` created `created-item-1` with the exact ticket body above.
2. `set_parent(created-item-1, P-1313)` applied.
3. `set_role(created-item-1, enhancement)` applied.
4. `set_state(created-item-1, ready-for-agent)` applied.
5. `get_item(created-item-1)` read back the exact body, parent `P-1313`, role `enhancement`, no assignee, state `ready-for-agent`, and no blockers.

The fixture observation was exact. Failed or unknown operations: none. The resulting frontier is exactly `created-item-1`.

## Safe continuation

Recommend exactly one next owner: `$implement` for the single ready item `created-item-1`. Do not invoke it.
