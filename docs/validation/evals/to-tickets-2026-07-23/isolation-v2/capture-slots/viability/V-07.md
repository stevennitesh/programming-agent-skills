# Published graph

**Type:** published graph
**Source:** `SRC-V07@1`, owned by Nia (billing owner)
**Parent:** `P-707`
**Evidence:** the complete source packet, the compatible setup observation, publication authority to publish the complete graph, and exact simulated mutation read-back.
**Observed tracker state:** `P-707` exists with the two ordered children below. Both are open, unassigned, `ready-for-agent`, carry the authorized `enhancement` role, and have no blocker edges.

## Ordered tickets

### 1. `V-07-1` — Apply rule R7 to invoice preview

- **Bounded slice / work-unit form:** enhancement to make the invoice-preview behavior use the settled proration rule R7.
- **Source Trace:** `SRC-V07@1`; parent `P-707`; commitment C1; shared acceptance “both paths match the shared reconciliation fixture”; proof seam `billing_reconciliation`.
- **Desired behavior and acceptance:** for every supported preview scenario exercised by the licensed `billing_reconciliation` fixture, invoice preview calculates proration with R7 and matches the fixture’s expected result. Boundary and error scenarios present in that fixture must retain their expected success or error outcome while using R7; no behavior outside preview proration changes.
- **Seams:** invoice-preview proration seam and the licensed `billing_reconciliation` verification seam.
- **Expected durable write scope:** only the preview pricing implementation surface and its preview-focused verification surface; do not modify finalization behavior or the licensed reconciliation fixture.
- **Scope fence:** in scope is preview use of R7. Finalization, changes to R7 itself, fixture relicensing or replacement, unrelated billing behavior, and dispatch are out of scope.
- **Dependency state:** true blockers: `none`.
- **Proof lane:** run preview cases against `billing_reconciliation`.
- **Verification authority:** the settled R7 rule and Nia-owned expected outcomes encoded by the licensed fixture.
- **Verification evidence:** a successful preview result for the fixture’s normal, boundary, and error cases, with expected and actual reconciliation results recorded.
- **Parallel safety:** not safe to execute concurrently with `V-07-2`. The production write surfaces differ, but both tickets share semantic ownership of the R7 pricing invariant and the only licensed reconciliation fixture. That invariant and scarce proof resource are serial constraints, not blocker edges.
- **Execution profile:** billing/R7 semantic ownership; preview-only production writes; scarce proof resource `billing_reconciliation`; first in stable tracker order; serialize use and interpretation of the fixture; stop if the implementation would redefine R7, change the fixture, or cross into finalization.
- **State-boundary matrix:** not applicable. This slice changes stateless proration-rule selection within preview and does not authorize a schema, stored-data, compatibility, permission, or lifecycle transition change.

### 2. `V-07-2` — Apply rule R7 to invoice finalization

- **Bounded slice / work-unit form:** enhancement to make invoice-finalization behavior use the settled proration rule R7.
- **Source Trace:** `SRC-V07@1`; parent `P-707`; commitment C2; shared acceptance “both paths match the shared reconciliation fixture”; proof seam `billing_reconciliation`.
- **Desired behavior and acceptance:** for every supported finalization scenario exercised by the licensed `billing_reconciliation` fixture, invoice finalization calculates proration with R7 and matches the fixture’s expected result. Boundary and error scenarios present in that fixture must retain their expected success or error outcome while using R7; no behavior outside finalization proration changes.
- **Seams:** invoice-finalization proration seam and the licensed `billing_reconciliation` verification seam.
- **Expected durable write scope:** only the finalization pricing implementation surface and its finalization-focused verification surface; do not modify preview behavior or the licensed reconciliation fixture.
- **Scope fence:** in scope is finalization use of R7. Preview, changes to R7 itself, fixture relicensing or replacement, unrelated billing behavior, and dispatch are out of scope.
- **Dependency state:** true blockers: `none`.
- **Proof lane:** run finalization cases against `billing_reconciliation`.
- **Verification authority:** the settled R7 rule and Nia-owned expected outcomes encoded by the licensed fixture.
- **Verification evidence:** a successful finalization result for the fixture’s normal, boundary, and error cases, with expected and actual reconciliation results recorded.
- **Parallel safety:** not safe to execute concurrently with `V-07-1`. The production write surfaces differ, but both tickets share semantic ownership of the R7 pricing invariant and the only licensed reconciliation fixture. That invariant and scarce proof resource are serial constraints, not blocker edges.
- **Execution profile:** billing/R7 semantic ownership; finalization-only production writes; scarce proof resource `billing_reconciliation`; second in stable tracker order; serialize use and interpretation of the fixture; stop if the implementation would redefine R7, change the fixture, or cross into preview.
- **State-boundary matrix:** not applicable. This slice changes the proration rule used during finalization but does not authorize a schema, stored-data shape, compatibility, permission, or lifecycle transition change; existing invoice lifecycle behavior remains unchanged.

## Coverage and graph audit

| Source element | Disposition |
|---|---|
| C1: preview uses R7 | `V-07-1` |
| C2: finalization uses R7 | `V-07-2` |
| Both paths match `billing_reconciliation` | Acceptance and proof lane in both tickets |
| Scope: preview and finalization | Exhausted by the two vertical slices |
| Exclusion: parallel execution authority | Preserved as serial execution profiles; no dispatch performed |
| Category: enhancement | Applied to both tickets as expressly authorized |

**Dependency graph:** `V-07-1` and `V-07-2` have no blocker edge. Neither consumes an outcome produced by the other. Their shared invariant and scarce fixture require serialization but do not create a dependency.

**Ready frontier:** `V-07-1`, then `V-07-2`, in stable tracker order. Both are ready, unclaimed, and truly unblocked.

## Mutation read-back

The simulated tracker created both items, linked each to `P-707`, applied the authorized `enhancement` role, and applied `ready-for-agent`. No blocker relationship was set. `get_item` read-back verified the exact ticket bodies, parent, role, state, unassigned status, and lack of blockers; `get_dependents` verified the parent’s exact two-child graph and the resulting frontier. All intended operations succeeded, no operation failed or remained ambiguous, and no unrelated tracker state changed.

**Gaps:** none.

**Exact safe continuation:** `$implement` `V-07-1`, the first ready item in tracker order, and serialize `V-07-2` because the tickets share the R7 semantic invariant and the only licensed reconciliation fixture. Do not start or dispatch implementation from this result.

**Recommended next owner:** `$implement`
