# Partial-publication recovery

## Evidence

- Source: `SRC-V12@1`, owned by Xia, search owner.
- Parent: `P-1212`.
- Publication authority: publish once and verify exact state.
- Setup and source were complete; `P-1212` existed with no children.
- Mutation receipts reported success for create, parent link, `enhancement`
  role, and requested `ready-for-agent` state.
- Mandatory `get_item` read-back returned the exact intended body, parent, and
  role, but observed status `backlog` instead of `ready-for-agent`.

## Frozen coverage and intended item

The single source commitment, C1, is covered by one bounded vertical behavior
slice. Fuzzy search is explicitly excluded; there are no deferrals or
unaccounted commitments.

**Stable order 1 — Add prefix search**

- Work-unit form: enhancement behavior slice.
- Source Trace: `SRC-V12@1`; parent `P-1212`; C1 “prefix query returns matching
  names”; accepted proof seam “search integration fixture.”
- Desired behavior and acceptance: a prefix query returns matching names, and
  case-normalized prefixes produce the same matching behavior.
- Edge/error behavior: a prefix with no matches returns no matching names
  without falling back to fuzzy matching; case variation does not change the
  result set.
- Expected durable write scope: prefix-search production behavior and its
  search integration fixture.
- Scope fence: include prefix matching and case normalization; exclude fuzzy
  search and unrelated search behavior.
- Dependency state: true blockers `none`.
- Proof lane: search integration fixture.
- Verification authority: the source acceptance owned by Xia and the configured
  Ready-for-agent contract.
- Verification evidence: passing integration cases for matching prefixes,
  case-normalized prefixes, and a no-match prefix, with evidence that fuzzy
  matching is not used.
- Parallel safety: serial by default because this is the only slice and its
  expected production and fixture writes constitute one semantic unit; this is
  not a blocker edge.
- Execution profile: search behavior is the semantic ownership boundary;
  expected writes are confined to prefix-search behavior and the integration
  fixture; the integration fixture is the scarce proof resource; no predecessor
  ordering applies; changes outside the scope fence or evidence of shared search
  semantics are serial tripwires.
- State-boundary matrix: not applicable because the slice is stateless query
  behavior and does not persist or migrate lifecycle state.
- Intended role/state: `enhancement`, `ready-for-agent`.

## Applied operations and read-back

Applied once, in order:

1. Created the one item with the frozen body above.
2. Linked it to `P-1212`.
3. Applied authorized role `enhancement`.
4. Requested state `ready-for-agent`.
5. Refetched it with `get_item`.

No operation returned a call-level failure, but the state mutation is
unverified and mismatched: read-back observed `backlog`. The exact body,
parent, and role read back correctly. There are no dependent items and no
blocker edges, so no dependent relationship was affected.

## Observed tracker state

`P-1212` now has the created child with the intended body and `enhancement`
role, but the child is in `backlog`. The truthful ready frontier is therefore
empty. The item must not be represented as Ready-for-agent, and the publication
must not be claimed as a complete graph.

## Gap and safe continuation

Gap: requested and observed lifecycle state disagree. Preserve the created item
and all successful metadata; do not create a duplicate or repeat the already
authorized publication sequence.

Exact safe continuation: the tracker owner must determine why
`ready-for-agent` was coerced to `backlog`, move this existing item to
`ready-for-agent` only after that lifecycle issue is resolved, then refetch the
item and verify its exact body, parent, role, assignee, and status. Recompute the
frontier from that read-back. Until then, tracker state remains as observed and
the recommended next owner is `none`.
