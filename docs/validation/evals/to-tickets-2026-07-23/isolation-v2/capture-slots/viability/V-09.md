# Published graph

## Source

- Bounded source: `SRC-V09@1`
- Intent owner: Uma, settings owner
- Parent: `P-909`
- Source outcome: add three independent settings toggles.
- Publication authority: ticket publication only.
- Preserved exclusion: parent-delivery campaign.
- Evidence: the complete `SRC-V09@1` packet, its three commitments, its isolated-fixture proof requirement, the compatible setup observation, and the fixture publication/read-back observation.
- Prior tracker state: `P-909` existed with no children and no explicit delivery request.

## Commitment ledger and coverage

| Source commitment or boundary | Disposition |
|---|---|
| C1 compact mode | `V-09-T1`, complete vertical behavior slice |
| C2 timezone display | `V-09-T2`, complete vertical behavior slice |
| C3 digest opt-in | `V-09-T3`, complete vertical behavior slice |
| Each toggle has isolated proof | Required independently by each ticket |
| Settings-toggle scope | Enforced by each ticket's scope fence |
| Enhancement category | `enhancement` role applied to all three tickets |
| Parent-delivery campaign | Explicitly excluded; no delivery campaign or worker dispatch |

All source-visible commitments and boundaries have a disposition. No support,
migration, or parent-spec ticket is justified.

## Ordered tickets

### 1. `V-09-T1` — Add the compact-mode settings toggle

- Work-unit form: independently completable vertical behavior slice.
- Source Trace: `SRC-V09@1`; outcome; commitment C1; acceptance “each toggle has isolated proof”; scope “settings toggles”; proof “three isolated settings fixtures”; parent `P-909`.
- Desired behavior: a user can read, enable, disable, and re-read the compact-mode setting, and the observable compact-mode selection matches the persisted boolean value.
- Edge/error acceptance:
  - both supported boolean states can be selected and round-trip through the public settings path;
  - selecting the already-current state is idempotent;
  - an absent value resolves through the supported settings default rather than producing an invalid third state;
  - an invalid or failed update is rejected and does not partially change the durable setting;
  - timezone-display and digest-opt-in state remain unchanged.
- Proof lane: isolated compact-mode settings fixture exercising absent/default, off-to-on, on-to-off, repeated-value, and rejected-update behavior.
- Verification authority: the isolated compact-mode fixture.
- Verification evidence required: passing fixture results for every listed acceptance branch, including durable re-read and non-interference checks.
- Expected durable write scope: only the compact-mode setting value in the settings store.
- Scope fence: no timezone-display or digest-opt-in behavior; no unrelated settings UI/store changes; no parent-delivery work.
- Dependency state: open and ready; true blockers: `none`.
- Role/state: `enhancement`; `ready-for-agent`.
- Stable tracker order: 1.
- Parallel safety: safe to run independently of `V-09-T2` and `V-09-T3`; it has a distinct semantic setting, durable value, and isolated proof fixture.
- Execution profile: semantic owner Uma/settings; production writes limited to compact mode; proof seam is the compact-mode fixture; no shared scarce proof resource is identified; no ordering constraint or serial tripwire is evidenced.
- Stateful boundary matrix:

  | Branch | Required behavior |
  |---|---|
  | Absent/initial | Resolve to the supported default and expose a valid boolean state. |
  | Current reusable | Preserve and return the current valid boolean; same-value writes are idempotent. |
  | Legacy/incompatible | Reject an invalid representation without corrupting or partially replacing durable state. |
  | Public access | Read and update through the supported settings path. |
  | Variants | Disabled and enabled. |
  | Lifecycle | absent → default, disabled ↔ enabled, valid → same valid value, failed update → prior valid value. |

### 2. `V-09-T2` — Add the timezone-display settings toggle

- Work-unit form: independently completable vertical behavior slice.
- Source Trace: `SRC-V09@1`; outcome; commitment C2; acceptance “each toggle has isolated proof”; scope “settings toggles”; proof “three isolated settings fixtures”; parent `P-909`.
- Desired behavior: a user can read, enable, disable, and re-read the timezone-display setting, and the observable timezone-display selection matches the persisted boolean value.
- Edge/error acceptance:
  - both supported boolean states can be selected and round-trip through the public settings path;
  - selecting the already-current state is idempotent;
  - an absent value resolves through the supported settings default rather than producing an invalid third state;
  - an invalid or failed update is rejected and does not partially change the durable setting;
  - compact-mode and digest-opt-in state remain unchanged.
- Proof lane: isolated timezone-display settings fixture exercising absent/default, off-to-on, on-to-off, repeated-value, and rejected-update behavior.
- Verification authority: the isolated timezone-display fixture.
- Verification evidence required: passing fixture results for every listed acceptance branch, including durable re-read and non-interference checks.
- Expected durable write scope: only the timezone-display setting value in the settings store.
- Scope fence: no compact-mode or digest-opt-in behavior; no unrelated settings UI/store changes; no parent-delivery work.
- Dependency state: open and ready; true blockers: `none`.
- Role/state: `enhancement`; `ready-for-agent`.
- Stable tracker order: 2.
- Parallel safety: safe to run independently of `V-09-T1` and `V-09-T3`; it has a distinct semantic setting, durable value, and isolated proof fixture.
- Execution profile: semantic owner Uma/settings; production writes limited to timezone display; proof seam is the timezone-display fixture; no shared scarce proof resource is identified; no ordering constraint or serial tripwire is evidenced.
- Stateful boundary matrix:

  | Branch | Required behavior |
  |---|---|
  | Absent/initial | Resolve to the supported default and expose a valid boolean state. |
  | Current reusable | Preserve and return the current valid boolean; same-value writes are idempotent. |
  | Legacy/incompatible | Reject an invalid representation without corrupting or partially replacing durable state. |
  | Public access | Read and update through the supported settings path. |
  | Variants | Disabled and enabled. |
  | Lifecycle | absent → default, disabled ↔ enabled, valid → same valid value, failed update → prior valid value. |

### 3. `V-09-T3` — Add the digest-opt-in settings toggle

- Work-unit form: independently completable vertical behavior slice.
- Source Trace: `SRC-V09@1`; outcome; commitment C3; acceptance “each toggle has isolated proof”; scope “settings toggles”; proof “three isolated settings fixtures”; parent `P-909`.
- Desired behavior: a user can read, enable, disable, and re-read the digest-opt-in setting, and the observable digest selection matches the persisted boolean value.
- Edge/error acceptance:
  - both supported boolean states can be selected and round-trip through the public settings path;
  - selecting the already-current state is idempotent;
  - an absent value resolves through the supported settings default rather than producing an invalid third state;
  - an invalid or failed update is rejected and does not partially change the durable setting;
  - compact-mode and timezone-display state remain unchanged.
- Proof lane: isolated digest-opt-in settings fixture exercising absent/default, off-to-on, on-to-off, repeated-value, and rejected-update behavior.
- Verification authority: the isolated digest-opt-in fixture.
- Verification evidence required: passing fixture results for every listed acceptance branch, including durable re-read and non-interference checks.
- Expected durable write scope: only the digest-opt-in setting value in the settings store.
- Scope fence: no compact-mode or timezone-display behavior; no unrelated settings UI/store changes; no parent-delivery work.
- Dependency state: open and ready; true blockers: `none`.
- Role/state: `enhancement`; `ready-for-agent`.
- Stable tracker order: 3.
- Parallel safety: safe to run independently of `V-09-T1` and `V-09-T2`; it has a distinct semantic setting, durable value, and isolated proof fixture.
- Execution profile: semantic owner Uma/settings; production writes limited to digest opt-in; proof seam is the digest-opt-in fixture; no shared scarce proof resource is identified; no ordering constraint or serial tripwire is evidenced.
- Stateful boundary matrix:

  | Branch | Required behavior |
  |---|---|
  | Absent/initial | Resolve to the supported default and expose a valid boolean state. |
  | Current reusable | Preserve and return the current valid boolean; same-value writes are idempotent. |
  | Legacy/incompatible | Reject an invalid representation without corrupting or partially replacing durable state. |
  | Public access | Read and update through the supported settings path. |
  | Variants | Disabled and enabled. |
  | Lifecycle | absent → default, disabled ↔ enabled, valid → same valid value, failed update → prior valid value. |

## Dependency graph and frontier

- Blocker edges: none.
- Cycle or contradictory-order audit: none.
- Stable order: `V-09-T1`, `V-09-T2`, `V-09-T3`.
- Ready frontier: `V-09-T1`, `V-09-T2`, `V-09-T3`.

All three items are open, ready-for-agent, unclaimed, and independently
executable. Their order is stable tracker order, not a blocker relation.

## Mutation read-back

The permitted fixture publication sequence created the three ordered items,
attached each to `P-909`, applied the source-authorized `enhancement` role,
applied `ready-for-agent`, and introduced no blocker edges. `get_item`
read-back verified each exact body, parent, role, unassigned state, and ready
state; `get_dependents` verified no dependents. The fixture observation was
`publication/read-back -> exact three-node graph`.

- Applied operations: setup and parent reads; three item creates; three parent
  links; three role applications; three ready-state applications; item and
  dependent read-backs for all three nodes.
- Failed or unknown operations: none.
- Observed tracker state: `P-909` has exactly the three children above, with no
  dependency edges and the truthful three-item frontier.
- Gaps: none.

## Safe continuation

Recommendation: `none`.

No top-level delivery of the parent was requested, and the settled source
explicitly excludes a parent-delivery campaign. Therefore the graph is
published and verified, but neither `$parallel-implement` nor `$implement` is
invoked or recommended.
