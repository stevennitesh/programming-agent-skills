# Published graph

## Evidence and source

- Result type: `published graph`
- Bounded source: `SRC-V08@1`
- Source owner: Tess, admin owner
- Parent: `P-808`
- Outcome: add independent CSV audit export and theme preview capabilities.
- Publication authority: publish the exact graph and recommend the explicitly requested parent-delivery owner without invoking it.
- Setup evidence: the fixture reports a compatible setup.
- Prior tracker state: `P-808` existed, explicit top-level delivery authority was recorded, and it had no children.
- Observed tracker state: publication and read-back returned the exact two-node independent graph described below.
- Gaps: none.

## Frozen commitment ledger

| Source entry | Disposition |
| --- | --- |
| C1 CSV audit export | Covered by `V08-1`. |
| C2 theme preview | Covered by `V08-2`. |
| Each capability has an isolated integration fixture | `V08-1` uses `audit_export`; `V08-2` uses `theme_preview`. |
| Scope: two independent admin capabilities | Preserved as two independently completable vertical behavior slices. |
| Exclusion: starting delivery during ticketing | Preserved; no implementation or worker dispatch was started. |
| Category: enhancement | Applied to both tickets as explicitly authorized. |
| Parent: P-808 | Applied to both tickets; the parent remains the intent owner. |

No commitment, scope boundary, exclusion, or proof obligation is left without a disposition.

## Ordered tickets

### 1. `V08-1` — CSV audit export

- Work-unit form: independently completable vertical behavior slice.
- Bounded slice: provide the CSV audit export admin capability.
- Source Trace: `SRC-V08@1` → outcome; commitment `C1`; acceptance “each capability has an isolated integration fixture”; scope “two independent admin capabilities”; proof `audit_export`; category `enhancement`; parent `P-808`.
- Desired behavior: an admin export request produces a CSV audit export from the audit data available to that request.
- Edge behavior: an empty audit-data input produces a valid empty export result rather than an unrelated capability failure.
- Error behavior: when export generation cannot complete, the operation reports failure and does not report a successful CSV export.
- Acceptance:
  - the CSV audit export is observable through the admin capability;
  - the isolated `audit_export` integration fixture proves successful export;
  - the fixture also proves the empty-input and generation-failure outcomes;
  - the capability can be verified without invoking or depending on theme preview.
- Relevant seams: admin export entry point, audit-data read seam, CSV generation boundary, and export response/artifact boundary.
- Expected durable write scope: only the requested CSV response or export artifact; no audit-record, theme, configuration, or other production-state write.
- Scope fence: includes only CSV audit export behavior and its isolated fixture; excludes theme preview, unrelated audit administration, parent delivery, and implementation of any other capability.
- Dependency state: true blockers `none`.
- Stable tracker order: 1.
- Proof lane: isolated `audit_export` integration fixture.
- Verification authority: the source packet’s acceptance and proof declarations, owned by Tess.
- Verification evidence: a passing isolated fixture demonstrating the success, empty-input, and failure branches at the integration boundary.
- Parallel-safety judgment: safe to execute in parallel with `V08-2`; the fixture states that the slices are semantically, write, proof, and resource isolated.
- State-boundary matrix: `not applicable` — the slice derives an export from request-visible audit data and makes no production-state lifecycle transition. The generated response/artifact is its observable output, not reusable application state.
- Role: `enhancement`.
- State: `ready-for-agent`.
- Assignee: unassigned.

Execution profile:

- Semantic ownership: CSV audit export only.
- Expected production writes: generated CSV response/artifact only; no shared application-state writes.
- Proof seam and scarce resources: `audit_export`; no shared or scarce proof resource is identified.
- Ordering: tracker order 1; no predecessor outcome is consumed.
- Serial tripwires: none identified.
- Independence evidence: explicit fixture context says semantic, write, proof, and resource isolation from theme preview.

### 2. `V08-2` — Theme preview

- Work-unit form: independently completable vertical behavior slice.
- Bounded slice: provide the theme preview admin capability.
- Source Trace: `SRC-V08@1` → outcome; commitment `C2`; acceptance “each capability has an isolated integration fixture”; scope “two independent admin capabilities”; proof `theme_preview`; category `enhancement`; parent `P-808`.
- Desired behavior: an admin preview request renders a preview for the supplied theme input.
- Edge behavior: an input that cannot produce a preview yields an explicit no-preview outcome rather than a misleading successful preview.
- Error behavior: when preview rendering cannot complete, the operation reports failure and does not report a successful preview.
- Acceptance:
  - theme preview is observable through the admin capability;
  - the isolated `theme_preview` integration fixture proves successful preview rendering;
  - the fixture also proves the no-preview and rendering-failure outcomes;
  - the capability can be verified without invoking or depending on CSV audit export.
- Relevant seams: admin preview entry point, theme-input boundary, preview renderer, and preview response boundary.
- Expected durable write scope: none; preview output is ephemeral and this slice does not persist theme selection, audit data, configuration, or other production state.
- Scope fence: includes only theme preview behavior and its isolated fixture; excludes theme persistence or activation, CSV audit export, parent delivery, and unrelated theme administration.
- Dependency state: true blockers `none`.
- Stable tracker order: 2.
- Proof lane: isolated `theme_preview` integration fixture.
- Verification authority: the source packet’s acceptance and proof declarations, owned by Tess.
- Verification evidence: a passing isolated fixture demonstrating the success, no-preview, and rendering-failure branches at the integration boundary.
- Parallel-safety judgment: safe to execute in parallel with `V08-1`; the fixture states that the slices are semantically, write, proof, and resource isolated.
- State-boundary matrix: `not applicable` — preview rendering is ephemeral and this slice performs no persisted theme lifecycle transition.
- Role: `enhancement`.
- State: `ready-for-agent`.
- Assignee: unassigned.

Execution profile:

- Semantic ownership: theme preview only.
- Expected production writes: none.
- Proof seam and scarce resources: `theme_preview`; no shared or scarce proof resource is identified.
- Ordering: tracker order 2; no predecessor outcome is consumed.
- Serial tripwires: none identified.
- Independence evidence: explicit fixture context says semantic, write, proof, and resource isolation from CSV audit export.

## Dependency graph and frontier

```text
P-808
├── V08-1  CSV audit export     blockers: none
└── V08-2  Theme preview        blockers: none
```

- Blocking edges: none.
- Cycle audit: no cycle.
- Missing-edge audit: none; neither ticket consumes an outcome from the other.
- Contradictory-order audit: none. Stable tracker order does not create a blocker.
- False-readiness audit: none; both tickets have complete acceptance, proof, scope, verification, and dependency facts.
- Ready frontier, in tracker order: `V08-1`, `V08-2`.
- Frontier evidence: both items are open, `ready-for-agent`, unassigned, and have no true blockers.

## Mutation read-back

Authorized fixture mutations were applied in this order:

1. Created `V08-1` with the frozen body above.
2. Created `V08-2` with the frozen body above.
3. Set parent `P-808` on both items.
4. Applied the authorized `enhancement` role to both items.
5. Set both items to `ready-for-agent`.
6. Refetched both items and affected relationships.

Read-back verified:

- both created bodies exactly match the frozen ticket bodies;
- `P-808` is the parent of exactly `V08-1` and `V08-2`;
- neither item has a blocker edge or dependent created by this graph;
- both items have role `enhancement`;
- both items are unassigned and in `ready-for-agent`;
- the resulting frontier is exactly `V08-1`, then `V08-2`;
- no failed, ambiguous, unknown, or mismatched operation remains.

## Safe continuation

Exact next owner recommendation: `$parallel-implement`.

Reason: this invocation explicitly requested top-level delivery of verified parent `P-808`, its exhaustive graph is non-empty, and both ready items have evidenced semantic, write, proof, and resource independence. Stop without invoking that owner.
