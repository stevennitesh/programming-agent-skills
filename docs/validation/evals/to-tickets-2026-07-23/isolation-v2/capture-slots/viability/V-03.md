# Source-gap packet

- Source: `SRC-V03@1`
- Source owner: Ravi, notifications owner
- Parent: `P-303`
- Evidence: the source acceptance explicitly leaves channel scope unset: whether a project mute applies to email only or to both email and in-app notifications. Ravi withholds publication until that scope is settled.
- Setup evidence: `get_setup -> compatible`; `get_parent P-303 -> exact parent`.

## Blocking decision

Ravi must settle exactly which delivery channels a project notification mute suppresses:

1. email only; or
2. both email and in-app notifications.

This decision changes the persistence contract and observable acceptance, so slicing or publishing before it is resolved would invent product behavior.

## Affected slices

- `C1 store mute preference`: the persisted meaning and any channel representation cannot be finalized until the mute's channel scope is settled.
- `C2 suppress matching delivery`: the delivery paths to suppress, the unaffected-path edge cases, and the integration-fixture assertions cannot be finalized until the same decision is settled.

The organization-wide mute exclusion remains unchanged. No other source gap is identified.

## Tracker state

`P-303` exists with no children. No item was created, related, labeled, or moved. The fixture mutation log remains empty.

## Exact safe continuation

Return the channel-scope decision and both affected slices to Ravi. After Ravi records one of the two choices in the settled source acceptance, rerun To Tickets against a new source revision, preserving `P-303` as the parent and using the notification delivery integration fixture as the proof seam.

Recommendation: `none`.
