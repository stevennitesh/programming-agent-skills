### A

Review owns this ordinary WIP diff. Review `src/a.py` from fixed snapshot `wip-A` against base `abc123`.

### B

Hand the high-risk local PR `PR-42`, fixed at snapshot `tree-B` against base `def456`, to Convergent PR Review and stop. Do not run a competing ordinary Review pass.

### C

Recommend Audit Codebase for the immutable baseline `repo@789abc` and stop. Do not run an ordinary Review pass.
