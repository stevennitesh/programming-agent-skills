Route the complete packet to Convergent PR Review and stop. `PR-42` is a high-risk local PR diff, so Review must not run a competing ordinary pass on any item in the packet. The ordinary `src/a.py` WIP diff would otherwise belong to Review, while the immutable `repo@789abc` repository-baseline audit would otherwise require Audit Codebase, but this mixed packet must first be resolved under a single owner.
