## Standards

### Findings

- **P2 — Retry exhaustion is no longer logged** (`client.py:40`). Changing `log=true` to `log=false` suppresses the required exhaustion log on a directly reachable retry path, violating S-LOG and reducing operability when retries fail.

### Summary

Standards review fails because the reachable retry-exhaustion path no longer emits its required log.

## Spec

### Findings

- **P1 — Required retries are disabled** (`client.py:40`). Changing `count=3` to `count=0` prevents the three retries required by AC-RETRY on a directly reachable path, so transient failures are no longer handled as specified.

### Summary

Spec review fails because the implementation performs zero retries instead of the required three.
