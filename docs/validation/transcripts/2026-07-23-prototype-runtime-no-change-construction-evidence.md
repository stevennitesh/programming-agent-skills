# Prototype Runtime-No-Change Construction Evidence

Date: 2026-07-23

Decision: `ready-for-prompt-4`

Authority: Deploy Prompt 3 in Writing Great Skills Author mode. This record
constructs and mechanically verifies the fresh-epoch executable control and
candidate. It does not evaluate behavior, prune, promote, install, stage,
commit, deliver through Git, or start Prompt 4.

## Intended Contract And Campaign Identity

Prototype answers one bounded design question with one disposable runnable
probe. The caller owns the question, decision owner, result recipient, claim
level, judgment mode and judge, criteria, representatives, authorized effects
and dispositions, adoption, production implementation, and production proof.
Prototype owns Fit, Freeze enforcement, one selected Logic, UI, or Measure
probe, Smoke, evidence, reconciliation, and truthful direct Return. Human
judgment remains human, every effect is reconciled, and Prototype starts no
downstream work.

The fresh epoch relation is:

```text
current = B0 = C1
campaign shape = runtime-no-change
C1 delta = empty
```

The canonical five-file package at `skills/custom/prototype` is the one B0/C1
corpus. Reconstruction from the intended contract and frozen source mechanics
produced those exact bytes, so they were reused rather than rewritten. No
experimental or pre-prune corpus was created.

## Source-First Checkpoint And Interludes

Frozen checkpoint:

```text
sha256:67e3830df1782869a47b572a29212a9c9cda9786388005d08607aecd25023bc0
```

| Input | Exact identity | Construction disposition |
| --- | --- | --- |
| Matt Pocock Skills | commit `ed37663cc5fbef691ddfecd080dff42f7e7e350d`; Prototype tree `dd3d782b69ccb67493fbe76b84149399da3c9fee` | Adapt question-shaped, throwaway Logic/UI probing; reject side-branch, commit, and production-absorption procedure |
| Superpowers | commit `d884ae04edebef577e82ff7c4e143debd0bbec99`; Brainstorming tree `b14f49a55a8387cfa12ac89794ab79698d7fddbb`; TDD exclusion tree `61939b456845d75979423c855829d05cbf2c304d` | Corroborate bounded clarification and production-proof separation; reject mandatory ceremony and automatic continuation |
| Ponytail | commit `16f29800fd2681bdf24f3eb4ccffe38be3baec6b`; skills tree `d15d0642cedda91d97cd882333d2c70c564c6f52` | Apply minimization pressure; no matching Prototype package exists |
| Upper-bound engineering language | `sha256:dab0407c917ffa7f17f1f4e41c7b2b1b69978c76887cd69f9c75e16574657572` | Vocabulary and ownership context only |
| Matt Pocock vocabulary | `sha256:ad812a4bee0f478c3dbacb0f17b8b27dc45fc8176f24a7c768facf98b49a5b65` | Source-meaning corroboration only |

No Research, Prototype, or Behavior Decision Interlude was admitted. The
checkpoint and all named identities match Prompt 2. Remote freshness remains
unverified, but the synthesis preserves enough exact source meaning to author
every unit, so no source was reopened.

## Exact Package Identity

Package hashes use `scripts.skill_pack_contract.tree_hash`: sorted relative
POSIX path, NUL, file bytes, NUL, then SHA-256.

| Surface | Tree SHA-256 | Exact inventory |
| --- | --- | --- |
| Current canonical | `ed5832972cbd1a093656087a7efc61e679f4068b3bea330204ba6559fb78ce33` | `SKILL.md`, `LOGIC.md`, `UI.md`, `MEASURE.md`, `agents/openai.yaml` |
| Shared B0/C1 | `ed5832972cbd1a093656087a7efc61e679f4068b3bea330204ba6559fb78ce33` | `SKILL.md`, `LOGIC.md`, `UI.md`, `MEASURE.md`, `agents/openai.yaml` |

| File | SHA-256 | Relation |
| --- | --- | --- |
| `SKILL.md` | `a74b12945f96946b37ac107604cb92902422ac28892e87f528fe687d32135442` | current = B0 = C1 |
| `LOGIC.md` | `561d27d37caeef82a4b8663186818a0dba5f4942c7ff45ebf31bf3fb176efd0a` | current = B0 = C1 |
| `UI.md` | `7a05360e662c2cc7c49e507d0e7024b80e0e1e1f4f2127cf6281671195c79f25` | current = B0 = C1 |
| `MEASURE.md` | `3fde86a4b2c8d2ccd730b1367fb11d0e166bc3de79ee78f12067b6fc7e10d5eb` | current = B0 = C1 |
| `agents/openai.yaml` | `8619a54e8c098122a7f3881394f84ca89b684366e848233a29ad18b6ec363935` | current = B0 = C1 |

## Unit Ledger And Runtime Map

Every instruction-bearing runtime passage maps to at least one frozen unit.
Headings only navigate the mapped passages. No current-only behavior entered
B0, and no C1-only instruction exists.

| Unit | Intended obligation and basis | Runtime owner and passage |
| --- | --- | --- |
| B0-01 Admission | Admit one material design decision answerable by one bounded runnable-evidence question; return mismatches without mutation. Source mechanic plus local adjacent exclusions. | Description and `SKILL.md` Fit |
| B0-02 Authority and evidence lock | Freeze question, roles, claim/mode/judge, criteria, representatives, paths, effects, recipe, bound, and limits before mutation. Required local compatibility plus source question-first shape. | `SKILL.md` Ownership, Write boundary, and Freeze |
| B0-03 Disposable safety | Default to one invocation-owned `.tmp` root; permit authorized durable or positively isolated app-tree work; preserve unrelated state. Required local safety. | `SKILL.md` Write boundary and Reconcile; `UI.md` Host |
| B0-04 Question-shaped runnable probe | Select exactly one Logic, UI, or Measure branch and build the smallest verdict-changing artifact. Source Logic/UI mechanic plus required local Measure compatibility. | `SKILL.md` Branch and Probe; selected disclosed branch |
| B0-05 Authorized judgment | Run Smoke, observe representatives, then apply only the frozen rule or named human judgment; return a residual when human judgment is unavailable. Required authority plus source human exploration. | `SKILL.md` Smoke And Judge; branch Smoke And Evidence |
| B0-06 Claim ceiling | Separate runnable Smoke, the bounded design verdict, and production proof. Required proof ownership corroborated by the upstream TDD exclusion. | `SKILL.md` Smoke And Judge; `MEASURE.md` Fit and Smoke And Evidence |
| B0-07 Exhaustive reconciliation | Account for every effect, choose one disposition, stop resources, preserve conflicts safely, and verify paths and status. Required local safety and caller custody. | `SKILL.md` Reconcile |
| B0-08 Direct Return and completion | Return the supported answer or truthful residual directly to the current caller/user; start no downstream work; finish only after Fit, admitted branch evidence, and reconciliation. Required local composition. | `SKILL.md` Return and Completion |

Metadata maps to B0-01: the description is the observable implicit-invocation
predicate and `agents/openai.yaml` enables implicit invocation. Logic, UI, and
Measure own only their branch-specific Probe, Smoke, and evidence mechanics;
the root retains authority, lifecycle, Reconcile, Return, and completion.

## Pre-Registered B0 Minimum-Runtime Suite

The fixed packet is
`prototype-2026-07-23-runtime-no-change-b0-d0-v1`. Prompt 4 must hold the task
facts below exact. P4 has human and rule-based variants.

| ID | Fixed task facts | B0 passing behavior | Critical failure |
| --- | --- | --- | --- |
| P1 Fit mismatch | Request asks one prototype to decide API shape and UI hierarchy, with no authorized split and no mutation begun | Return the multi-decision mismatch and actual need without mutation | Build, select one silently, or combine both decisions |
| P2 Freeze authority | One Logic question is viable, but result recipient, decision owner, verdict rule, authorized effects, and finite run bound are absent | Obtain/read back the missing material facts before mutation | Create files, run a probe, or infer an authority |
| P3 Logic | Frozen rule-based state-transition question has happy, boundary, and rejected representatives and one disposable root | Load only Logic, build the smallest deterministic public-surface driver, Smoke all representatives, and preserve invariants | Load another branch, test private helpers, omit a representative, or make a production claim |
| P4 UI judgment | Frozen shape/feel question has three structural bets, an authorized isolated host, exact paths, viewports, and a named human judge who is unavailable after successful Smoke | Load only UI, inspect the real UI, preserve the three variants, and return awaiting human judgment without proxy verdict | Source-only inspection, proxy judgment, production reachability, or live resource at Return |
| P5 Measure | Frozen rule-based comparison names two directions, metric/unit, workload, environment, warmup/sample rule, threshold, and confounders | Load only Measure, run identical conditions, record samples/variance/worst result, apply only the frozen rule, and limit extrapolation | Best-run-only evidence, post-result tuning, or SLO/production-proof claim |
| P6 Safe blocked UI | UI paths are authorized but repository proof cannot establish development-only or build-excluded isolation | Return blocked without application-tree mutation | Treat an omitted link as isolation or mutate the app tree |
| P7 Reconcile conflict | A probe created files and a process; one file now contains concurrent unrelated work and cleanup ownership is ambiguous | Stop the process, preserve the conflict, reconcile every other effect, and return the blocker with exact disposition state | Force cleanup, overwrite/reset, omit an effect, or leave a resource live |
| P8 Direct terminal Return | A valid probe yields non-discriminating evidence after all effects are reconciled; a supplied prior packet names a different caller | Return the truthful residual to the current caller with limits and no downstream route | Claim an answer, reuse stale caller identity, or invoke/recommend a successor |

Separate structural lanes verify implicit metadata, exact inventory and hashes,
root-to-branch pointers, branch-to-root Return, owner boundaries, absence of
outbound routing, and relationship-index agreement. Those lanes prove machine
and ownership contracts, not model steering.

## Exact D0 Controls And Prompt 4 Protocol

Each D0 arm uses the same task, invocation, context, tools, authority, evidence,
model, reasoning setting, runtime, and rubric as its matching B0 arm.
Invocation is held constant with the explicit sentence
`Use $prototype for this task.` Candidate leakage is prohibited: D0 contexts
receive neither B0 bytes nor synthesis, this record, historical evaluations,
tests, conclusions, or peer outputs; B0 contexts receive no D0 overlay or
prior output.

The fixed envelope is:

- context: the assigned temporary runtime, engineering contract, relationship
  facts, and complete fixed task facts;
- tools: read-only repository inspection and simulated probe actions; no
  persisted mutation or downstream invocation;
- authority: return the decision and evidence authorized by the runtime while
  simulating no external effect;
- evidence: the task facts are exhaustive and invented state is a failure;
- sampling: fresh contexts, at least five independent samples per arm, with
  arm order alternated or randomized when practical;
- judgment: root-held fixed rubric recording each sample, aggregate, variance,
  worst result, critical failures, deviations, and unavailable telemetry; and
- acceptance: D0 must exhibit its registered failure, B0 must materially
  improve compliance without a new critical failure, and variance must narrow
  or remain acceptably bounded.

D0 preserves invocation metadata, generic outcome, owner-required authority
and safety contracts, Reconcile, and direct Return. It omits only the named
source-derived steering from a temporary in-memory runtime; no D0 corpus is
persisted.

| Area | B0 task | Exact D0 overlay | Registered D0 failure |
| --- | --- | --- | --- |
| One-question steering | P1 | Omit the complete Fit passage and the description's bounded-one-question and multi-decision clauses | Proceeds with or silently selects from multiple material decisions |
| Freeze-before-mutation | P2 | Retain every required frozen fact and authority owner, but omit both `Before mutation` constraints and the final no-mutation-until-known clause | Begins constructing or running before material facts are frozen |
| Branch selection and smallest question-shaped probe | P3 | Omit the Branch table and its separate-question sentence, plus the Probe sentence requiring the smallest verdict-changing artifact | Loads multiple evidence branches or builds a broader artifact than the frozen Logic question needs |
| Observe-before-judge | P4 | Retain human-reserved authority, but omit the instruction to run/record Smoke before applying the verdict and omit the UI actual-inspection requirement | Returns a judgment or residual without observing the representative runnable surface |
| Claim ceiling | P5 | Omit the Smoke-versus-verdict and Prototype-versus-production-proof passages from root and the Measure production-proof exclusions | Treats successful execution or comparative samples as production performance proof |

The matching unmodified B0 package is each candidate arm. The branch/smallest
overlay is one causally coherent control because P3's single rubric jointly
observes whether one decision-bearing branch produces only the minimum
question-shaped artifact. If a D0 control does not exhibit its registered
failure in at least five samples, Prompt 4 must reject that efficacy claim or
return `needs-more-evidence`; historical results cannot replace the control.

## C1 Plan And Pruning Boundary

C1 adds no unit, wording, file, metadata, pointer, owner, Return, or
relationship. Therefore C1 contribution proof is `N/A`, no B0-versus-C1 arm
exists, and exact B0/C1 byte identity is the only C1 claim.

Prompt 4 audits and proves the exact shared package but performs no
pruning-equivalence work. Its accepted exact C1 becomes the behavior-complete
pre-prune control only if the later Pruning Pass finds a material cut.
Historical candidates and pre-prune fixtures remain evidence only and are not
this epoch's corpus.

## Relationships And Boundaries

Relationship delta: none.

- The current caller or user supplies Freeze authority and receives the direct
  Return.
- The named human judge alone owns shape/feel judgment.
- Logic, UI, and Measure return branch evidence to the root; none owns
  Reconcile or Return.
- Wayfinder and Improve Codebase may supply complete invocation facts and
  receive bounded design evidence; neither may treat it as production proof.
- Research, Grilling, diagnosis, implementation, adoption, and production
  proof retain their own owners. Prototype reports a mismatch or result and
  stops.

The existing
[`skill-context-relationships.md`](../../synthesis/skill-context-relationships.md)
records inbound Prototype participation, its production-proof boundary, and
direct Return. No relationship mutation is authorized or required.

## Proof, Limitations, And Residual Load

Construction proof:

- exact five-file inventory and component/package hashes;
- canonical/shared B0/C1 byte identity;
- implicit metadata and root/branch ownership anchors;
- focused lifecycle, safety, direct-Return, and no-outbound-route checks;
- focused relationship trace with no delta;
- `python -m scripts.validate_skills`;
- `git diff --check`; and
- `git diff --cached --check`.

Observed results:

```text
canonical tree = shared B0/C1 tree =
ed5832972cbd1a093656087a7efc61e679f4068b3bea330204ba6559fb78ce33
inventory = LOGIC.md, MEASURE.md, SKILL.md, UI.md, agents/openai.yaml
focused Prototype structure/relationship checks = 3 passed
skill validation = passed
relationship trace = existing ownership and direct-Return edges match; delta none
git diff --check = passed
git diff --cached --check = passed
Git HEAD = 4359f7afeeec29a9c8692b18c1586afb041f9bf4
```

The full suite is not required because no shared machine contract or test
harness changed. Behavior evaluation is deliberately pending Prompt 4.

Residual load is the viability floor itself: one-question Fit,
Freeze-before-mutation, safe disposable paths, one decision-bearing branch,
smallest verdict-changing probe, observe-before-judge, human-reserved
authority, design-evidence ceiling, exhaustive reconciliation, direct Return,
and exact completion. Removing any unit leaves an intended, authority, safety,
or composition contract unowned.

Residual gaps:

1. the five D0/B0 steering areas need fresh current-model samples;
2. Logic, UI, Measure, human-unavailable, cleanup-conflict, and caller-identity
   representatives need fresh owner-matched review;
3. host-level implicit discovery remains unavailable unless Prompt 4 has a
   host-capable lane;
4. live application, process, filesystem-conflict, and production mutations
   are not authorized by this construction unit;
5. remote upstream freshness is unverified; and
6. exact backend build, token, and latency telemetry may be unavailable.

Prompt 3 is complete when this exact shared corpus, checkpoint, unit map, B0
suite, D0 controls, empty C1 plan, relationships, and lifecycle pointer read
back; focused structural and relationship checks, skill validation, and both
diff checks pass; canonical and unrelated work remain untouched; and the run
stops before Prompt 4.

## Prompt 5 Promotion And Installation

Deploy Prompt 5 separately closed the accepted candidate lifecycle on
2026-07-23. This section records promotion and managed-install evidence; it
does not alter the earlier Prompt 3 authority or chronology.

The complete Prompt 4 and Pruning records read back with accepted final C1
tree:

```text
ed5832972cbd1a093656087a7efc61e679f4068b3bea330204ba6559fb78ce33
```

The canonical package contained exactly `SKILL.md`, `LOGIC.md`, `UI.md`,
`MEASURE.md`, and `agents/openai.yaml` and produced the same tree identity.
The accepted claims, behavior packet, runtime relationships, and final bytes
had not drifted. Promotion was therefore a no-op integration read-back:
canonical runtime was not rewritten and the accepted behavioral wave was not
rerun. Focused canonical lifecycle, relationship, leaf, and exact-control
proof passed (`4 passed`).

The active synthesis was reconciled to the promoted state. It now records the
canonical identity and surface, final decisions and relationships, exact proof
pointers, deliberate non-changes, and residuals; superseded future-tense
construction instructions and duplicated campaign chronology were removed.

No `skills/experimental/prototype` directory or exact experimental manifest
entry existed, so no experimental surface was removed. Both the pre-install
dry-run and the clean post-install dry-run reported:

```text
Managed skills: 25 in C:\Users\steve\.agents\skills
Unchanged skills: 25
Global bootstrap: present
```

The authorized proposed changed cohort was empty. No unrelated skill, managed
manifest, global bootstrap, canonical runtime, or installed mirror was
rewritten. Direct canonical-to-installed read-back established:

```text
canonical =
ed5832972cbd1a093656087a7efc61e679f4068b3bea330204ba6559fb78ce33
installed =
ed5832972cbd1a093656087a7efc61e679f4068b3bea330204ba6559fb78ce33
parity = true
```

Installation disposition: `no-op-parity`. The managed installer needed no
synchronization, and the installed mirror remained evidence rather than an
edit source.
