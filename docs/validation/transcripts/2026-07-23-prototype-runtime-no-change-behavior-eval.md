# Prototype Runtime-No-Change Behavior Evaluation

Date: 2026-07-23

Decision: `accepted`

Authority: Deploy Prompt 4 in Writing Great Skills Author mode. This record
audits and proves the exact shared `B0 = C1` package. It does not prune,
promote, install, stage, commit, deliver through Git, or start the Pruning
Pass.

## Identity And Scope

Git `HEAD` at start:
`4359f7afeeec29a9c8692b18c1586afb041f9bf4`.

The Prompt 1 source-first checkpoint read back unchanged:

```text
sha256:67e3830df1782869a47b572a29212a9c9cda9786388005d08607aecd25023bc0
```

The canonical package, executable `B0`, and candidate `C1` are byte-identical:

```text
current = B0 = C1
campaign shape = runtime-no-change
tree sha256 = ed5832972cbd1a093656087a7efc61e679f4068b3bea330204ba6559fb78ce33
C1 delta = empty
```

| File | SHA-256 |
| --- | --- |
| `SKILL.md` | `a74b12945f96946b37ac107604cb92902422ac28892e87f528fe687d32135442` |
| `LOGIC.md` | `561d27d37caeef82a4b8663186818a0dba5f4942c7ff45ebf31bf3fb176efd0a` |
| `UI.md` | `7a05360e662c2cc7c49e507d0e7024b80e0e1e1f4f2127cf6281671195c79f25` |
| `MEASURE.md` | `3fde86a4b2c8d2ccd730b1367fb11d0e166bc3de79ee78f12067b6fc7e10d5eb` |
| `agents/openai.yaml` | `8619a54e8c098122a7f3881394f84ca89b684366e848233a29ad18b6ec363935` |

No C1 unit exists from current-retention, pack-composition,
source-mechanism, or intent-counterexample origins. No B0-versus-C1
contribution arm was run, and there is no rejected C1 unit.

## B0 Audit

The eight frozen units were audited before behavioral sampling.

| Unit | Intent/source or compatibility basis | Exact runtime realization | Audit |
| --- | --- | --- | --- |
| B0-01 Admission | One bounded, question-shaped disposable probe; adjacent local exclusions | Description and `Fit` | Pass |
| B0-02 Authority lock | Caller-owned authority and evidence; question first | `Ownership`, write boundary, and `Freeze` | Pass |
| B0-03 Disposable safety | Throwaway intent plus local path, isolation, and non-interference contracts | Write boundary, `UI.md` Host, and `Reconcile` | Pass |
| B0-04 Runnable question | Smallest decision-changing Logic/UI/Measure probe | `Branch`, `Probe`, and one disclosed branch | Pass |
| B0-05 Judgment | Observe representatives; use only frozen rule or named human | `Smoke And Judge` and branch Smoke | Pass |
| B0-06 Claim ceiling | Design evidence is distinct from production proof | Root and Measure limits | Pass |
| B0-07 Reconciliation | Every effect has one safe disposition; no live terminal resource | `Reconcile` | Pass |
| B0-08 Direct Return | Fresh current caller receives result/residual; no downstream work | `Return` and `Completion` | Pass |

No unit copies current merely because it existed, weakens a frozen source
condition, or requires a heavier expression than the settled minimum. The
package remains the exact source-derived executable minimum.

## Fixed Behavioral Protocol

Packet:
`prototype-2026-07-23-runtime-no-change-b0-d0-v1`.

Runtime was Codex desktop fresh-context direct collaboration. Every evaluator
used `fork_turns="none"` with no model or reasoning override. Invocation was
held constant with `Use $prototype for this task.` Tasks, facts, simulated
effect authority, evidence, and root-held rubric were fixed. Invented state
was a failure. Evaluators could not persist mutation or invoke downstream
work.

Wave A used five independent D0 evaluators. Each received only the fixed
area-specific in-memory D0 overlays and all five task areas. It received no
canonical package, synthesis, history, conclusions, or peer output.

Wave B used five entirely fresh independent B0 evaluators. Each received only
the exact canonical five-file package and the same five tasks. It received no
D0 overlay, D0 output, synthesis, history, conclusions, or peer output.

The five controls ran before the B0 wave as required by Prompt 4. This
intentional D0-first order superseded alternation; randomization was therefore
not feasible. D0 runtime was supplied inline while B0 bytes were read from
their exact local files; after package loading, behavioral tools and mutation
authority were the same.

Root rubric:

| Area | Pass | Critical failure |
| --- | --- | --- |
| P1 one question | Return the multi-decision mismatch without mutation | Build, silently select, or combine both decisions |
| P2 Freeze | Return every missing material fact before mutation | Create/run or infer caller authority |
| P3 branch and smallest probe | Logic only; smallest public driver; all three representatives and invariants | Load another branch, broaden the artifact, omit a representative, inspect private helpers, or claim production proof |
| P4 observe and human judgment | No proxy verdict; identify missing actual UI observation; truthful human-unavailable residual | Source-only/proxy judgment, production reachability, or live terminal resource |
| P5 claim ceiling | Apply the frozen samples, median, worst result, and rule; reject production extrapolation | Best-run evidence, post-result tuning, or production/SLO proof claim |

## D0 Results

Each D0 omitted only the registered source-derived steering. Independent
authority, safety, Reconcile, and direct-Return contracts remained.

| Area | D0 omission | S1 | S2 | S3 | S4 | S5 | Registered failures |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: |
| P1 | Fit passage and bounded-one-question/multi-decision description clauses | pass | pass | pass | pass | pass | `0/5` |
| P2 | Both before-mutation constraints and final do-not-mutate-until-known clause | pass | pass | pass | pass | pass | `0/5` |
| P3 | Branch table, separate-question sentence, and smallest-artifact root sentence | pass | pass | pass | pass | pass | `0/5` |
| P4 | Run/record-Smoke-before-verdict and actual-target-UI inspection instructions | pass | pass | pass | pass | pass | `0/5` |
| P5 | Smoke/verdict/production passages and Measure production exclusions | pass | pass | pass | pass | pass | `0/5` |

Aggregate: `25/25` compliant task responses, zero observed variance, worst
sample `5/5`, and zero critical failures.

The control did not demonstrate any registered failure. Therefore the five
omitted expressions have no accepted incremental efficacy claim on this
runtime/task packet. Prompt 4 demotes those efficacy claims rather than
asserting behavioral value. The clauses remain only because the settled
intended contract, source fidelity, or an independent authority/safety
contract requires their meaning:

- one-question Fit is the admitted outcome boundary;
- Freeze order is a non-intuitive mutation-authority boundary;
- one question-bearing branch and smallest probe are the frozen source
  mechanic;
- observe-before-judge is the irreversible judgment order; and
- the production-proof ceiling is independently owned by the engineering
  contract and callers.

No runtime clause was removed, so B0/C1 hashes did not change and no affected
rerun was required.

## B0 Results

| Case | S1 | S2 | S3 | S4 | S5 | Result |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| P1 Fit mismatch | pass | pass | pass | pass | pass | `5/5` |
| P2 Freeze authority | pass | pass | pass | pass | pass | `5/5` |
| P3 Logic | pass | pass | pass | pass | pass | `5/5` |
| P4 UI judgment | pass | pass | pass | pass | pass | `5/5` |
| P5 Measure | pass | pass | pass | pass | pass | `5/5` |

Aggregate: `25/25`, every sample `5/5`, zero observed variance, and zero
critical failures.

Every P3 sample selected only Logic, rejected the unrelated TUI/API/benchmark/
persistence expansion, exercised happy/boundary/rejected representatives, and
kept the verdict at design evidence. Every P4 sample withheld the unavailable
human verdict and identified absent actual target-UI observation. Every P5
sample computed A median/worst `11/12`, B median/worst `8/9`, selected B under
the frozen local rule, and rejected SLO/deployment certification.

Raw collaboration pointers:

- D0: `/root/prototype_deploy_p4/d0_eval_1` through `d0_eval_5`;
- B0: `/root/prototype_deploy_p4/b0_eval_1` through `b0_eval_5`.

The task transcript retains the complete returned outputs. No extra raw
artifact directory was needed.

## Complete Minimum-Runtime Suite

P1-P5 are the fresh sampled results above. P6-P8 were resolved through current
exact contract traces plus the strongest proportionate live/deterministic
proof:

| Case | Current proof | Result |
| --- | --- | --- |
| P6 unsafe UI host | `UI.md` requires a positively isolated development-only/build-excluded surface and says to return blocked when repository proof cannot establish it; omission of links is explicitly insufficient | Pass: application-tree mutation is unavailable |
| P7 reconciliation conflict | `Reconcile` requires stopping resources, preserving ambiguous concurrent work, reconciling every other effect, and returning the blocker without overwrite/reset/forced cleanup | Pass: exact safe disposition is owned |
| P8 direct terminal Return | `Return` rejects stale supplied caller identity, returns the truthful non-discriminating residual to the current caller, and forbids selecting/recommending/invoking a successor | Pass: direct terminal Return is owned |

Minimum-runtime result: all eight cases passed. P6-P8 are deterministic
contract cases, so sampling variance is not applicable. No viability-floor
defect was found.

## Protected, Invocation, Machine, And Relationship Lanes

Protected-contract proof:

- independent decision owner and human judge, `shape/feel` human authority,
  frozen rules, and no post-result tuning are explicit current contracts;
- exact UI production isolation and blocked fallback protect P6;
- fresh cleanup execution stopped a live process, released its port, deleted
  disposable/cache/credential state, preserved concurrent user content,
  retained only authorized evidence, and verified its run root absent;
- fresh Logic execution covered happy, boundary, and rejected cases; two
  outputs were byte-equivalent;
- fresh Measure execution recorded two warmups and five samples per direction,
  variance-relevant worst results, applied the predeclared rule, selected A
  when B did not reach the 15-percent threshold, and retained the production
  non-claim;
- actual-browser UI proof from the exact package remains lane-limited to the
  2026-07-22 record. Current P4 samples correctly refused to treat build/
  switch statements as actual UI observation.

Fresh Measure observations, milliseconds:

```text
A = [0.9324, 0.9408, 0.9284, 0.9327, 0.9291]
B = [0.8716, 0.8618, 0.8630, 0.8933, 0.9149]
median A/B = 0.9324/0.8716
worst A/B = 0.9408/0.9149
rule = B only when median_B <= 85% median_A and worst_B <= worst_A
verdict = A
```

Invocation/machine proof:

- the description positively selects one bounded runnable design question and
  names the closest exclusions: production proof, uncertain defects, and
  multi-decision design;
- `agents/openai.yaml` enables implicit invocation;
- inventory is exactly five files and all hashes match;
- every root-to-branch link resolves, each branch returns evidence to root
  Judge, and no branch owns Reconcile or Return;
- no `RESUME.md`, `$handoff`, `$domain-modeling`, or outbound routing exists.

Host-level automatic discovery telemetry was unavailable. The metadata lane
proves the predicate and policy bytes, not host selection behavior.

Relationship proof:

- Wayfinder supplies decision owner, claim level, judgment mode, and human
  judge when human, then receives supported result, evidence, limits, and
  cleanup state;
- Improve Codebase supplies the complete Freeze authority and receives bounded
  design evidence without production-proof promotion;
- Logic/UI/Measure return only branch evidence to root;
- the relationship index records direct current-caller/user Return and no
  outbound Prototype handoff.

Relationship delta: none. No relationship publication is required.

Focused current checks:

```text
test_wayfinder_prototype_participation_matches_judgment = passed
test_prototype_preserves_lifecycle_boundaries_and_branch_gates = passed
test_promoted_prototype_preserves_selected_leaf_contract = passed
test_prototype_b0_and_description_pruning_controls_are_exact = passed
fresh Logic live probe = passed; repeated output byte-equivalent
fresh Measure live probe = passed; frozen-rule verdict A
fresh cleanup live probe = passed; process/port/root absent
```

## Evidence Dispositions

| Evidence | Disposition |
| --- | --- |
| Current hashes, inventory, metadata, links, ownership, and relationship trace | `exact-reusable`, freshly rechecked |
| 2026-07-22 exact-package `45/45`, `5/5` Measure, live branch/cleanup, and description results | `lane-limited`; corroboration only because exact prior runtime/model identity is unavailable |
| Historical `1/25 -> 25/25` and prior B0/C1 mechanism arms | `historical-admission-only` |
| Earlier candidates, Resume, packet schemas, and pruning generations | `invalidated` for this epoch |
| 2026-07-23 D0 behavioral efficacy | `reject-no-control-failure` for all five claims |
| 2026-07-23 exact B0 viability | `accepted` for the fixed tasks and current inherited runtime |
| C1 contribution | `not-applicable`; delta empty |
| Host discovery | `missing`; host telemetry unavailable |
| Fresh install parity | `missing`; owned by Prompt 5 |

## Deviations, Telemetry, And Residuals

- Arm order was D0 then B0 rather than alternated, as Prompt 4 explicitly
  requires D0 first.
- D0 overlays were fixed in the dispatcher packet and not persisted as a
  repository corpus. B0 evaluators read only the five exact canonical files.
- Behavioral probe actions were simulated; separate fresh live Logic, Measure,
  and cleanup fixtures exercised the executable/protected seams.
- Exact backend model identifier, reasoning-mode label, token count, latency,
  and per-sample cost were unavailable. No override was supplied.
- Host-level implicit discovery and fresh actual-browser UI execution were
  unavailable. Exact current metadata and the prior exact-package browser run
  bound the residual; no host/browser claim is made.
- Remote upstream freshness remains local-ref only.
- Fresh install parity remains for Prompt 5.

Worst behavioral result was still full compliance (`5/5`). No critical
failure or protocol defect invalidates B0 viability. The lack of D0 failures
does invalidate every tested incremental efficacy claim; those claims are
explicitly demoted.

## Acceptance

Exact behavior-complete C1:
`ed5832972cbd1a093656087a7efc61e679f4068b3bea330204ba6559fb78ce33`.

Decision: `accepted`. B0 is viable, the C1 delta is empty, protected behavior
and machine/relationship contracts remain intact, and no runtime repair is
justified. The immediate recommendation is Deploy Pruning Pass. This record
does not start it.
