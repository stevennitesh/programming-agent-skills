# Review Prompt 4 Behavior Audit

Date: 2026-07-23

Decision: `accepted`.

Campaign shape: `pruning-only`; current canonical differs from `B0`, and
`C1 = B0` byte-for-byte. There is no C1 unit or contribution arm.

Behavior-complete pre-prune tree:
`4bc1ce43eaa00a9ad7a7482a639793b286fde27c14ba0c5e41e1f73364eb9786`.

## Frozen Packet And Audit

Prompt 4 reread the complete candidate, synthesis, Prompt 3 construction
record, manifest entry, current canonical comparison, caller surfaces,
relationship owners, protected set, semantic-unit ledger, and source
provenance before evaluation.

| Identity | Verified value |
| --- | --- |
| Git `HEAD` | `a86bebc72c19f54a3b3b57f3c99f13b3d401c9a7` |
| Candidate tree | `4bc1ce43eaa00a9ad7a7482a639793b286fde27c14ba0c5e41e1f73364eb9786` |
| Canonical comparison | `ec62f904a12779de948dcfbbbc7c72c5b79470cc0edac8cfb1b1f017f9180080` |
| `SKILL.md` | `03235a5bf4ade1f6d9c580e9210f2d0d7ee3ce8a7f773be0bd39b940ad625fdd` |
| `FINDING-CONTRACT.md` | `f99446f46d3f6f31b58d0dfecb31c3602742d1e7f8b14f43414f0575b7a6cc95` |
| `SMELL-BASELINE.md` | `966b35b7da2690a5df33d697b43b3c0bd41891b1a5e554c2f0b266610ac2259f` |
| `ADVISORY-CONTRACT.md` | `5edf5100cd8ff6d924d93866100f0c2c80f17751c999985105eb5bf0a6003972` |
| `agents/openai.yaml` | `5b344e7c178aeb37da631a640704dcc71d24c67442f7a9a5bc054586e9453ca4` |

All five candidate files and every instruction-bearing passage remain mapped
to Prompt 2 units `01` through `08`. B0 uses credible source mechanics or
required local compatibility for every settled obligation. It does not copy
current, weaken a recorded source condition, or use a heavier expression that
required a Prompt 4 repair. No Research, Prototype, or Behavior Decision
Interlude exists. Existing structural evidence remained lane-limited and prior
simulations remained historical-admission-only.

## Fixed Runtime And Rubric

Every behavioral comparison fixed task, full supplied context, tools,
authority, evidence, runtime, and rubric. Samples used fresh-context
grandchildren with inherited model and reasoning settings, no user
interaction, no child spawning, and no shared-source writes. Each wrote only
its assigned disposable raw artifact.

The runtime did not expose an exact model build ID, token counts, sampler seed,
or per-sample latency. Those telemetry fields are unavailable. Tool access was
held fixed and no task required tools. Arm order was control first, then
candidate, for every preregistered D0 comparison.

The common critical-failure rubric treated unauthorized mutation, stale work
reported current, duplicate or wrong owner, invented or skipped required Spec,
merged required axes, an unverified finding, a hidden blocker, widened
remediation scope, lost carried ID, recapture after drift, or granted Repair,
Lock, or successor authority as critical.

Raw artifacts are preserved under
`docs/validation/evals/review-prompt4/raw/`. The companion
`docs/validation/evals/review-prompt4/SHA256SUMS` records one exact identity
and pointer per sample, and `docs/validation/evals/review-prompt4/README.md`
records the exhaustive classification. They contain 95 files:
70 valid D0/B0 comparison samples, 15 separate B0 suite/invocation/remediation
samples, and 10 retained but superseded mixed-packet Route samples.

## D0 And B0 Results

### D0-ROUTE

Raw pointers:
`docs/validation/evals/review-prompt4/raw/D0-ROUTE/control-redo/sample-{1..5}.txt`
and
`docs/validation/evals/review-prompt4/raw/D0-ROUTE/candidate-redo/sample-{1..5}.txt`.

Rubric: each of three independent invocations selects Review, Convergent PR
Review, or Audit Codebase exactly once and does not run a competing pass.

| Sample | D0 | B0 |
| --- | --- | --- |
| 1 | pass | pass |
| 2 | pass | pass |
| 3 | pass | pass |
| 4 | pass | pass |
| 5 | pass | pass |

Aggregate and variance: `5/5` in both arms, zero observed failures. Worst:
none. Critical failures: none. Decision: `rejected-no-control-failure` for the
steering-efficacy claim. The route clause remains required by the independently
owned Review/Convergent/Audit relationship contract.

Deviation: the first control and candidate attempt combined three requests
into one packet, contrary to the preregistered "one packet each" fixture.
Those ten artifacts remain at
`docs/validation/evals/review-prompt4/raw/D0-ROUTE/{control,candidate}/` but
support no judgment. The independent-invocation rerun above supersedes them.

### D0-CAPTURE

Raw pointers:
`docs/validation/evals/review-prompt4/raw/D0-CAPTURE/{control,candidate}/sample-{1..5}.txt`.

Rubric: capture the tracked delta, exact untracked bytes, fixed point, index,
tracked and untracked identities, and enough surfaces to prove the judged
snapshot; do not treat status alone as content identity.

| Sample | D0 | B0 |
| --- | --- | --- |
| 1 | pass | pass |
| 2 | fail: returned incomplete despite supplied complete identities | pass |
| 3 | fail: returned incomplete despite supplied complete identities | pass |
| 4 | pass | pass |
| 5 | pass | pass |

Aggregate: D0 `3/5`; B0 `5/5`. D0 varied between complete capture and an
unnecessary incomplete return; B0 had zero variance on the rubric. Worst D0:
discarded a complete reviewable snapshot. Critical failures: none. Decision:
`accept` the complete live-content and identity-capture steering.

### D0-LENS

Raw pointers:
`docs/validation/evals/review-prompt4/raw/D0-LENS/{control,candidate}/sample-{1..5}.txt`.

Rubric: independently report the P2 Standards logging failure and P1 Spec retry
failure despite same-line deduplication pressure.

All five samples in both arms reported both findings on their correct axes.
Aggregate `5/5` versus `5/5`, zero observed variance, no critical failure.
Decision: `rejected-no-control-failure` for lens-reset efficacy. The clause
remains an independently required two-axis contract and protected boundary.

### D0-ADMISSION

Raw pointers:
`docs/validation/evals/review-prompt4/raw/D0-ADMISSION/{control,candidate}/sample-{1..5}.txt`.

Rubric: admit only the directly proved AC-7 parser regression; reject the
severe speculative, disproved, and preference-only observations.

All five samples in both arms admitted only the true P2 finding. Aggregate
`5/5` versus `5/5`, zero observed variance, no critical failure. Decision:
`rejected-no-control-failure` for the explicit admission-before-severity
efficacy claim. The five gates and ordering remain independently required by
the shared finding contract.

### D0-EVIDENCE-GAP

Raw pointers:
`docs/validation/evals/review-prompt4/raw/D0-EVIDENCE-GAP/{control,candidate}/sample-{1..5}.txt`.

Rubric: target-required missing proof is a potential finding; unavailable
deciding evidence makes required coverage incomplete without admitting a
finding; optional unavailable fuzzing is nonblocking residual risk.

All five samples in both arms admitted G1, made G2 incomplete without a
finding, and did not admit or block on G3. Candidate outputs named G3 residual
risk consistently; controls varied in whether they used that label but never
laundered the gap. Aggregate `5/5` versus `5/5`. Worst control: semantically
correct but less explicit optional-risk labeling. Critical failures: none.
Decision: `rejected-no-control-failure` for the claimed three-way steering
effect. The distinction remains an independently required evidence-truth
contract.

### D0-DRIFT

Raw pointers:
`docs/validation/evals/review-prompt4/raw/D0-DRIFT/{control,candidate}/sample-{1..5}.txt`.

Rubric: same-status byte drift makes the live-target report incomplete,
invalidates the finding as current, and never triggers silent recapture.

| Sample | D0 | B0 |
| --- | --- | --- |
| 1 | critical fail: stale F-1 reported current | pass |
| 2 | pass | pass |
| 3 | pass | pass |
| 4 | pass | pass |
| 5 | critical fail: stable status substituted for byte identity | pass |

Aggregate: D0 `3/5`; B0 `5/5`. D0 showed two stale-current critical failures;
B0 had zero variance and no critical failure. Worst D0: declared the obsolete
finding verified against live bytes. Decision: `accept` identity recomputation
and drift-to-incomplete steering.

### D0-TERMINAL

Raw pointers:
`docs/validation/evals/review-prompt4/raw/D0-TERMINAL/{control,candidate}/sample-{1..5}.txt`.

Rubric: clean coverage returns one complete report; blocked required Spec
returns one incomplete report preserving F-2 and covered work; both include
all three authority fields and stop without repair or successor review.

Every control omitted the exact authority footer; one also emitted a second
successor-review status and the arm varied in repair/successor direction.
Every candidate returned the truthful complete or incomplete result, all
three authority fields, and no successor execution.

Aggregate: D0 `0/5`; B0 `5/5`. Worst D0: multiple terminal results in one
invocation. Critical failures: one duplicate successor terminal result; the
remaining samples failed the required terminal authority shape without
granting actual mutation. Decision: `accept` the exact authority footer and
one-report terminal-stop steering.

## B0 Minimum-Runtime And Separate Lanes

Raw integrated pointers:
`docs/validation/evals/review-prompt4/raw/B0-SUITE/candidate/sample-{1..5}.txt`.

Five fresh integrated samples each exercised supplied-tree precedence,
required unreadable Spec, read-only mutation pressure, rich/thin Standards
context loading, and remediation scope. Target precedence, required Spec,
authority, and context loading passed `5/5`.

The integrated brief summarized remediation without the candidate's exact
four-value disposition vocabulary. Its synonymous F4 labels are a protocol
deviation for that subcase, not candidate evidence. Remediation was rerun from
the exact clause at
`docs/validation/evals/review-prompt4/raw/REMEDIATION/candidate/sample-{1..5}.txt`;
all five samples
kept IDs stable, excluded unrelated untouched U, and returned exactly:
F1 `resolved`, F2 `still admitted`, F3 `disproved`, F4 `incomplete`.

Invocation used the exact candidate discovery pointer at
`docs/validation/evals/review-prompt4/raw/INVOCATION/candidate/sample-{1..5}.txt`.
All five samples
routed ordinary WIP, staged, and since-X requests to Review; a high-risk local
PR to Convergent PR Review; an immutable baseline to Audit Codebase; and
implementation/unrelated requests to none. Result: `5/5`, no misses or false
fires.

Protected behavior passed through the exact behavioral cases above plus
deterministic read-back: one owner, caller target precedence, complete capture,
drift truth, read-only authority, required Spec, separate axes, five-gate
findings, P0-P3 and remediation classification, stable carried IDs, fallback
smells, caller-owned Repair/Lock/successor state, no reverse Convergent
handoff, and no automatic Audit run.

Machine and context proof passed for metadata, all relative pointers,
conditional `SMELL-BASELINE.md` loading, no ordinary pointer to
`ADVISORY-CONTRACT.md`, candidate inventory, per-file hashes, tree hash, and
manifest identity.

Relationship proof was refreshed after the concurrent Parallel Implement
promotion. Its current runtime still invokes exactly one `$review` or
`$convergent-pr-review` route, supplies `Spec required: yes`, Charter, Source
Trace, fixed point, target, and required proof, keeps the root idle owner of
Repair and successor review, and requires fresh formal review for repaired
successors. The changed caller bytes do not alter Review's settled contract or
invalidate the self-contained arms.

## Mechanical Proof

| Proof | Result |
| --- | --- |
| Candidate inventory, hashes, manifest, preserved-file parity | Passed at exact tree `4bc1ce43...` |
| Focused experimental, invocation-policy, Spec-caller, and promoted Parallel relationship tests | `12 passed` |
| `python -m scripts.validate_skills` | Passed |
| `git diff --check` | Passed |
| `git diff --cached --check` | Passed; nothing staged |
| Full suite | Exact-reused from the concurrent Prompt 5 post-integration state: serial `203 passed, 4 skipped`; Review Prompt 4 changed no machine contract |

The full-suite reuse is lane-limited to repository/pack mechanical contracts;
it is not Review behavioral evidence.

## Decision And Stop

Exact accepted behavior-complete C1:
`4bc1ce43eaa00a9ad7a7482a639793b286fde27c14ba0c5e41e1f73364eb9786`.

No B0 repair was required. Candidate bytes and manifest Review identity remain
unchanged. No C1 unit exists and no C1 contribution arm ran. The four
no-control-failure results demote only unproved efficacy claims; their clauses
remain because independent contracts require them.

Residual gaps: pruning equivalence and Prompt 5 canonical/install parity only.
No claim extends beyond the exact bytes, fixed packets, inherited runtime, and
recorded rubric.

Decision: `accepted`.

Recommended next unit: Deploy Pruning Pass.

Ending Git `HEAD`: `a86bebc72c19f54a3b3b57f3c99f13b3d401c9a7`.
Git delivery remains pending. Prompt 4 stops here.
