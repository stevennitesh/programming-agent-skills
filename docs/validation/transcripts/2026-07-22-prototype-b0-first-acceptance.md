# Prototype B0-First Acceptance

Date: 2026-07-22

## Decision

**Accepted for Prompt 5.**

Deploy Prompt 4 ran exact B0 controls before every claimed behavioral
mechanism, removed or demoted guidance when B0 did not fail, proved mandatory
safety and authority contracts deterministically, reconciled the inactive
candidate, and reran all evidence affected by changed package bytes.

This acceptance advances only the inactive experimental candidate to Prompt 5.
It does not promote or install Prototype, edit canonical runtime, change a
caller or relationship, or authorize downstream execution.

## Exact Identities

| Package | Tree SHA-256 | Disposition |
| --- | --- | --- |
| Executable B0, `docs/validation/evals/prototype-b0/` | `c1a79fa3a144e1cac39be80233e1a3a2756c2f5130af14d3bc20c53418c6d307` | Frozen and unchanged |
| Accepted C1, `skills/experimental/prototype/` | `ed5832972cbd1a093656087a7efc61e679f4068b3bea330204ba6559fb78ce33` | Inactive; eligible for Prompt 5 |
| Description pre-prune control, `docs/validation/evals/prototype-description-pre-prune/` | `22614fe625ec8ecbb176ef8af07cb4c0b186e92dee1348f9efb85be10120f668` | Frozen against accepted C1 |
| Prompt 4 protocols and live probes, `docs/validation/evals/prototype-prompt4/` | `556951e87111636df9a29d376a48f097d6ec12cb8453d58f623530681bef47a4` | Validation fixture |
| Canonical runtime, `skills/custom/prototype/` | `ecba1e84f0e0df9a0b32b2febdac4e1d7f096dcbf468f9c054c0d5bf7d95a3ef` | Unchanged |

Accepted C1 file identities:

| Relative path | SHA-256 |
| --- | --- |
| `SKILL.md` | `a74b12945f96946b37ac107604cb92902422ac28892e87f528fe687d32135442` |
| `LOGIC.md` | `561d27d37caeef82a4b8663186818a0dba5f4942c7ff45ebf31bf3fb176efd0a` |
| `UI.md` | `7a05360e662c2cc7c49e507d0e7024b80e0e1e1f4f2127cf6281671195c79f25` |
| `MEASURE.md` | `3fde86a4b2c8d2ccd730b1367fb11d0e166bc3de79ee78f12067b6fc7e10d5eb` |
| `agents/openai.yaml` | `8619a54e8c098122a7f3881394f84ca89b684366e848233a29ad18b6ec363935` |

The description control has the same inventory and identical bytes for every
file except `SKILL.md`, whose SHA-256 is
`c22169ff87bbb4fe7ae058c52c7ad5c3956679ff5c52ec09f6b4e606cc1be5fd`.
Replacing only its description line with accepted C1's description produces
accepted C1 exactly.

## Evidence Reuse Discipline

The historical `1/25` versus `25/25` aggregate result remains historical
admission evidence only. It is not exact contribution proof against this B0.

The Prompt 3 `4d9ca65...` package was reconciled because B0 controls showed
that its Admit expansion, five-lock presentation, exact packet schema, and
Resume branch did not contribute. Its unchanged branch evidence remains
historically valid for those unchanged bytes, but it does not prove
contribution against B0. Because the root and package inventory changed, the
old C1 Measure arm was treated as affected and rerun against exact accepted C1.

The earlier description-pruning result was not reused: the package bytes and
current runtime identity were not all identical. Five fresh metadata-only
contexts ran each exact Prompt 4 arm with the same tasks, protocol, runtime,
and rubric.

## B0-First Mechanism Ledger

All behavioral samples used fresh agents with `fork_turns="none"`, no model or
reasoning override, no peer output, and only the assigned exact package and
protocol.

| Claimed mechanism | Exact B0 result first | C1 arm | Prompt 4 disposition |
| --- | --- | --- | --- |
| Four-predicate Admit and fit/readiness split | Cases A and B correct in `5/5`; no mutation and correct mismatch/readiness distinction | Skipped | Remove expansion; keep B0-equivalent Fit |
| Five-lock Freeze | Case B and focused role cases complete and safe in `5/5` samples | Skipped | Remove grouping; keep flat minimum Freeze |
| Measure | Case C chose Logic rather than variable comparative evidence in `5/5`; `0/5` correct branch | Required; accepted C1 selected Measure and applied the rule in `5/5` | Accept Measure as the one demonstrated behavioral contribution |
| Exactly-one branch wording | UI-only case I correct in `5/5` | Skipped | Demote to compact B0-equivalent context rule |
| Claim, mode, and role labels | B, M1, and M2 preserved independent authority in `5/5` | Skipped | Retain exact labels only as deterministic Wayfinder compatibility and authority contracts |
| Three named proof levels | D and E preserved Smoke/verdict/production boundaries in `5/5` | Skipped | Remove taxonomy; retain B0-equivalent safety boundary |
| Four packet schemas and identity read-back | A-H returned truthful applicable facts and H preserved sequential caller identity in `5/5` | Skipped | Remove packet mechanism; retain direct current-caller Return |
| Four artifact dispositions | F safely reconciled every effect in `5/5` | Skipped | Retain compact exact dispositions only as mandatory deterministic safety |
| Awaiting-only Resume branch | G plus focused R1/R2 handled safe restart, drift, and refusal in `5/5` | Skipped | Delete `RESUME.md`; a continued artifact is safely re-frozen as a current invocation |

Focused B0 Resume/authority controls also passed:

- R1 valid pending-human restart: `5/5`;
- R2 drifted pending-human artifact refusal: `5/5`;
- M1 invalid `shape/feel` plus rule-based pairing: `5/5`; and
- M2 independent decision owner, judge, custodian, and return owner: `5/5`.

## Accepted C1 Behavior

After reconciliation, five fresh exact-package samples each ran cases A-I.
Every sample:

- rejected the broad multi-decision production request before mutation;
- distinguished UI fit from missing readiness and isolation;
- selected Measure for variable comparative evidence;
- refused to turn green startup into a verdict or production proof;
- preserved unavailable-human authority and left no live resource;
- reconciled mixed artifacts without overwriting concurrent work;
- re-froze blocked Resume state instead of trusting stale identity or custody;
- kept sequential TDD and Improve Codebase caller identities separate; and
- loaded UI as the sole decision-bearing branch when timing was irrelevant.

Result: `45/45`, worst sample `9/9`, zero observed variance, no critical
failure.

The exact accepted Measure arm then ran in five fresh contexts. All `5/5`
selected only `MEASURE.md`, retained the frozen workload and rule, computed A
median/worst as `10/12`, B median/worst as `8/9`, selected B from the fixed
20-percent median improvement and worst-sample rule, preserved production
non-claims, returned to Audit Codebase with Casey as decision owner, and
truthfully required or reported verified cleanup. Two samples explicitly
withheld a cleanup-complete claim because the read-only fixture supplied
dispositions but no literal post-cleanup read-back; this is correct
false-completion resistance, not a failure.

## Description Pruning Equivalence

The longer pre-prune description and shortened accepted description each ran
the same nine metadata-only request families in five fresh contexts:

| Arm | Samples | Aggregate | Worst | Critical failures |
| --- | ---: | ---: | ---: | ---: |
| Pre-prune description | 5 | `45/45` | `9/9` | 0 |
| Shortened description | 5 | `45/45` | `9/9` | 0 |

Both invoked only the bounded Logic, structural UI, and predeclared comparative
Measure cases, rejecting production implementation, uncertain diagnosis,
source research, multi-decision design, ordinary one-number measurement, and
incidental naming. This is pruning-equivalence evidence, not a behavior-
changing C1 mechanism or host-discovery proof.

## Deterministic And Live Proof

- Logic: `logic_probe.py` exercised happy, boundary, and rejected reducer
  cases; every criterion passed and two runs were byte-equivalent.
- UI: a local isolated route exposed cards, table, and timeline variants.
  Browser inspection verified direct URL state, next/wrap switching, reload
  persistence, distinct DOM structures, unobscured controls, and no horizontal
  overflow at `390x844`. The browser tabs and local server were closed.
- Measure: `measure_probe.py` used two warmups and five raw samples per
  direction. One recorded run observed A
  `[0.9364, 0.9415, 0.9463, 0.9338, 0.9575]` and B
  `[0.9301, 0.9554, 0.9556, 0.9483, 0.9217]` milliseconds, medians
  `0.9415/0.9483`, worst results `0.9575/0.9556`, and selected A under the
  predeclared rule. The result explicitly remains a local microbenchmark, not
  a production baseline or SLO.
- Cleanup: `cleanup_probe.py` proved the process was live, then stopped it and
  released its port; deleted the disposable file, cache, and credential;
  removed only the Prototype-owned shared-file line; preserved the concurrent
  user line and authorized verdict evidence; and verified the run root absent.
- Resume and caller Return: the B0 focused controls and final exact-package
  A-I wave exercised successful restart, drift refusal, stale blocked state,
  sequential caller identity, direct Return, and no downstream execution.

The authority checks are deterministic contracts against the active
experimental Wayfinder surfaces: Prototype preserves the exact `claim level`
and `judgment mode` vocabulary, treats decision owner and human judge as
independent, and leaves canonical callers and relationships unchanged.

Accepted relationship delta for Prompt 5: promote the accepted Prototype
package atomically while preserving the existing caller-return topology.
Wayfinder continues to supply Prototype decision authority, claim level, and
judgment mode and receive its result; Prototype continues to start no
downstream route. No relationship-index publication is part of Prompt 4.

## Structural Validation

- Exact B0 hash remained unchanged.
- Exact candidate and description-control tree hashes match tests and the
  experimental manifest.
- Candidate inventory contains only `SKILL.md`, `LOGIC.md`, `UI.md`,
  `MEASURE.md`, and `agents/openai.yaml`; `RESUME.md` is absent.
- `$handoff` and `$domain-modeling` recommendations are absent while direct
  current-caller Return remains.
- Focused Prototype contracts: `2 passed`.
- Full repository pytest: `193 passed, 4 skipped`.
- Focused pack pytest: `59 passed`.
- `python -m scripts.validate_skills`: passed.
- Installer dry-run: 25 managed skills unchanged; global bootstrap present.
- `git diff --check` and `git diff --cached --check`: passed.

## Prompt 5 Handoff

Only accepted work may advance. Exact inactive C1
`ed5832972cbd1a093656087a7efc61e679f4068b3bea330204ba6559fb78ce33`
is accepted and may proceed to Prompt 5. Prompt 5 must still own coordinated
canonical/caller promotion, any relationship change, installation, and parity
proof. None occurred here.

## Prompt 5 Promotion And Installation

Deploy Prompt 5 completed on 2026-07-22.

- Exact experimental C1 was read back at
  `ed5832972cbd1a093656087a7efc61e679f4068b3bea330204ba6559fb78ce33`
  immediately before promotion.
- `skills/custom/prototype/` was replaced byte-for-byte with that package.
  No wording repair occurred. Canonical tree hash is
  `ed5832972cbd1a093656087a7efc61e679f4068b3bea330204ba6559fb78ce33`.
- `RESUME.md` remains absent. The rejected Admit expansion, five-lock
  presentation, exact packet mechanism, proof taxonomy, and outbound
  `$handoff` and `$domain-modeling` recommendations were not restored.
- Prompt 4 behavior evidence was reused because candidate bytes, tasks,
  claims, and evidence contracts were unchanged.
- The accepted caller delta updated canonical Wayfinder to pass Prototype's
  decision owner, claim level, judgment mode, and human judge when human, then
  receive its supported result, evidence, limits, and cleanup state.
- Independent Spec/Standards review found and repaired one additional accepted
  direct caller: Improve Codebase now supplies the complete Freeze authority,
  uses `.tmp/prototype/<question-slug>/`, and receives the supported result
  without treating it as production proof.
- The relationship map removed only Prototype's outbound Handoff and Domain
  Modeling edges and retained direct Return to the current caller or user.
- The synthesis's rejected five-lock, packet, and Resume design is explicitly
  demoted to historical evidence so it cannot direct a future restoration.
- Focused canonical Prototype/caller proof passed `8/8`; full pytest passed
  `193` with `4` skipped; focused pack pytest passed `59/59`; skill validation
  and both diff checks passed. One full-suite run produced three unrelated
  empty-subprocess failures in Parallel Implement helpers; all three passed on
  immediate focused rerun and the complete suite then passed.
- The entire `skills/experimental/prototype/` directory and only its manifest
  entry were retired. Every other experimental candidate was preserved.
- The initial pre-install dry-run proposed exactly `prototype, wayfinder`.
  After independent review admitted the stale Improve Codebase caller, a
  second dry-run proposed exactly `improve-codebase`. The cumulative affected
  cohort was therefore exactly `prototype`, `wayfinder`, and
  `improve-codebase`; no unrelated skill or global bootstrap change appeared.
- The supported installer synchronized each verified pending cohort.
- Canonical and installed Prototype inventories and bytes match at
  `ed5832972cbd1a093656087a7efc61e679f4068b3bea330204ba6559fb78ce33`.
- Canonical and installed Wayfinder inventories and bytes match at
  `f964bd8861829d8bef2fbd6874bdfd9508e13c9b02ceb775714c28193818b38a`.
- Canonical and installed Improve Codebase inventories and bytes match at
  `1af18e7d7fe09d1753d3b8ab285e555c0b2e5475451cbd34cb993be5cfad1d86`.
- Post-install dry-run reports all 25 managed skills unchanged and the global
  bootstrap present.
- Independent Spec and Standards rechecks confirmed both admitted findings
  resolved with no blocking finding remaining.

Prompt 5 status: `complete`. Git delivery did not start.
