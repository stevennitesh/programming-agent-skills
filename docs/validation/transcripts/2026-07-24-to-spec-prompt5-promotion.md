# To Spec Deploy Prompt 5 Promotion And Installation

Authorized unit: Deploy Prompt 5 for `to-spec`.

## Decision

`complete`

Prompt 4 acceptance and the completed `pruning-not-needed` pass were read
back. V1 and P1 both alias exact M0. Canonical `SKILL.md` and
`agents/openai.yaml` were replaced with exact P1 bytes, read back completely,
and proved against the accepted inventory and tree identity.

The directly affected contract assertions were reconciled from superseded
heading and literal-token snapshots to P1's ordered semantic gates and full
durable read-back contract. All 59 focused contract tests passed. The target
experimental directory and exact manifest entry were absent.

The managed dry-run reported 25 skills with only `to-spec` updated. The
supported installer synchronized the pack. Installed `to-spec` contained
exactly `SKILL.md` and `agents/openai.yaml`; both matched canonical bytes, and
the post-install dry-run reported 25 unchanged skills. The full integration
suite passed 206 tests with 4 skips.

`scripts.validate_skills` retained unrelated concurrent trailing-whitespace
failures in `convergent-pr-review` viability captures V01, V09-invalid, V11,
V19, and V23. No `to-spec` validation failure remained.

## Shared Run Contract Return

Authorized unit completed: Deploy Prompt 5 for `to-spec`.

Decision: `complete`.

Campaign shape: `hypothesis-candidate`.

Runtime identities: exact P1, canonical, and installed identities are owned by
the campaign manifest and are equal to V1/M0.

Artifacts changed: exact canonical package; directly affected contract proof;
active synthesis; campaign manifest, candidate, decision, and this transcript.

Evidence used or reused: exact Prompt 4 acceptance, exact V1/P1 identity,
complete pruning audit, focused proof, full integration suite, scoped install
preview, supported synchronization, parity read-back, and clean preview.

Residual gaps: live provider mutation/recovery and transfer beyond the fixed
Prompt 4 execution and fixture conditions.

Recommended next unit: none.

Git HEAD: `f3be70c31dd8f2ae9f12a75248065ef313790bda` ->
`f3be70c31dd8f2ae9f12a75248065ef313790bda`.

Git delivery: pending.

Exact stop reason: bare campaign authorization ends after completed Prompt 5;
Prompt 6 and every other skill are outside this unit.
