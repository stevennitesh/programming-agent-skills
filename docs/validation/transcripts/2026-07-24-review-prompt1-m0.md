# Review Deploy Prompt 1 — M0 Freeze

Campaign epoch: `2026-07-24-review-f3be70c`

Authorized unit: Deploy Prompt 1 only

Operation: `writing-great-skills` Audit

Starting Git `HEAD`: `f3be70c31dd8f2ae9f12a75248065ef313790bda`

Authoritative checkpoint:
[campaign.json](../../../.scratch/deploy-campaigns/2026-07-24-review-f3be70c/campaign.json)

## Blindness and mutation boundary

The pass did not inspect the current `review` skill body or its disclosed
files, target review synthesis conclusions, review historical evaluations or
transcripts, experimental candidates, promotion records, upstream packages,
or outside research. It did not modify the canonical skill, installed mirror,
scripts, tests, prior records, or the unrelated untracked
`docs/validation/transcripts/2026-07-24-convergent-pr-review-prompt1-m0.md`.

This transcript and the linked machine-readable checkpoint are the only
Prompt 1 writes.

## Intent sources read

The pass read the complete repo primer, pack context, engineering contract,
domain router, configured issue-tracker contract, applicable ADRs, the
complete `implement` caller contract, the complete review-facing
`parallel-implement` caller contract and integrator handoff, and the complete
`skill-router` relationship contract. It inspected only the focused existing
compatibility assertions capable of settling the review interface. Exact
source identities are owned by the checkpoint.

## Viability decision

The minimum intended outcome is one terminal, read-only judgment of one
immutable ordinary diff against a fixed point. The reviewer covers the
bounded target, judges Standards and Spec separately, admits only verified
actionable findings, reads the snapshot back for drift, returns authority to
the caller, and stops.

The ordinary route accepts branch, WIP, staged, and since-X diffs. Local pull
requests, release candidates, and bounded high-risk diffs return to
`convergent-pr-review`; open-ended immutable repository-baseline audits return
to `audit-codebase`; implementation and repair requests return to their caller
without mutation.

The caller owns Spec, Charter, Source Trace, commitment boundaries, supplied
fixed point, risk classification, repair admission, residual-risk acceptance,
mutation, and every successor snapshot. The reviewer owns only bounded
read-only inspection, evidence-backed judgment, finding admission into the
report, coverage accounting, and terminal status for the pinned snapshot.

No material user-owned behavior axis remained fuzzy. The Prompt 1 decision is
`ready-for-research`.

## M0 checkpoint contents

The linked manifest freezes:

- the intended contract and required order;
- fourteen independently owned semantic behavior units;
- the runtime clause map and clause-to-intent cut audit;
- the committed-tree, index, working-tree, connector-source, caller-source,
  and terminal-report state-location ledger;
- seventeen viability cases;
- exact local source identities and method identities;
- required compatibility fields and authority lines;
- limitations and five research clusters;
- exactly one authorized research-note path; and
- the semantic content fingerprint.

M0 is behavior-minimal. It makes no claim about professional-method quality,
optimal wording, source-derived vocabulary, or behavioral effectiveness.

## Prompt 1 proof outline

Prompt 1 proof is structural:

1. Parse the manifest as JSON.
2. Recompute the canonical fingerprint of `m0_checkpoint`.
3. Require every semantic unit to name authority, neutral expression, entry
   case, wrong-condition case, failure Return, and proof.
4. Require every runtime clause to map to at least one semantic unit and every
   semantic unit to appear in the clause map.
5. Require every state transition to name owner, authoritative location,
   allowed transition, order, read-back, and failure Return.
6. Require the viability suite to cover routing, baseline, all snapshot
   locations, Source requirements, both axes, finding admission, report
   compatibility, drift, and the no-mutation boundary.
7. Run affected Markdown gates, `python -m scripts.validate_skills`, both diff
   checks, work-state read-back, and Git `HEAD` read-back.

## Residual gaps

Research must still test applicable professional evidence, alternatives,
counterpressure, and limits. Later units must inspect current and upstream
runtime evidence only after this M0 identity is frozen. Behavioral proof is
not part of Prompt 1 and has not been claimed.

## Proof read-back

- Checkpoint JSON parsing, canonical fingerprint recomputation, semantic-unit
  field coverage, clause-map bijection, state-ledger field coverage, viability
  count, decision, and successor-authorization checks passed.
- The checkpoint semantic fingerprint read back as
  `sha256:3fa19cd24076c25090adc1921dc1e5f1c21e74749e4179a6c59c17a523330366`.
- Affected Markdown local-link, unique-anchor, balanced-fence, and table gates
  passed.
- `python -m scripts.validate_skills` passed.
- `git diff --check` and `git diff --cached --check` passed.
- Git `HEAD` read back unchanged at
  `f3be70c31dd8f2ae9f12a75248065ef313790bda`.
- The starting unrelated convergent-review transcript remains unmodified.
  A concurrent unrelated untracked
  `docs/validation/transcripts/2026-07-24-to-spec-prompt1-m0.md` appeared
  during Lock and was also preserved unmodified.

## Stop

Recommended next unit: Research Pass.

The recommendation does not authorize this Prompt 1 owner to start it.
