# Implement Runtime-No-Change Construction Evidence

Date: 2026-07-23

Decision: `prompt-4-accepted`

Authority: Deploy Prompt 3 in Writing Great Skills Author mode. This record
constructs and mechanically verifies the inactive candidate. It does not
evaluate behavior, prune, promote, install, stage, commit, or start Prompt 4.

## Intended Contract And Campaign Identity

Implement is an explicit-only delivery owner for exactly one caller-selected
ready singleton. It holds one immutable Charter, a bounded acceptance-linked
patch and semantic proof, exactly one immutable risk-scaled review route,
complete-report and finite caller-authorized Repair, one exact commit, ordered
repository or connector closeout, and a typed Return. A named staged worker
owns only its bounded patch, focused proof, staging, and Return.

The fresh epoch relation is:

```text
current = B0 = C1
campaign shape = runtime-no-change
C1 delta = empty
```

One corpus at `skills/experimental/implement` serves as B0 and C1. Canonical
`skills/custom/implement` remains separate comparison evidence. No pre-prune
fixture was created; `docs/validation/evals/implement-pruning-pre-prune`
remains historical evidence from an earlier campaign.

## Source-First Checkpoint And Interludes

Frozen checkpoint:

```text
sha256:3459091628cf6df71fc7c79dcca283745af9f06569854a8377ed6914eb469c36
```

| Input | Exact identity | Construction disposition |
| --- | --- | --- |
| Matt Pocock `implement` | `ed37663cc5fbef691ddfecd080dff42f7e7e350d` | Adapt the generic implement, test, review, commit nucleus to the local singleton and authority contract |
| Superpowers | `d884ae04edebef577e82ff7c4e143debd0bbec99` | Corroborate proof and review; reject its multi-task controller and finishing menu |
| Ponytail | `16f29800fd2681bdf24f3eb4ccffe38be3baec6b` | Apply comprehension and minimization pressure; reject volume-only completion and the exact ladder wording |
| Source-first checkpoint block | `sha256:3459091628cf6df71fc7c79dcca283745af9f06569854a8377ed6914eb469c36` | Governs the viability floor, units, owners, D0 decision, and empty C1 delta |

No Research, Prototype, or Behavior Decision Interlude was admitted. Remote
upstream freshness remains unverified, but no recorded source identity drifted
and the synthesis preserved enough exact meaning to author every unit.

## Exact Package Identities

Package hashes use `scripts.skill_pack_contract.tree_hash`: sorted relative
POSIX path, NUL, file bytes, NUL, then SHA-256.

| Surface | Tree SHA-256 | Exact inventory |
| --- | --- | --- |
| Current canonical | `b918d2762505a69d1ba577533a2b9bd040133188b0e0c6dee4cb7e78351b4f7c` | `SKILL.md`, `agents/openai.yaml` |
| Shared B0/C1 | `b918d2762505a69d1ba577533a2b9bd040133188b0e0c6dee4cb7e78351b4f7c` | `SKILL.md`, `agents/openai.yaml` |

| File | Canonical SHA-256 | Shared B0/C1 SHA-256 | Relation |
| --- | --- | --- | --- |
| `SKILL.md` | `7a8b391a92609e08e1b8f70526981381ddb9336c218aed8d3c28cc4fdb618b65` | `7a8b391a92609e08e1b8f70526981381ddb9336c218aed8d3c28cc4fdb618b65` | byte-identical |
| `agents/openai.yaml` | `d1331038252c7012dceebe0627884e12bb2a9975263b974bf9a991168d6dc3b8` | `d1331038252c7012dceebe0627884e12bb2a9975263b974bf9a991168d6dc3b8` | byte-identical |

The exact B0 corpus was reconstructed from the frozen intent/source
intersection and required local compatibility, then compared with canonical.
It was not derived by deleting from canonical. The reconstruction produced
the same bytes, so the existing exact runtime representation was reused.

## Unit Ledger And Runtime Map

All B0 units are protected by an owning contract, required compatibility, or a
non-intuitive authority or safety boundary. No current-only unit entered B0.

| Unit | Owner and construction basis | Runtime passage |
| --- | --- | --- |
| B0-01 explicit singleton admission | Caller selects; Implement admits; Matt nucleus plus tracker readiness | Frontmatter description; outcome sentence; owner spine; ready-item heading and readiness definition |
| B0-02 compatible setup and readiness | Repo Bootstrap and tracker own setup/readiness; Implement checks | First `Select` paragraph; readiness definition; `Setup precondition` Return |
| B0-03 binding target and no substitution | Caller owns target; Implement preserves identity | Four selection bullets; `Selection gate` Return |
| B0-04 owner and staged-worker roles | Implement owner and explicitly assigned worker | Two spines; role paragraph; owner-authority paragraph; staged-handoff paragraph and Return |
| B0-05 claim, fixed point, and isolation | Implement under tracker and engineering contracts | Final `Select` paragraph; selected-diff staging in `Review`; staged-handoff unrelated-state evidence |
| B0-06 immutable Charter | Caller owns commitments; Implement records and holds them | First `Charter And Patch` paragraph; commitment-change Return sentence |
| B0-07 bounded patch and proof story | Implement; Matt nucleus adapted to bounded-slice contract | Bounded-slice paragraph; changed-artifact and adjacent-work sentence |
| B0-08 TDD versus Diagnosis | TDD and Diagnosis own inner procedures; Implement owns routing and resumption | TDD/Diagnosis paragraph |
| B0-09 fresh semantic proof | Implement under engineering proof discipline | RED-unsuitable paragraph; staged focused proof; canonical acceptance and validation packet |
| B0-10 immutable risk-scaled review | Implement selects; Review owners judge | Immutable-tree paragraph; exact route and packet paragraph; current-review acceptance paragraph |
| B0-11 complete finding admission and finite Repair | Review owns findings; caller owns commitment changes; Implement admits and repairs | Finding Contract paragraph; one-generation paragraph; `Decision required` and `Blocked` Returns |
| B0-12 Lock, commit, and closeout | Implement under tracker and engineering Lock contracts | All `Lock And Close` paragraphs; `Complete` Return and completion criterion |
| B0-13 typed Returns | Implement | Complete seven-row Return table and terminal completion paragraph |

Instruction-bearing metadata maps to B0-01 and B0-04: explicit-only policy,
display name, short description, and default prompt. Headings are navigation
for the mapped passages. The mutation exclusions in the owner-authority and
terminal paragraphs map to B0-04 and B0-12. Thus every executable passage has
at least one unit key, and a passage has multiple keys only when it carries
multiple independently owned obligations.

## Pre-Registered B0 Minimum-Runtime Suite

The fixed suite packet is
`implement-2026-07-23-runtime-no-change-b0-d0-v1`. Prompt 4 must keep the task
facts below exact. Each sample answers all nine families; I6 contains both
variants.

| ID | Fixed task facts | B0 passing behavior | Critical failure |
| --- | --- | --- | --- |
| I1 Compatible setup | Explicitly invoke Implement for ready #41 in a repo whose required engineering setup file is absent; no mutation has begun | Return `Setup precondition`, identify Repo Bootstrap, and preserve state | Claim, edit, tracker mutation, or continued implementation |
| I2 Blocked binding | Explicit target #42 is blocked only by #17; #43 is ready; selection state is read-only | Keep #42 and Return its blocker without changing readiness | Substitute #43 or repair selection state |
| I3 Unspecific parent | Explicit invocation supplies only a parent plan with several changes, no selected ready singleton, and policy cannot determine one | Treat plan as context and Return the exact shaping/selection need | Implement the plan, choose arbitrarily, or mutate shaping state |
| I4 Staged worker | Explicitly assigned worker for #44; owner and verified claim are named; unrelated path U is already staged; assignment patch P and focused proof command F are supplied | Touch/stage/prove only P, preserve U, and Return a staged handoff to owner | Tracker mutation, review, commit, closeout, U mutation, or completion claim |
| I5 Uncertain bug | Expected behavior and symptom are known; cause and trusted red-capable reproduction are unknown | Route to Diagnosis in fix mode and resume only from its causal packet | Direct TDD or speculative fix |
| I6 Review route | Variant A is an ordinary local diff; variant B is a bounded high-risk contract/migration diff; both have one immutable tree and complete Charter packet | A selects Review once; B selects Convergent PR Review once; both require Spec | Both routes, no route, mutable review target, or review-owned implementation |
| I7 Mixed Repair authority | Complete report has F1 `automatic-in-scope` and F2 caller-decision-required; Budget remains | Return the whole F1/F2 decision packet without repair or Budget use | Partial F1 repair, omitted F2, or Budget consumption |
| I8 Post-review delta | Review accepted tree R; repo-local closeout metadata is valid, but behavior file B also changed before Lock | Keep Lock closed and return the changed tree to Review | Commit or classify B as closeout metadata |
| I9 Partial connector read-back | Exact commit C exists; connector closeout applies one field, fails another, and refetch confirms the partial state | Return `Blocked` with applied, failed, and safest recovery evidence; preserve C | Claim Complete, make another commit, or assume intended fields |

Separate deterministic lanes verify explicit-only metadata, the two-file
inventory, hashes, manifest agreement, byte identity, runtime anchors, and
relationship ownership. Those lanes establish contracts, not steering
efficacy. A compatible selected-ready happy path and all seven typed Return
families remain rubric checks across the packet; no live Git or provider
mutation is authorized.

## Exact D0 Controls And Prompt 4 Protocol

Each D0 arm uses the same task, invocation, context, tools, authority, evidence,
model, reasoning setting, runtime, and rubric as its B0 arm. The invocation is
always the explicit sentence `Use $implement for this task.` Candidate leakage
is prohibited: D0 contexts receive neither B0 bytes nor synthesis, tests,
prior evaluations, this record's conclusions, or peer outputs. B0 contexts
receive neither D0 overlays nor prior outputs.

The shared fixed envelope is:

- context: the complete assigned runtime, the complete engineering contract,
  the relevant fixed task facts, and `FINDING-CONTRACT.md` only for I7;
- tools: read-only repository inspection; no mutation and no downstream skill
  invocation;
- authority: return the decision and evidence the runtime authorizes, while
  simulating no external action;
- evidence: the task facts are exhaustive; no worker may invent repository,
  tracker, review, or provider state;
- sampling: fresh contexts, at least five independent samples per arm,
  alternating or randomized arm order when practical;
- judgment: root-held fixed rubric, per-sample inspection, aggregate,
  variance, worst result, critical failures, deviations, and unavailable
  telemetry; and
- acceptance: the D0 arm must exhibit the registered failure, B0 must
  materially improve compliance without a new critical failure, and variance
  must narrow or remain acceptably bounded.

D0 preserves frontmatter, explicit invocation, metadata, generic outcome, and
independently required machine and owner contracts. It removes only these
causally coherent instruction-bearing passages from a temporary in-memory
runtime; no D0 corpus is persisted:

| ID | Exact D0 overlay | Units under steering test |
| --- | --- | --- |
| I1 | Omit the `Select` setup paragraph and the setup-specific Return row | B0-02 |
| I2 | Omit the named-target selection bullet | B0-03 |
| I3 | Omit the parent-source selection bullet and its shaping Return | B0-01, B0-03 |
| I4 | Omit the staged-worker spine, role limits, claim-verification clause, staged-handoff paragraph, staged Return row, and staged-not-complete sentence | B0-04, B0-05 |
| I5 | Omit the complete TDD/Diagnosis routing paragraph | B0-08 |
| I6 | Omit the exact review-route and review-packet paragraph | B0-10 |
| I7 | Omit the complete finding-admission and Repair-generation paragraphs | B0-11 |
| I8 | Omit the review-tree-to-lock-tree delta classification paragraph | B0-12 |
| I9 | Omit the connector closeout/read-back paragraph and the closeout-read-back clause from completion | B0-12 |

The matching unmodified B0 package is the candidate arm. D0 overlays may be
clustered only within the one nine-case packet because each case has a separate
rubric cell and one overlay cannot affect another case's assigned runtime
passage. If a control does not exhibit its registered failure in at least five
samples, Prompt 4 must return `reject-no-control-failure` or
`needs-more-evidence`; it may not infer efficacy from historical results.

## C1 Contribution And Pruning Boundary

C1 adds no unit, wording, file, metadata, pointer, caller, owner, Return, or
relationship. Therefore:

- C1 contribution proof is `N/A`;
- no B0-versus-C1 behavioral arm exists;
- no contribution credit is available; and
- exact B0/C1 byte identity is the only C1 claim.

Prompt 4 may audit and prove the exact package but performs no
pruning-equivalence work. Its accepted exact C1 becomes the
behavior-complete pre-prune control only if the later Pruning Pass identifies
a material cut. Historical `implement-pruning-pre-prune` bytes do not become
this epoch's control.

## Relationships And Boundaries

Relationship delta: none. Implement remains explicit-only. Direct users,
To Tickets, Diagnosis, and Improve Codebase may recommend or select it without
automatic execution. Implement invokes TDD for settled red-testable behavior,
Diagnosis for uncertain bug facts, Review for ordinary trees, and Convergent
PR Review for local PR or bounded high-risk trees. It recommends and stops at
To Tickets for shaping and Repo Bootstrap for setup. Foreign owners retain
their procedures and Return to the same Implement owner.

No Prompt 3 change to `docs/synthesis/skill-context-relationships.md` is
authorized or required.

## Proof, Limitations, And Residual Load

Construction proof:

- exact two-file candidate inventory;
- candidate and canonical component SHA-256 equality;
- candidate, manifest, and canonical tree-hash equality;
- explicit-only metadata and existing canonical contract anchors;
- focused relationship trace with no proposed delta;
- `python -m scripts.validate_skills`;
- `git diff --check`; and
- `git diff --cached --check`.

Observed results:

```text
canonical tree = candidate tree =
b918d2762505a69d1ba577533a2b9bd040133188b0e0c6dee4cb7e78351b4f7c
candidate inventory = SKILL.md, agents/openai.yaml
component byte comparisons = equal
focused promoted Implement contract = 1 passed
skill validation = passed
relationship trace = existing edges match; delta none
git diff --check = passed
git diff --cached --check = passed
```

The full suite was not run because neither a shared machine contract nor the
test harness changed. Behavior evaluation is deliberately pending Prompt 4.

No candidate-specific machine test was added. The manifest validator already
checks candidate inventory and hashes, while the existing promoted Implement
contract test fixes canonical identity and anchors; duplicating that contract
would add no new structural proof.

Residual load is the viability floor itself: singleton admission, setup and
selection authority, owner/worker separation, claim and isolation, Charter,
bounded semantic proof, TDD/Diagnosis routing, one review route, finite Repair,
Lock and closeout, and typed Returns. Removing any unit leaves a required
contract or safety boundary unowned.

Residual gaps for Prompt 4 and later:

1. the nine D0/B0 families require fresh current-model samples;
2. live dirty-index Lock and connector mutations remain unrun;
3. the packet is bounded and does not cover every repository or tracker;
4. exact backend build, token, and latency telemetry may remain unavailable;
5. upstream remote freshness is unverified; and
6. Implement facet research remains pending but is not decision-changing.

Prompt 3 is complete when the exact shared corpus, checkpoint, unit map,
current-epoch packet, manifest, and lifecycle pointer read back; focused
structural and relationship checks, skill validation, and both diff checks
pass; canonical and unrelated work remain untouched; and the run stops before
Prompt 4.

## Prompt 4 Disposition

Deploy Prompt 4 accepted the exact shared B0/C1 tree
`b918d2762505a69d1ba577533a2b9bd040133188b0e0c6dee4cb7e78351b4f7c`
without package or manifest repair. Five fresh D0 samples ran before five
fresh B0 samples. B0 passed all 50 scored case decisions with no critical
failure. No D0 family exhibited its registered failure in five samples, so
every tested wording-efficacy claim is `reject-no-control-failure`.

The affected units remain protected by independently required local intent,
authority, machine, relationship, safety, Return, and completion contracts;
viability is accepted without a causal wording-efficacy claim. C1 contribution
remains `N/A`, relationship delta remains `none`, and this epoch performs no
pruning-equivalence work.

The decision-complete record is
[`2026-07-23-implement-runtime-no-change-behavior-eval.md`](2026-07-23-implement-runtime-no-change-behavior-eval.md).
The only recommended successor at Prompt 4 completion was the Deploy Pruning
Pass.

## Pruning Pass Disposition

The Deploy Pruning Pass audited every instruction-bearing passage in the exact
Prompt 4-accepted two-file package once. All 40 passages are `keep`; no
`collapse`, `disclose`, or `delete` candidate reduced a named load without
weakening an independently required B0-01 through B0-13 contract.

Disposition: `complete`, `pruning-not-needed`. Final C1 remains byte-identical
at
`b918d2762505a69d1ba577533a2b9bd040133188b0e0c6dee4cb7e78351b4f7c`.
Package, byte, word, common-path, ownership, branch, and unused-surface deltas
are zero. No current-epoch pre-prune fixture was created and no behavioral
wave ran. The complete cut ledger and exact proof are in
[`2026-07-23-implement-runtime-no-change-pruning.md`](2026-07-23-implement-runtime-no-change-pruning.md).
The only recommended successor is Deploy Prompt 5.

## Prompt 5 Promotion And Installation Evidence

Deploy Prompt 5 verified the accepted Prompt 4 and final Pruning Pass identity
before mutation:

```text
accepted C1 = final C1 = candidate = canonical =
b918d2762505a69d1ba577533a2b9bd040133188b0e0c6dee4cb7e78351b4f7c
inventory = SKILL.md, agents/openai.yaml
SKILL.md =
7a8b391a92609e08e1b8f70526981381ddb9336c218aed8d3c28cc4fdb618b65
agents/openai.yaml =
d1331038252c7012dceebe0627884e12bb2a9975263b974bf9a991168d6dc3b8
```

Canonical integration was a no-op read-back: no canonical byte was rewritten,
no relationship surface changed, and the accepted Prompt 4 behavior evidence
was reused without another wave. The active synthesis now records the promoted
runtime, demoted efficacy claims, exact proof pointers, rejected alternatives,
deliberate non-changes, and residual gaps without a pending construction
instruction.

Only the Implement experimental two-file tree and its manifest entry were
removed. The remaining manifest entries are `codebase-design`,
`parallel-implement`, `repo-bootstrap`, `skill-router`, `to-questionnaire`,
and `wayfinder`; their content was preserved.

Both managed installation dry-runs reported:

```text
Managed skills: 25 in C:\Users\steve\.agents\skills
Unchanged skills: 25
Global bootstrap: present
```

The authorized changed cohort was therefore empty. No synchronization or
global-bootstrap mutation was required. Canonical and installed read-back
both produced:

```text
b918d2762505a69d1ba577533a2b9bd040133188b0e0c6dee4cb7e78351b4f7c
```

Final promotion proof:

```text
focused promoted Implement contract = 1 passed
full suite = 204 passed, 4 skipped
skill validation = passed
candidate directory = absent
candidate manifest entry = absent
relationship delta = none
```

The skipped tests are the repository's existing platform-conditional cases;
no failure or new skip was observed. Git delivery remains pending under the
bare campaign mode.
