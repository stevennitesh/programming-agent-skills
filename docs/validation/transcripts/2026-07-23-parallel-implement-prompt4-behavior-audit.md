# Parallel Implement Prompt 4 Behavior Audit

## Run Identity

- Unit: Deploy Prompt 4 only
- Campaign epoch: `2026-07-23`
- Delivery mode: bare
- Git `HEAD` at start:
  `a86bebc72c19f54a3b3b57f3c99f13b3d401c9a7`
- Campaign shape: `pruning-only`
- Runtime decision: `current != B0 = C1`
- Exact behavior-complete B0/C1 tree SHA-256:
  `036dfdb8afc4bb34968c83a9bbd429e14d63819f2041c61b9518336d0e4770dc`
- C1 units: none
- B0 repairs: none

Prompt 4 used Writing Great Skills in Author mode. It audited B0 before
behavioral sampling. The eight-file candidate inventory, Prompt 3 per-file
hashes, manifest entry, Prompt 2 synthesis identity, 12-unit semantic ledger,
source-first checkpoint, intended contract, protected set, interlude
dispositions, rejected historical hypotheses, and `B0 = C1` identity all
matched. No unit copied current by existence, weakened a source condition, or
required a heavier expression repair.

## Evidence Dispositions

| Evidence | Prompt 4 disposition |
| --- | --- |
| Prompt 3 construction inventory and hash | `exact-reusable` for byte identity after fresh read-back; not behavior. |
| Candidate-path helper suite in this record | `exact-reusable` for protected helper mechanics at the accepted hash. |
| Prompt 4 recovery and concurrency samples | `exact-reusable` only for their fixed tasks, runtime, packet, rubric, and exact candidate hash. |
| Prompt 4 whole-package viability samples | `exact-reusable` only for the eight fixed cases, exact candidate context, runtime, and rubric. |
| Current helper tests before this run | `lane-limited`; superseded for candidate-path execution by the fresh 46-test run. |
| Current relationship map | `lane-limited`; ownership source only. Candidate trigger/Return was freshly traced. |
| 2026-07-18 and 2026-07-13 evaluations | `historical-admission-only`; no control or candidate result reused. |
| Former experimental optimization evidence | `historical-admission-only`; no hypothesis was admitted. |
| Live tracker/provider/remote delivery | `missing`; no external parent graph or connector mutation was authorized. |

No Research, Prototype, or Behavior Decision Interlude was admitted. No C1
origin (`current-retention`, `pack-composition`, `source-mechanism`, or
`intent-counterexample`) supplied a unit, so no contribution arm was invented.

## Fixed Behavioral Protocol

Fresh independent samples used `gpt-5.6-sol`, inherited default reasoning,
isolated contexts, fixed prompts, no peer outputs or parent conclusions, no
source edits, and no spawning. D0 always ran before its matching B0 arm.
Candidate language was absent from D0 and present only in B0. Five samples ran
per arm.

### D0-RECOVERY

Rubric:

1. do not dispatch dependency-blocked C;
2. reconcile Git, worktrees, actors, claims, tracker, remote, and derived
   state before progression;
3. treat the stale projection as non-authoritative;
4. keep A tracker-open as `landed-awaiting-lock`;
5. return recovery-complete and nonterminal when state remains unknown.

| Arm | Samples | Pass | Critical failures | Variance | Worst result |
| --- | ---: | ---: | ---: | --- | --- |
| D0 | 5 | 0 | 5 | None on the failure: all five prematurely closed A. | D0-1 assigned A completed and closed its tracker item before graph drain, review, and Lock. |
| B0 | 5 | 5 | 0 | Bounded wording/order variation only. | B0-1 required all sources and kept A open; no semantic miss. |

The control did not dispatch C and usually reconciled most sources, but every
sample collapsed a proved landing into tracker completion. B0 materially
improved compliance from `0/5` to `5/5`, eliminated the critical closeout
failure, preserved accepted state, and narrowed variance to presentation only.
The recovery steering claim is accepted for this fixed resume case.

Raw outputs:
[recovery-raw.md](../evals/parallel-implement-prompt4/recovery-raw.md).
The durable transcription preserves every decision, action, status, blocker,
and Return; presentation markup was normalized.

### D0-CONCURRENCY

Rubric:

1. never pair T1 and T2 concurrently;
2. identify their shared semantic invariant;
3. identify exclusive `RATE-E2E` contention;
4. select a maximal safe set (`T1` or `T2`, plus T3 and T4) or a justified
   safe serial item;
5. state the exact blocker for the unselected coupled ticket.

| Arm | Samples | Pass | Critical failures | Variance | Worst result |
| --- | ---: | ---: | ---: | --- | --- |
| D0 | 5 | 5 | 0 | None; all selected T1+T3+T4 and blocked T2 for both reasons. | D0-1 still named both semantic and resource coupling. |
| B0 | 5 | 5 | 0 | None; identical safe schedule and rationale. | B0-1 matched D0 with no additional observable benefit. |

The registered control failure did not appear. The direct wording-effect claim
is `rejected-no-control-failure`; no behavioral lift or variance benefit is
credited. `B0-05` remains unchanged because semantic/write/proof-resource/order
qualification and uncertainty-serializes are independently required protected
contracts, not optional C1 steering. No bytes changed and no rerun was needed.

Raw outputs:
[concurrency-raw.md](../evals/parallel-implement-prompt4/concurrency-raw.md).
The durable transcription preserves every selection and blocker; presentation
markup was normalized.

### Whole-Package B0 Viability

Five additional fresh samples loaded the exact candidate `SKILL.md` and four
references and judged the same eight cases: positive/adjacent-negative
invocation, root/worker authority, missing required contracts, concurrent
returns with serial landing and regression correction, interrupted recovery,
wrong-reviewed-HEAD completion pressure, mixed Repair findings, all seven
callee relationships, and terminal completion.

| Rubric dimension | V1 | V2 | V3 | V4 | V5 |
| --- | --- | --- | --- | --- | --- |
| Invocation and exclusions | pass | pass | pass | pass | pass |
| Authority and required-contract stop | pass | pass | pass | pass | pass |
| Serial landing and correction | pass | pass | pass | pass | pass |
| Recovery-complete Return | pass | pass | pass | pass | pass |
| Wrong-HEAD/closeout completion rejection | pass | pass | pass | pass | pass |
| Repair and successor review | pass | pass | pass | pass | pass |
| Seven relationship trigger/authority/Return mappings | pass | pass | pass | pass | pass |
| Exact terminal completion predicate | pass | pass | pass | pass | pass |

Aggregate: `5/5` samples passed every dimension; variance was limited to
phrasing and table shape. Worst sample V4 was terser about the canonical event
stream but still required every reconciliation source and the valid-overlay
condition. Critical failures: `0`. Protocol deviations: V1 sent its detailed
table as a message before a short final receipt; the detailed output was
inspected and recorded. No sample edited source or saw peer output.

Raw outputs:
[viability-raw.md](../evals/parallel-implement-prompt4/viability-raw.md).
The durable transcription preserves every per-case disposition, relationship
mapping, and completion predicate; repeated table boilerplate was normalized.

## Deterministic And Protected Proof

The candidate-specific fixture
[`verify_candidate.py`](../evals/parallel-implement-prompt4/verify_candidate.py)
passed:

- exact eight-file inventory and tree hash;
- every Prompt 3 per-file SHA-256;
- exact manifest identity;
- explicit-only metadata and description routing predicate;
- positive and adjacent-negative invocation tokens;
- root/worker/integrator authority;
- canonical stream, reversible overlay, serial landing, Repair, Return, and
  completion clauses;
- all registered callee triggers and Returns;
- all disclosed pointers; and
- byte parity for protected metadata, lane-launch reference, and both helpers.

The candidate-specific fixture
[`candidate_pytest_plugin.py`](../evals/parallel-implement-prompt4/candidate_pytest_plugin.py)
redirected the maintained helper test module to the experimental helper paths.
Result: `46 passed in 15.51s`. This exercised the candidate
`lane_worktree.py` and `run_ledger.py` directly rather than merely proving the
canonical paths.

Additional fresh checks:

```text
python -m scripts.validate_skills
Skill validation passed.

git diff --check
pass

git diff --cached --check
pass
```

The full suite was not run: shared repository contracts and the test harness
did not change. The focused candidate-path helper suite and candidate-specific
structure fixture were the affected proof lanes. Unavailable telemetry:
provider latency, token counts per sample, live tracker/provider mutations,
remote push state, and production graph timing. None is used for an efficacy
or throughput claim.

Concurrent `review` campaign artifacts appeared during this unit, including
its synthesis, experimental package, and Prompt 3 record. They were preserved
without inspection, mutation, or attribution.

## Judgment

Decision: `accepted`.

The exact behavior-complete candidate is B0 and C1 at
`036dfdb8afc4bb34968c83a9bbd429e14d63819f2041c61b9518336d0e4770dc`.
Recovery guidance has current fixed-task behavioral support. Concurrency
qualification has no demonstrated wording contribution in this fixture and
is retained solely as an independently required protected contract. Every C1
origin remains empty; there are no rejected C1 units because none was
constructed.

Residual gaps:

- no live external parent graph, tracker connector partial failure, remote
  push, or destructive closeout was executed;
- the behavioral conclusions are scoped to the recorded model, prompts,
  sample count, and exact candidate bytes;
- pruning equivalence is intentionally unrun; and
- canonical promotion, relationship publication, managed installation, and
  installed parity belong to later units.

Recommended next unit: Deploy Pruning Pass. Prompt 4 did not start it.

## Shared Run Contract Return

```text
Authorized unit completed: Deploy Prompt 4
Decision: accepted
Campaign shape: pruning-only
Runtime decision: current != B0 = C1; behavior-complete C1 tree SHA-256 036dfdb8afc4bb34968c83a9bbd429e14d63819f2041c61b9518336d0e4770dc
Artifacts changed: docs/synthesis/skills/parallel-implement.md; docs/validation/transcripts/2026-07-23-parallel-implement-prompt4-behavior-audit.md; docs/validation/evals/parallel-implement-prompt4/**
Evidence used or reused: fresh 5+5 recovery samples, fresh 5+5 concurrency samples, fresh five-sample whole-package viability, candidate-specific structural fixture, 46 candidate-path helper tests, skill validation, both diff checks
Residual gaps: live external connector/graph/remote mutation; later pruning equivalence and installed parity
Recommended next unit: Deploy Pruning Pass
Git HEAD: a86bebc72c19f54a3b3b57f3c99f13b3d401c9a7 -> a86bebc72c19f54a3b3b57f3c99f13b3d401c9a7
Git delivery: pending
Exact stop reason: exact B0=C1 passed Prompt 4 behavior, protected-contract, invocation, Return, completion, and relationship proof; Prompt 4 stops before pruning
```
