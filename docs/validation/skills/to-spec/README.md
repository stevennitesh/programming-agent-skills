# To Spec Validation

This directory owns historical and fresh per-skill validation for `to-spec`.
Historical campaigns preserve their original schema and runtime labels.
Evaluation directories use stable `EV-...` identities.
