# To Tickets P1 Candidate

This candidate record references authoritative owners instead of copying
their identities or decisions. The intended contract and M0 semantic units
remain in [Prompt 1](prompt1-m0.md), research provenance remains in the
[research packet](../../../../../research/to-tickets-deploy-2026-07-25.md), active
dispositions remain in the
[synthesis](../../../../../synthesis/skills/to-tickets.md), and the campaign
[manifest](manifest.json) owns exact identities, evidence dispositions, and
promotion-critical paths.

## Candidate And Pruning

H1-01 is `reject-no-control-deficit`: five fresh exact-M0 positive controls
all scored 4, so no H1 or wrong-condition sample ran. H1 was rederived with
zero surviving transformations, making V1 exact M0.

The complete Pruning Pass classified all 24 instruction-bearing V1 passages
`keep`. No material collapse, disclosure, or deletion survived, so the
disposition is `pruning-not-needed` and exact P1 is byte-for-byte V1.

Prompt 4 behavior evidence is exact-reusable for P1 under its frozen model,
host, reasoning configuration, tools, tasks, authority, evidence, and runtime
bytes. No new behavioral wave ran for pruning or promotion.

## Candidate-Specific Proof

The complete M0 viability suite passed 24 of 24 registered cases with zero
critical failures. Source-completeness repairs changed only affected worker
fixtures, invalidated every affected sample, refroze their identities, and
restarted each affected family gate with task and rubric fixed. The compact
[Prompt 4 results](../../../../evals/to-tickets-2026-07-25/prompt4-results.json)
own final sample identities, deviations, judgments, and aggregates.

Candidate and canonical compatibility use the shared parameterized semantic
owner in `tests/test_skill_pack_contracts.py`. It accepts an explicit package
root and expected campaign-tree identity, then checks heading-bounded
normalized semantics plus machine-consumed policy, name, tree, Return tokens,
and forbidden absence.

## Promotion Evidence

Exact P1 was promoted as the complete canonical two-file package. Canonical
read-back and campaign-tree identity matched P1. The shared semantic owner
passed against canonical at the expected identity.

Four directly affected human-prose snapshots were repaired without changing
P1: mutation read-back, canonical package targeting, delivery-route ordering,
and state-boundary ownership. Each now checks heading-bounded normalized
semantics. The affected contract file passed 59 tests; the full integration
suite passed 215 with 4 skipped. Skill validation, Markdown gates, and both
diff checks passed.

Accepted behavior evidence was reused because canonical executable bytes
equal P1. No research result was rewritten as behavioral proof, and no claim
was generalized beyond the fixed Prompt 4 conditions.

## Installation Evidence

After canonical proof, only this skill's experimental package and manifest
entry were removed. Every other experimental candidate was preserved.

The required managed-install dry-run reported 25 managed skills, only
`to-tickets` updated, and 24 unchanged. The supported installer synchronized
the authorized cohort with global bootstrap skipped. Canonical and installed
campaign-tree identities then matched byte-for-byte. The exact post-install
dry-run reported all 25 managed skills unchanged.

## Relationships, Load, And Residuals

Relationships to `$repo-bootstrap`, `$implement`, and
`$parallel-implement` retain their existing triggers, authority, and Returns;
the relationship delta is none.

P1 retains the registered 9,547-byte, 1,234-word, 171-line proxy. Its
V1-to-P1 delta is zero, and the rejected provisional H1 delta is absent. These
are exact-byte and whitespace-delimited-word proxies, not token, latency, cost,
or prevalence telemetry.

Residual gaps are live-provider behavior, idempotency, eventual consistency,
mutation durability, duplicate avoidance, token and latency telemetry, and
transfer beyond the fixed model, host, tools, tasks, authority, evidence,
configuration, and exact runtime bytes.
