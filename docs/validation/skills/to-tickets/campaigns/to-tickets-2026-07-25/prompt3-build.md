# To Tickets Deploy Campaign: Prompt 3 Build

- Skill: `to-tickets`
- Unit: Deploy Prompt 3
- Operation: `$writing-great-skills` Author
- Campaign epoch: `2026-07-25`
- Starting Git `HEAD`: `8752406cf437629787c2abec5f66c3c0e6eda8b1`
- Decision: `ready-for-prompt-4`

The content strictly between the markers is the immutable Prompt 3 stage
capsule. Its identity is the SHA-256 of those exact UTF-8 bytes, including the
first and last newline.

<!-- BEGIN TO-TICKETS PROMPT-3 BUILD CAPSULE -->

## Decision

- Campaign shape: `hypothesis-candidate`.
- M0 materializes exact M01-M22 solely from the frozen Prompt 1 checkpoint.
- H1 is exact M0 plus only H1-01; the delta is one 47-word common-inline
  passage and no new section or file.
- Current canonical and historical C0 remain separate exact comparison
  evidence. No incumbent-removal ablation or C0 behavioral arm is registered
  because Prompt 2 closed all removal risk.
- Relationships: delta `none`; no relationship publication occurred.
- Behavioral sampling: not started.

## Frozen Artifacts

The campaign manifest owns the exact M0, H1, canonical, and C0 inventories,
file identities, campaign-tree-v1 identities, passage maps, semantic owners,
forbidden absence checks, proof IDs, and runtime load profiles.

The eval packet owns worker-fixture schema v2, the root-only registration and
fixture-lint map, the reconstructible dispatch plan, and frozen dispatch and
shared-payload hashes. Distinct runtime and dispatch bytes are stored once;
resolved dispatch envelopes are reconstructed from the fixture and recorded
runtime parameters.

The candidate record references the intended contract, M0 checkpoint,
research, Prompt 2 dispositions, manifest, and semantic ledger, and adds only
the candidate-specific proof plan, Pruning Pass boundary, affected
relationships, residual load, and residual risk.

## Proof

- All Prompt 1, research, Prompt 2, synthesis, current canonical, and
  historical C0 identities matched their fixed inputs before construction.
- `lint-fixture` passed for all 32 sourced cases: exact V01-V24, five
  H1-positive cases across three registered families, and three
  wrong-condition cases.
- Every registered resolved payload passed `lint-payload`; every H1 comparison
  passed `compare-payloads`, proving equality outside `/runtime`. Returned
  dispatch and shared-payload hashes are frozen in `dispatch-plan.json`.
- One heading-bounded normalized semantic assertion owner accepts an explicit
  package root and expected campaign-tree-v1 identity. It passed unchanged
  current canonical, exact M0, and exact H1 without provisional promotion.
- Directly affected prose assertions are semantic and heading-bounded.
  Explicit-only policy, frontmatter name, tree identity, Return tokens, and
  complete-package forbidden-absence checks are machine-consumed contracts.
- Focused structural and relationship checks, skill-pack validation, Markdown
  gates, and both diff checks are required before Return and are recorded in
  the campaign manifest.

## Prompt 4 Entry

Prompt 4 may run only the pre-registered M0-first viability and H1-01
contribution protocol against these exact packages and dispatch identities.
It must not infer efficacy from this structural proof.

Residuals are M0 behavioral viability, the registered M0 quality deficit,
H1-01 contribution and variance, wrong-condition non-ceremony, protected
behavior under sampling, and transfer beyond the exact fixed evaluation
conditions.

<!-- END TO-TICKETS PROMPT-3 BUILD CAPSULE -->

Content fingerprint:
`sha256:8c7de55182c509cad48f6fc03c1f4a9cd747c46fd19e7aecfd1ec28a06fe7614`
