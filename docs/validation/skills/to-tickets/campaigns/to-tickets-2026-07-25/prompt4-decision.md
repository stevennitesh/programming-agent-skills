# To Tickets Deploy Campaign: Prompt 4 Decision

The content strictly between the markers is the immutable Prompt 4 stage
capsule. The campaign manifest owns its exact fingerprint and all shared
machine state.

<!-- BEGIN TO-TICKETS PROMPT-4 DECISION CAPSULE -->

## Decision

Prompt 4 is `accepted`. M0 is viable, H1-01 is
`reject-no-control-deficit`, and exact V1 equals M0.

- M0 / V1 campaign-tree-v1:
  `480f837cde4088e41ff7a4ada97be9df634e0ba73cba55fb9c786873d95e40d3`
- V1 materialized path: `skills/experimental/to-tickets`
- M0 immutable control path:
  `docs/validation/evals/to-tickets-2026-07-25/runtimes/m0`
- Final identity relationship: current canonical != M0 = H1 = V1
- Campaign shape after resolution: `minimum-candidate`

The accepted successor recommendation is the Pruning Pass. This unit does not
start it.

## M0 Intent Fidelity And Viability

All 22 M0 semantic units map one settled local obligation to one neutral
minimum behavior. The complete M0 audit found no research leakage,
current-by-existence protection, or beyond-minimum method ceremony.

The deterministic lane passed manifest schema, artifact identities, package
inventories, semantic-ID coverage, forbidden-semantic absence, explicit-only
invocation, typed Return literals, relationship qualification and order,
runtime transformation identity, fixture isolation, and arm equality.

The behavioral lane passed all 24 registered viability cases:

- valid / required: 24 / 24;
- critical failures: 0;
- protected-behavior regressions: 0;
- M0 runtime repairs: none.

Every semantic judgment, action, Return, completion, safety, authority,
publication-recovery, routing, and mutation-boundary obligation has current
behavioral evidence. Structural and ownership claims remain in the
deterministic lane.

## Protocol Deviation And Repair

The first-sample gates exposed source-completeness and simulated-operation
fidelity omissions in several worker fixtures. Task, rubric, model, host,
tools, runtime, and root-only evaluation fixture remained fixed.

Worker-visible repairs changed affected payload bytes, so every affected
sample received zero credit, `facts_changed` is true, all affected payload
identities were refrozen, and each affected family restarted at its
first-sample gate. The final fixture and every final dispatch pass
`lint-payload`; every comparison case retains exact runtime package as its sole
arm delta. The compact results manifest owns exact invalidated and final
sample identities.

No deviation required new intent, authority, rubric, or runtime behavior.

## H1-01 Adaptive Gate

H1-01 was registered as a situational `quality-lift` for acceptance
discrimination. Five fresh exact-M0 entry-positive controls scored:

| Case | M0 score |
| --- | ---: |
| H1P01 | 4 |
| H1P02 | 4 |
| H1P03 | 4 |
| H1P04 | 4 |
| H1P05 | 4 |

Median is 4, variance is 0, worst case is 4, critical failures are 0, and
0 of 5 scores are 2 or below. The registered trigger required at least three
scores at 2 or below or a median at 2 or below. It did not open.

H1-01 therefore receives `reject-no-control-deficit`. No H1 sample and no
wrong-condition pair ran. This is not a regression or insufficient-
contribution decision: exact M0 already demonstrated the registered quality.
Method evidence remains `independently-supported` admission evidence under its
recorded limits; exact wording efficacy was neither needed nor claimed.

## V1, Load, And Protected Behavior

H1 was rederived from M0 plus zero surviving transformations. The experimental
package now matches the exact M0 tree, so V1 is exact M0.

- V1 has 2 files, 1,234 whitespace-delimited words, and 9,547 UTF-8 bytes
  under the registered proxy.
- H1-01's provisional 47-word, 319-byte common-inline delta is removed.
- Relationship topology and every caller/callee predicate remain unchanged.
- No current-only or historical incumbent-removal risk remains unresolved.
- All M01-M22 behavior, required compatibility, safe failure, irreversible
  order, Return, completion, mutation boundary, and no-successor behavior
  remain protected.

## Evidence And Limits

The authoritative compact results record is
`docs/validation/skills/to-tickets/evals/EV-to-tickets-prompt4-20260725-01/prompt4-results.json`.
Prompt 3 evidence was reused only for deterministic construction, identity,
relationship, machine, and fixture-isolation lanes. Historical behavior
evidence remained admission-only or lane-limited.

Residual gaps are live-provider behavior, installed identity, exact
idempotency and eventual-consistency behavior in real trackers, and transfer
beyond the fixed model, host, reasoning configuration, tools, tasks,
authority, evidence, and runtime bytes. Per-sample token and latency telemetry
were unavailable and were not decision-bearing because the registered
candidate gate closed on control quality before H1.

<!-- END TO-TICKETS PROMPT-4 DECISION CAPSULE -->

Content fingerprint:
`sha256:8e90fcd7621fa80b0a9536b5806bfb5b53694bd0e273a485515d0e1965d38518`
