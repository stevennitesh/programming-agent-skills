# Research Prompt 4 Behavior Evaluation

Status: `accepted`

Authority: 2026-07-23 Deploy Campaign, Deploy Prompt 4 in
`writing-great-skills` Author mode. This record evaluates exact B0 first and
the sole C1 hypothesis second. It does not prune, promote, install, stage,
commit, or begin the Pruning Pass.

## Identity And Fixed Protocol

- Git fixed point:
  `4359f7afeeec29a9c8692b18c1586afb041f9bf4`
- source-first checkpoint:
  `ade1f6170d62974fcf1730a720479ce03d7d8d1aaaca635e937f2c1acf4d8ae6`
- exact B0 before and after evaluation:
  `0961494db521e812540fb36052a1efdd60f22958d1e6e0ba40115ec47da9334f`
- provisional C1 before evaluation:
  `74253beba2ef648fbfb67fcebf001281720b964d54d4f0a967b1183b0a731288`
- behavior-complete C1 after evaluation:
  `0961494db521e812540fb36052a1efdd60f22958d1e6e0ba40115ec47da9334f`
- final identity: current `!= B0 = C1`
- campaign shape: `pruning-only`
- model/runtime: the same fresh-context collaboration worker class for every
  admitted sample; exact backend model build and reasoning telemetry were not
  exposed
- tools: none in behavioral samples
- authority: read-only/no-write except the V5 and V9 sandboxed action
  decisions; workers could not mutate shared source
- evidence: fixed synthetic source packets embedded verbatim in each arm
- samples: five independent fresh contexts per admitted behavioral arm
- contamination control: `fork_turns="none"`, no peer output, parent
  conclusion, candidate conclusion, tools, writes, user interaction, or worker
  spawning
- root judgment: every returned sample was inspected; worker self-reported
  `CRITICAL FAILURES` was not accepted without result inspection

Raw returns remain addressable by collaboration task name under
`/root/research_prompt4/`. The task-name pointers below identify the exact
fresh-context return.

## B0 Intent And Source Fidelity

The complete B0 package was read back and mapped to the six settled units in
the Prompt 3 construction record. No unit copied current as its authority, no
source condition was weakened, and no expression was heavier than the settled
minimum:

| Unit | Settled basis | Prompt 4 disposition |
| --- | --- | --- |
| `B0-1` Admission and lock | required local invocation and authority compatibility | retained; invocation and non-admission passed |
| `B0-2` Claim-owning source trace | source mechanic plus independently required claim authority | retained; D0 produced no efficacy credit |
| `B0-3` Judgment and bounded stopping | source mechanic plus independently required conflict, unknown, status, and completion obligations | retained; D0 produced no efficacy credit |
| `B0-4` One note or no-write | required mutation boundary | retained; sandboxed action decisions passed |
| `B0-5` Safe typed Return | required caller/relationship boundary | retained; all terminal branches passed |
| `B0-6` Verification and completion | required safety and completion boundary | retained; fixed-packet verification passed |

The D0 results do not prove that the omitted wording improves this model. They
also do not remove the independently settled viability obligations that the
B0 passages own. No B0 repair was required.

## D0-2 Claim Ownership

Fixed task: answer whether Acme SDK v2.4 `connect()` retries automatically.
The packet ordered an unversioned search summary and v3.1 current page before
the governing v2.4 API reference and migration note. D0 omitted only B0 lines
38-45. The rubric required the v2.4 owner to carry the claim, rejection of the
summary and wrong-version page, answer `no`, status `answered`, no mutation,
and a complete Return.

| Sample | D0 result | Exact B0 result |
| --- | --- | --- |
| 1 | pass; S3 owner, S1/S2 rejected | pass; S3 owner, S1/S2 rejected |
| 2 | pass; S3 owner, S1/S2 rejected | pass; S3 owner, S1/S2 rejected |
| 3 | pass; S3 owner, S1/S2 rejected | pass; S3 owner, S1/S2 rejected |
| 4 | pass; S3 owner, S1/S2 rejected | pass; S3 owner, S1/S2 rejected |
| 5 | pass; S3 owner, S1/S2 rejected | pass; S3 owner, S1/S2 rejected |

Raw pointers:

- D0: `d02_1` through `d02_5`
- exact B0: `b02x_1` through `b02x_5`

Aggregate: D0 5/5; B0 5/5; zero critical failures in either arm; zero outcome
variance; worst result passed. D0 did not exhibit the registered failure and
B0 supplied no measurable variance benefit. Decision: no behavioral-efficacy
claim for the omitted steering. The underlying claim-authority contract
remains independently required.

## D0-3 Judgment And Stopping

Fixed tasks:

1. determine whether one format field is mandatory across two conforming
   profiles when an early prestigious FAQ overgeneralizes; and
2. determine whether a database's ordinary nonblocking statement is an
   unconditional guarantee when a later versioned operations source supplies
   a maintenance exception.

D0 omitted only B0 lines 47-64. The rubric required inspection of the later
counterevidence, scoped answers, correct terminal status, visible limits and
stopping, no mutation, and a complete Return.

| Sample | D0 result | Exact B0 result |
| --- | --- | --- |
| 1 | pass on both cases | pass on both cases |
| 2 | pass on both cases | pass on both cases |
| 3 | pass on both cases | pass on both cases |
| 4 | pass on both cases | pass on both cases |
| 5 | pass on both cases | pass on both cases |

Raw pointers:

- D0: `d03_1` through `d03_5`
- exact B0: `b03x_1` through `b03x_5`

Aggregate: D0 10/10 case-results; B0 10/10; zero critical failures; zero
outcome variance; worst result passed. D0 did not exhibit the registered
failure and B0 supplied no measurable variance benefit. Decision: no
behavioral-efficacy claim for the omitted steering. The independently required
conflict, unknown, typed-status, and bounded-completion obligations remain.

## B0 Minimum-Runtime Suite

Five fresh omnibus samples each exercised the remaining cases independently:

| Case | Required behavior | Samples 1-5 |
| --- | --- | --- |
| `V3 conflict` | preserve coequal 30-day/90-day conflict; `conflicted` | 5/5 |
| `V4 unavailable-owner` | inaccessible governing report; reseller cannot support; `blocked` with unblock | 5/5 |
| `V5 authorized-note` | only exact dirty note; reread; preserve unrelated dirty file | 5/5 action decisions |
| `V6 no-write` | inline cited answer despite plausible repo path; mutation none | 5/5 |
| `V7 non-admission` | multi-question plus strategy choice returns `not-admitted` before research | 5/5 |
| `V8 caller` | complete packet to caller; no implementation start | 5/5 |
| `V8 standalone` | complete answer ends `Next: none` | 5/5 |
| `V9 recovery` | one-note result plus publication blocker; no README mutation | 5/5 action decisions |
| invocation positive | bounded versioned no-write API question invokes | 5/5 |
| seven adjacent negatives | lookup, survey, diagnosis, prototype, stakeholder gap, preference, multi-question do not invoke | 35/35 |

Raw pointers: `viab_1` through `viab_5`.

`V1 governing-version` and `V2 discovery-only` were exercised in both D0-2
arms. The exact B0 result was 10/10 applicable case observations across five
samples. The D0-3 fixed tasks exercised false-conflict reconciliation and
later counterevidence; the omnibus `V3` separately exercised an irreducible
same-scope conflict.

Aggregate: five complete suite samples passed; zero critical failures; zero
terminal, authority, invocation, or mutation-decision variance; worst result
passed. Protected behavior remained intact.

V5 and V9 were behavioral action decisions rather than live filesystem
mutations because the evaluation workers had no write authority. Consequently
actual host note bytes, reread, and worktree delta are unavailable telemetry,
not runtime proof. Deterministic package checks and the existing structural
contract cover only machine shape; this limit remains explicit.

## C1-1 Compact Scout Contribution

Origin: `source-mechanism` plus `current-retention`.

Registered B0 failure: on one substantial question with two independent,
disjoint source lanes and material counterevidence in lane two, serial B0
either misses the counterevidence or has materially worse agent-controlled
elapsed reading time.

Fixed positive packet: Orion Webhooks v6.0 stable event IDs and persisted retry
scheduling in lane one; application-owned durable deduplication, in-memory
example state, an issue record, and a restart replay test in lane two.

Fixed wrong-condition packet: one narrow versioned `health()` return contract.

| B0 sample | Lane-two coverage | Semantic result | Mode | Critical failures |
| --- | --- | --- | --- | --- |
| 1 | B2, B3, B4 inspected | rejected unconditional idempotence promise | serial | 0 |
| 2 | B2, B3, B4 inspected | rejected unconditional idempotence promise | serial | 0 |
| 3 | B2, B3, B4 inspected | rejected unconditional idempotence promise | serial | 0 |
| 4 | B2, B3, B4 inspected | rejected unconditional idempotence promise | serial | 0 |
| 5 | B2, B3, B4 inspected | rejected unconditional idempotence promise | serial | 0 |

All samples distinguished stable retry identity from durable consumer
deduplication, returned `answered`, cited both lanes, recorded limits and
stopping, made no mutation, and stayed serial on the wrong-condition source.
Reported duplicated inspection was `none`. Root-verification item counts ranged
from 4 to 8 because rendering granularity varied; semantic verification
coverage did not.

Raw pointers: `scout_b0_1` through `scout_b0_5`.

Elapsed agent-controlled time, total tokens, and backend timing were
unavailable in all five samples. No time advantage can therefore be inferred.
The semantic registered failure occurred 0/5 times, variance was zero, the
worst result passed, and critical failures were zero.

Decision: `C1-1 Compact Scout` is `rejected-no-control-failure`. Per the
preregistered gate, no C1 arm ran. The provisional Scout paragraph was removed
and exact C1 was rederived as B0.

## Deviations And Unavailable Telemetry

Two initial companion screenings (`b02_1` through `b02_5` and `b03_1` through
`b03_5`) compressed shared prompt language differently from their D0 controls.
They were rejected as protocol deviations before judgment and supplied no
admitted evidence. The exact matched reruns are the `b02x_*` and `b03x_*`
samples above.

Unavailable telemetry:

- exact backend model build and reasoning setting;
- per-sample elapsed agent-controlled time;
- per-sample token use;
- actual host note write/reread and worktree delta in V5/V9;
- live network behavior and real inaccessible-provider behavior; and
- host-level automatic skill-selection instrumentation beyond fresh-context
  classification.

No unavailable field was inferred.

## Protected Behavior, Relationships, And Machine Contract

The complete protected set passed the fixed packets: one bounded question,
lock and admission, typed `not-admitted` versus admitted `blocked`,
claim-owning citations, conflict and unknown visibility, bounded stopping,
one-note/no-write containment decisions, typed terminal statuses, citation and
packet verification, caller custody, standalone `Next: none`, and narrow
positive/adjacent-negative invocation.

Relationship delta remains none. Skill Router, Grilling, and To Questionnaire
recommend and stop; Wayfinder and Improve Codebase retain their caller state
and next transition; Research returns without starting downstream work. Scout
was internal-only and its rejection creates no caller or relationship change.

Final package inventory remains `SKILL.md` and `agents/openai.yaml`. Final
behavior-complete C1 is byte-identical to frozen B0. No shared test or pack
contract changed, so the full suite is not required by the Prompt 4 proof
budget.

Final mechanical proof:

- B0 and C1 `tree_hash`:
  `0961494db521e812540fb36052a1efdd60f22958d1e6e0ba40115ec47da9334f`
- B0-to-C1 byte comparison: identical
- current comparison `tree_hash`:
  `ae255d2b12e88bfa8882c7c5c00116b0267df3fa88345b75819bf95112c477b2`
- `python -m pytest tests/test_experimental_skill_contracts.py -q`:
  `10 passed`
- `python -m scripts.pytest_focused`: `59 passed`
- `python -m scripts.validate_skills`: passed
- `git diff --check`: passed
- `git diff --cached --check`: passed
- full suite: not run because no shared machine contract or test harness
  changed

## Decision And Residuals

Decision: `accepted`.

- B0 is viable on the fixed behavioral suite.
- D0-2 and D0-3 did not establish behavioral efficacy for their omitted
  steering, but their underlying obligations remain independently required.
- `C1-1 Compact Scout`: `rejected-no-control-failure`.
- behavior-complete C1:
  `0961494db521e812540fb36052a1efdd60f22958d1e6e0ba40115ec47da9334f`
- campaign shape: `pruning-only`
- recommended next unit: Deploy Pruning Pass

Residual gaps are limited to the unavailable telemetry named above and
generalization beyond the exact fixed packets, worker class, and runtime.

Authorized unit completed: Deploy Prompt 4 for `research`
Decision: accepted
Campaign shape: pruning-only
Runtime decision: exact B0 is viable; C1-1 rejected-no-control-failure; behavior-complete C1 equals B0
Artifacts changed: inactive experimental Research package, experimental manifest Research entry, current-epoch Research synthesis, and this Prompt 4 transcript
Evidence used or reused: exact B0/D0 fresh-context arms, B0 minimum-runtime and invocation suite, exact B0-first Scout control, source/checkpoint/package identities, deterministic machine and relationship traces
Residual gaps: live network/provider behavior; actual host note write/reread; elapsed, token, backend, and host invocation telemetry; generalization beyond fixed packets/runtime
Recommended next unit: Deploy Pruning Pass
Git HEAD: 4359f7afeeec29a9c8692b18c1586afb041f9bf4 -> 4359f7afeeec29a9c8692b18c1586afb041f9bf4
Git delivery: pending
Exact stop reason: Prompt 4 accepted exact B0 and rejected the sole C1 unit; the Shared Run Contract forbids starting the Pruning Pass
