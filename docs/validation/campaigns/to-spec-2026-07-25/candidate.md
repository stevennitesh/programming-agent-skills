# To Spec Prompt 4 Candidate

The intended contract and minimum semantic units remain authoritative in
[Prompt 1](prompt1-m0.md). Research provenance remains in the
[research packet](../../../research/skill-facets/to-spec/2026-07-25-deploy-research.md),
and the active dispositions remain in the
[synthesis](../../../synthesis/skills/to-spec.md) and campaign
[manifest](manifest.json).

Prompt 4 accepted exact V1 as M0 plus H1-01 at `campaign-tree-v1`
`47c223639318b041e6c86e6144b7fb23399634ead73e18ddcf306ab8242effeb`.
The [Prompt 4 decision](prompt4-decision.md) owns the human decision and the
[compact results](../../evals/to-spec-2026-07-25/prompt4-results.json) own
per-sample identities and judgments.

## Candidate-Specific Proof Result

Exact M0 passed V01-V13. H1-01 accepted after five viable controls showed the
registered ceremonial-content deficit, five exact candidate samples produced
a median score lift of one with narrowed variance, and the repaired dense
wrong-condition pair preserved every material family. H1-02 is
`reject-no-control-deficit`: five exact M0 controls already chose adequate
scope-matched portfolios, so no candidate or wrong-condition sample ran.

The [worker fixture](../../evals/to-spec-2026-07-25/worker-fixture.json) owns
worker-visible facts and requested outputs. The
[root registration](../../evals/to-spec-2026-07-25/root-registration.json)
owns hypotheses, expected weaknesses, rubrics, scoring, configurations, and
acceptance budgets. The
[fixture-lint map](../../evals/to-spec-2026-07-25/fixture-lint-map.json) and
[dispatch plan](../../evals/to-spec-2026-07-25/dispatch-plan.json) own
referential coverage and frozen dispatch identities. AW01 received one
source-completeness repair with task and rubric fixed; its affected payloads
were refrozen and prior affected samples received zero credit.

Candidate compatibility uses the shared parameterized semantic owner in
`tests/test_skill_pack_contracts.py`. V1 equals the previously proved isolated
H1-01 package byte-for-byte. H1-02 remains isolated historical candidate
evidence and is absent from V1.

## Pruning Pass Boundary

The Pruning Pass may begin from exact V1. It must reuse the shared semantic
owner, protect all surviving U01-U16 and accepted H1-01 behavior, and test only
material cut groups against exact V1. It may reduce wording and package load
but may not add behavior, change authority, revive H1-02, or infer efficacy
from structural proof.

## Relationships And Residual Load

Relationship delta is `none`. Preserve Wayfinder and Improve Codebase
recommend-and-stop callers, the vocabulary-only `$codebase-design` load, and
the unstarted `$repo-bootstrap` and `$to-tickets` recommendations. No
relationship publication occurs before promotion.

Accepted H1-01 adds 33 whitespace-delimited words, within its 45-word maximum,
and no file, section, helper, or conditional reference. Rejected H1-02
contributes no V1 load. Token, latency, cost, and prevalence telemetry are
unavailable.

Residual risk is bounded to live-provider recovery, idempotency, eventual
consistency, mutation durability, and transfer beyond the fixed model, host,
tools, tasks, authority, evidence, configuration, source families, and tracker
simulations.
