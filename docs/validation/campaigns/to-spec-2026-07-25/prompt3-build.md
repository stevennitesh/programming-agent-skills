# To Spec Deploy Campaign: Prompt 3 Build

- Skill: `to-spec`
- Unit: Deploy Prompt 3
- Operation: `$writing-great-skills` Author
- Campaign epoch: `2026-07-25`
- Starting Git `HEAD`: `b9750babea998c4bc0f5809972a8651934218eca`
- Decision: `ready-for-prompt-4`

The content strictly between the markers is the immutable Prompt 3 stage
capsule. Its identity is the SHA-256 of those exact UTF-8 bytes after the LF
ending the start marker through the byte immediately before the end marker.

<!-- BEGIN TO-SPEC-2026-07-25-PROMPT3-CAPSULE -->

## Decision

- Campaign shape: `hypothesis-candidate`.
- Exact M0 materializes U01-U16 solely from the frozen Prompt 1 checkpoint at
  `campaign-tree-v1`
  `8d1a70398c4eaa0386574d2594cf41658900facc92d3ab7fb6e20358c92d76eb`.
- H1-01 and H1-02 remain isolated because their entry predicates, arm deltas,
  rubrics, and dispositions differ. Their exact isolated identities are
  `47c223639318b041e6c86e6144b7fb23399634ead73e18ddcf306ab8242effeb`
  and
  `aab3d073179fcedcc18845d950073db2ccd2b1b4dd2f8ac6f76da982ad87f68d`.
- The inactive combined candidate contains exact M0 plus only both admitted
  passages at `059fd225266a521fdd61ac74ef86c8fcf471e6477b74ea70e62c5d2e8298cbf1`.
  It receives no bundled causal credit.
- Current canonical and the exact user-designated historical C0 remain
  separate comparison evidence. No incumbent-removal behavioral arm is
  registered because Prompt 2 closed all removal risk.
- Relationships remain delta `none`. Behavioral sampling did not run.

## Frozen Construction

The campaign manifest owns exact package inventories, bytes, hashes, passage
maps, semantic owners, forbidden absence checks, runtime identities, load
proxies, and proof IDs. Every M0 instruction-bearing passage maps to exactly
one U01-U16 unit; every required unit has one owner, runtime passage set, and
Prompt 3 plus Prompt 4 proof. Each H1-only passage records its admitted origin,
method-evidence classification, and `quality-lift` contribution mode.

The runtime packages contain only `SKILL.md` and explicit-only
`agents/openai.yaml`. H1-01 adds 33 whitespace-delimited words; H1-02 adds 38;
the combined candidate adds 71. They add no helper, section, conditional
reference, authority, Return, completion condition, exclusion, or
relationship edge.

## Frozen Evaluation Inputs

The worker fixture uses schema version 2 and contains 25 sourced cases:
V01-V13, five H1-01 entry-positive cases across two families plus one
wrong-condition case, and five H1-02 entry-positive cases across two families
plus one wrong-condition case. The root registration keeps hypotheses,
expected weaknesses, rubrics, scoring, candidate terms, and conclusions out of
worker-visible bytes.

The root-only fixture-lint map binds every criterion, decision input, and
required output semantic to worker-visible fact IDs or observable operations.
All 25 cases passed `lint-fixture`. All 37 resolved envelopes passed
`lint-payload`; all 12 M0/H1 pairs passed `compare-payloads`, proving equality
outside `/runtime`. The dispatch plan freezes every returned dispatch and
shared-payload hash. Sampling status is `not-started`.

## Candidate-Aware Preflight

`assert_to_spec_semantic_contract` is the shared heading-bounded normalized
semantic owner. It accepts an explicit package root and expected
campaign-tree identity. It passed unchanged current canonical, exact M0, both
isolated H1 candidates, and the combined inactive candidate without
provisional promotion.

Human prose is checked as normalized section semantics. Explicit-only policy,
frontmatter name, package inventory and tree identity, typed Return tokens,
and complete-package forbidden absence are machine-consumed contracts.
Relationship, invocation, context, structural, Markdown, skill-validation,
diff, mutation-boundary, and unchanged-HEAD gates are recorded in the
manifest.

## Prompt 4 Entry

Prompt 4 may run only the registered M0-first viability and two independent H1
protocols against the exact frozen runtimes, fixtures, configurations, and
dispatch identities. Structural proof supplies no wording-efficacy credit.

Residuals are exact M0 behavioral viability, both registered control deficits,
each H1 contribution and variance, each wrong-condition non-regression,
live-provider recovery, idempotency, eventual consistency, mutation
durability, unavailable token/latency/cost telemetry, and transfer beyond the
fixed future conditions.

<!-- END TO-SPEC-2026-07-25-PROMPT3-CAPSULE -->

Content fingerprint:
`sha256:ab140cc953322c659184981efefc2919fd85813ab230626fa2ea3db40d275586`
